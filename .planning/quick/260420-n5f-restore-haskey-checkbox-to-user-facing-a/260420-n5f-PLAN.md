---
phase: quick-260420-n5f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/ViewModels/AccountViewModels/EditProfileViewModel.cs
  - EuphoriaInn.Service/Controllers/Admin/AccountController.cs
  - EuphoriaInn.Service/Views/Account/Edit.cshtml
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sees a 'Has Building Key' checkbox on the Account/Edit form"
    - "HasKey value is pre-populated from the current user record on GET"
    - "Saving the form persists the HasKey change to the database"
  artifacts:
    - path: "EuphoriaInn.Service/ViewModels/AccountViewModels/EditProfileViewModel.cs"
      provides: "HasKey property on EditProfileViewModel"
      contains: "bool HasKey"
    - path: "EuphoriaInn.Service/Views/Account/Edit.cshtml"
      provides: "form-check checkbox for HasKey"
      contains: "asp-for=\"HasKey\""
    - path: "EuphoriaInn.Service/Controllers/Admin/AccountController.cs"
      provides: "GET population and POST persistence of HasKey"
      contains: "HasKey = user.HasKey"
  key_links:
    - from: "EditProfileViewModel.HasKey"
      to: "User.HasKey"
      via: "AccountController Edit GET assignment and POST assignment"
      pattern: "HasKey = "
---

<objective>
Restore the HasKey checkbox to the user-facing Account/Edit form.

Purpose: HasKey tracks who physically holds a building key — it is informational only, not a security permission. It was mistakenly removed from the user-facing edit form during SEC-04 (which correctly stripped it from privilege-elevation paths). Users should be able to see and update their own key-holder status.

Output: HasKey property back in EditProfileViewModel, checkbox in Edit.cshtml, GET/POST assignments in AccountController.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore HasKey to EditProfileViewModel, controller, and view</name>
  <files>
    EuphoriaInn.Service/ViewModels/AccountViewModels/EditProfileViewModel.cs,
    EuphoriaInn.Service/Controllers/Admin/AccountController.cs,
    EuphoriaInn.Service/Views/Account/Edit.cshtml
  </files>
  <action>
Make three targeted changes:

1. **EditProfileViewModel.cs** — add the HasKey property after IsDungeonMaster:
   ```csharp
   [Display(Name = "Has Building Key")]
   public bool HasKey { get; set; }
   ```

2. **AccountController.cs Edit GET** (around line 112) — add HasKey to the model initialiser:
   ```csharp
   HasKey = user.HasKey,
   ```

3. **AccountController.cs Edit POST** (around line 132, after `user.Email = model.Email`) — add:
   ```csharp
   user.HasKey = model.HasKey;
   ```

4. **Account/Edit.cshtml** — insert a form-check block between the Email field and the first `<hr>` (before the Password Management section). Match the style used in the admin EditUser view:
   ```html
   <div class="mb-3 form-check">
       <input asp-for="HasKey" class="form-check-input" type="checkbox" />
       <label asp-for="HasKey" class="form-check-label">Has Building Key</label>
   </div>
   ```

Do NOT change the IsDungeonMaster display (read-only field, not a checkbox — the view already hides it). Do NOT add any role-change logic; HasKey is a plain boolean on the User domain model, persisted via `userService.UpdateAsync(user)` which already exists in the POST.
  </action>
  <verify>
    <automated>cd /mnt/c/Repos/quest-board && dotnet build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Build succeeds with 0 errors. EditProfileViewModel has a HasKey bool property. The Edit GET populates it from user.HasKey. The Edit POST assigns model.HasKey back to user.HasKey before calling UpdateAsync. The Edit.cshtml renders a form-check checkbox bound to HasKey.
  </done>
</task>

</tasks>

<verification>
After the single task completes:
- `dotnet build` exits 0
- Navigate to /Account/Edit — "Has Building Key" checkbox is visible
- Toggling and saving the checkbox updates the value (confirmed by re-opening the form)
</verification>

<success_criteria>
HasKey checkbox appears on the user-facing Account/Edit page. The value round-trips correctly: GET loads current value, POST saves new value. Build and existing tests remain green.
</success_criteria>

<output>
After completion, create `.planning/quick/260420-n5f-restore-haskey-checkbox-to-user-facing-a/260420-n5f-SUMMARY.md`
</output>
