---
phase: 24-email-confirmation-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Domain/Models/User.cs
  - EuphoriaInn.Domain/Extensions/UserExtensions.cs
  - EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs
  - EuphoriaInn.UnitTests/Services/UserExtensionsTests.cs
  - EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs
autonomous: true
requirements: [REQ-24-01, REQ-24-04]
must_haves:
  truths:
    - "User domain model carries a bool EmailConfirmed that flows up from UserEntity via AutoMapper convention"
    - "WhereEmailConfirmed() returns only users whose EmailConfirmed is true"
    - "UserManagementViewModel exposes EmailConfirmed for the Admin/Users view to drive button visibility"
  artifacts:
    - path: "EuphoriaInn.Domain/Models/User.cs"
      provides: "EmailConfirmed property + Equals/GetHashCode inclusion"
      contains: "public bool EmailConfirmed"
    - path: "EuphoriaInn.Domain/Extensions/UserExtensions.cs"
      provides: "WhereEmailConfirmed extension on IEnumerable<User> (D-06, D-07)"
      contains: "WhereEmailConfirmed"
    - path: "EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs"
      provides: "EmailConfirmed flag for button visibility"
      contains: "public bool EmailConfirmed"
    - path: "EuphoriaInn.UnitTests/Services/UserExtensionsTests.cs"
      provides: "WhereEmailConfirmed unit tests (Wave 0 — covers REQ-24-04)"
      contains: "WhereEmailConfirmed"
    - path: "EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs"
      provides: "Job-guard test scaffold (Wave 0 — REQ-24-04 for the string[] jobs)"
      contains: "EmailConfirmationJobGuard"
  key_links:
    - from: "EuphoriaInn.Domain/Models/User.cs"
      to: "EuphoriaInn.Repository/Automapper/EntityProfile.cs"
      via: "CreateMap<UserEntity, User>() maps EmailConfirmed by convention — no ForMember needed"
      pattern: "EmailConfirmed"
    - from: "EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs"
      to: "EuphoriaInn.Domain/Models/User.cs"
      via: "AdminController.Users sets ViewModel.EmailConfirmed from User.EmailConfirmed"
      pattern: "EmailConfirmed"
---

<objective>
Establish the domain-layer foundation for the email confirmation flow: add `bool EmailConfirmed` to the `User` domain model (D-01), create the `WhereEmailConfirmed()` extension on `IEnumerable<User>` (D-06, D-07), surface `EmailConfirmed` on `UserManagementViewModel`, and create the two Wave 0 test files that downstream plans verify against.

Purpose: Every other plan in this phase depends on `User.EmailConfirmed` existing. This plan makes the field available to jobs, controllers, and views without any layer violation. `EmailConfirmed` is already a column on `UserEntity` (inherited from `IdentityUser<int>`), so no EF migration is required — AutoMapper convention mapping picks it up.
Output: Modified `User`, new `UserExtensions`, modified `UserManagementViewModel`, two new test files.
</objective>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- `User.EmailConfirmed` — new `bool` property on `EuphoriaInn.Domain.Models.User`
- `UserExtensions` — new static class in `EuphoriaInn.Domain.Extensions`
- `UserExtensions.WhereEmailConfirmed(this IEnumerable<User>)` — new extension method
- `UserManagementViewModel.EmailConfirmed` — new `bool` property
- `UserExtensionsTests` — new test class
- `EmailConfirmationJobGuardTests` — new test class
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-email-confirmation-flow/24-CONTEXT.md
@.planning/phases/24-email-confirmation-flow/24-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add EmailConfirmed to User domain model and UserManagementViewModel</name>
  <files>EuphoriaInn.Domain/Models/User.cs, EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs</files>
  <read_first>
    - EuphoriaInn.Domain/Models/User.cs (current model — note the hand-coded Equals at lines 24-31 and GetHashCode at lines 33-36; both include HasKey)
    - EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs (current ViewModel — flat bool properties IsAdmin/IsDungeonMaster/IsPlayer)
    - EuphoriaInn.Repository/Automapper/EntityProfile.cs (confirms CreateMap<UserEntity, User>() is convention-based; UserEntity inherits EmailConfirmed from IdentityUser<int>)
  </read_first>
  <behavior>
    - A User with EmailConfirmed=true is NOT equal to an otherwise-identical User with EmailConfirmed=false (Equals returns false; GetHashCode differs)
    - Two Users identical in all fields including EmailConfirmed ARE equal
    - UserManagementViewModel.EmailConfirmed defaults to false
  </behavior>
  <action>
    In `EuphoriaInn.Domain/Models/User.cs`, add a public auto-property `public bool EmailConfirmed { get; set; }` immediately after the existing `public bool HasKey { get; set; }` line. Then update the hand-coded equality members to include the new field: in `Equals`, add `&& EmailConfirmed == user.EmailConfirmed` to the boolean chain (after the `HasKey == user.HasKey` term); in `GetHashCode`, add `EmailConfirmed` as the final argument to `HashCode.Combine(Id, Name, Email, HasKey)` so it becomes `HashCode.Combine(Id, Name, Email, HasKey, EmailConfirmed)`. Do NOT add a `ForMember` call anywhere — AutoMapper's `CreateMap<UserEntity, User>()` maps `EmailConfirmed` by name convention since `UserEntity` already exposes it via `IdentityUser<int>`.
    In `EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs`, add `public bool EmailConfirmed { get; set; }` after the existing `public bool IsPlayer { get; set; }` property. This flat property (not `User.EmailConfirmed` nesting) is what the view condition `@if (!userModel.EmailConfirmed)` reads in plan 03 per the UI-SPEC ViewModel contract.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Domain EuphoriaInn.Service</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Domain/Models/User.cs` contains the line `public bool EmailConfirmed { get; set; }`
    - `User.Equals` includes `EmailConfirmed == user.EmailConfirmed`
    - `User.GetHashCode` call is `HashCode.Combine(Id, Name, Email, HasKey, EmailConfirmed)`
    - `EuphoriaInn.Service/ViewModels/AdminViewModels/UserManagementViewModel.cs` contains `public bool EmailConfirmed { get; set; }`
    - `dotnet build EuphoriaInn.Domain EuphoriaInn.Service` exits 0
  </acceptance_criteria>
  <done>User and UserManagementViewModel both carry EmailConfirmed; equality members updated; solution compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create UserExtensions.WhereEmailConfirmed and Wave 0 test stubs</name>
  <files>EuphoriaInn.Domain/Extensions/UserExtensions.cs, EuphoriaInn.UnitTests/Services/UserExtensionsTests.cs, EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs</files>
  <read_first>
    - EuphoriaInn.Domain/Extensions/ServiceExtensions.cs (namespace pattern: `EuphoriaInn.Domain.Extensions`; static class with extension methods)
    - EuphoriaInn.Domain/Models/User.cs (the type the extension operates on — has EmailConfirmed after Task 1)
    - EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs (test conventions: xunit.v3 [Fact], NSubstitute, FluentAssertions `.Should()`; GlobalUsings already imports FluentAssertions and Xunit)
    - EuphoriaInn.UnitTests/GlobalUsings.cs (confirms which usings are global so test files don't re-import them)
  </read_first>
  <behavior>
    - WhereEmailConfirmed on a list of [confirmed=true, confirmed=false, confirmed=true] returns exactly the two confirmed users
    - WhereEmailConfirmed on an all-unconfirmed list returns an empty sequence
    - WhereEmailConfirmed on an empty sequence returns empty (no exception)
  </behavior>
  <action>
    Create `EuphoriaInn.Domain/Extensions/UserExtensions.cs` in namespace `EuphoriaInn.Domain.Extensions` with a `public static class UserExtensions` containing one method: `public static IEnumerable<User> WhereEmailConfirmed(this IEnumerable<User> users) => users.Where(u => u.EmailConfirmed);`. Add `using EuphoriaInn.Domain.Models;` for the `User` type and ensure `System.Linq` is available (global or explicit).
    Create `EuphoriaInn.UnitTests/Services/UserExtensionsTests.cs` in namespace `EuphoriaInn.UnitTests.Services` with a `public class UserExtensionsTests`. Add three `[Fact]` tests exercising the three behaviors above: build `List<User>` instances with `new User { Id = N, Name = "...", EmailConfirmed = true/false }`, call `.WhereEmailConfirmed().ToList()`, and assert with FluentAssertions (e.g. `result.Should().HaveCount(2)` and `result.Should().OnlyContain(u => u.EmailConfirmed)`). Add `using EuphoriaInn.Domain.Extensions;` and `using EuphoriaInn.Domain.Models;`.
    Create `EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs` in namespace `EuphoriaInn.UnitTests.Services` with a `public class EmailConfirmationJobGuardTests`. This is the Wave 0 scaffold for the string[]-array job guard (QuestService finalize/date-change call sites, wired in plan 04). Add ONE placeholder `[Fact]` named `Placeholder_UntilPlan04WiresGuard` whose body is a single passing assertion `true.Should().BeTrue();` and a `// TODO(plan 04): replace with QuestService guard assertions per REQ-24-04` comment. Plan 04 replaces the body with real assertions. Do NOT leave the test empty (an empty test would falsely pass the filter without a class to discover).
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~UserExtensionsTests|FullyQualifiedName~EmailConfirmationJobGuard"</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Domain/Extensions/UserExtensions.cs` declares `WhereEmailConfirmed` returning `IEnumerable<User>` filtered on `u.EmailConfirmed`
    - `EuphoriaInn.UnitTests/Services/UserExtensionsTests.cs` contains 3 `[Fact]` tests covering confirmed-only, all-unconfirmed-empty, and empty-input cases
    - `EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs` exists with class `EmailConfirmationJobGuardTests` and a discoverable placeholder Fact
    - `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~UserExtensionsTests|FullyQualifiedName~EmailConfirmationJobGuard"` passes (all green)
  </acceptance_criteria>
  <done>WhereEmailConfirmed extension exists and is unit-tested; the job-guard Wave 0 scaffold class is discoverable and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job recipient list → SMTP send | The `EmailConfirmed` filter is the control that prevents email being sent to addresses the owner never verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing | Email sent to unverified address | mitigate | `WhereEmailConfirmed()` filters recipients on `User.EmailConfirmed`; jobs in plan 04 apply it before send. This plan creates the filter + tests proving it returns only confirmed users. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all libraries already present (ASP.NET Identity, AutoMapper, xunit/NSubstitute). No `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- `dotnet build` succeeds for Domain and Service projects
- `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~UserExtensionsTests|FullyQualifiedName~EmailConfirmationJobGuard"` is green
- `EmailConfirmed` appears in User.cs, UserManagementViewModel.cs, and is referenced by WhereEmailConfirmed
</verification>

<success_criteria>
- `User.EmailConfirmed` exists and participates in equality
- `WhereEmailConfirmed()` returns only confirmed users (3 unit tests green)
- `UserManagementViewModel.EmailConfirmed` exists for the view
- Both Wave 0 test files exist and are discoverable
</success_criteria>

<output>
Create `.planning/phases/24-email-confirmation-flow/24-01-SUMMARY.md` when done
</output>
