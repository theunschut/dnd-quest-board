---
phase: 25-confirmation-email-razor-template
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Domain/Interfaces/IEmailService.cs
  - EuphoriaInn.Domain/Services/EmailService.cs
  - EuphoriaInn.UnitTests/Services/EmailServiceTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "IEmailService no longer declares SendQuestFinalizedEmailAsync or SendQuestDateChangedEmailAsync"
    - "EmailService no longer implements SendQuestFinalizedEmailAsync or SendQuestDateChangedEmailAsync"
    - "EmailService.SendAsync (the generic method) is unchanged and still present"
    - "The two unit tests covering the deleted methods are removed; the remaining EmailService tests still pass"
    - "Solution compiles with no references to the removed methods anywhere"
  artifacts:
    - path: "EuphoriaInn.Domain/Interfaces/IEmailService.cs"
      provides: "IEmailService interface with only the generic SendAsync method"
      contains: "Task SendAsync(string toEmail, string subject, string htmlBody)"
    - path: "EuphoriaInn.Domain/Services/EmailService.cs"
      provides: "EmailService implementation without the obsolete typed methods"
      contains: "public async Task SendAsync(string toEmail, string subject, string htmlBody)"
  key_links:
    - from: "EuphoriaInn.Domain/Services/EmailService.cs"
      to: "EuphoriaInn.Domain/Interfaces/IEmailService.cs"
      via: "EmailService implements IEmailService — interface and implementation must stay in sync after removal"
      pattern: "class EmailService.*: IEmailService"
---

<objective>
Remove the two `[Obsolete]` typed email methods — `SendQuestFinalizedEmailAsync` and `SendQuestDateChangedEmailAsync` — from `IEmailService` (declarations) and `EmailService` (bodies), and delete the two unit tests that exercise them (D-07, D-08). These were marked `[Obsolete]` in Phase 21 with "Will be removed in a future phase"; all real call sites were migrated to `SendAsync` in Phase 21, so the only remaining references are the interface, the implementation, and their own two tests (verified: no external `.cs` callers).

Purpose: Eliminate dead, obsolete API surface now that no caller remains, keeping `IEmailService` to the single generic `SendAsync` contract used by every Hangfire job.
Output: Trimmed interface, trimmed implementation, two tests deleted — solution still builds and the remaining suite is green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-confirmation-email-razor-template/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove obsolete typed methods from IEmailService and EmailService</name>
  <files>EuphoriaInn.Domain/Interfaces/IEmailService.cs, EuphoriaInn.Domain/Services/EmailService.cs</files>
  <read_first>
    - EuphoriaInn.Domain/Interfaces/IEmailService.cs — current state: SendAsync (line 5–6) plus the two [Obsolete] declarations on lines 9–12. Only the two obsolete declarations and their comment lines are removed; SendAsync stays.
    - EuphoriaInn.Domain/Services/EmailService.cs — current state: CreateSmtpClient helper (lines 14–31), SendQuestFinalizedEmailAsync body (lines 33–70), SendQuestDateChangedEmailAsync body (lines 72–112), and the generic SendAsync (lines 114–136). Remove the two method bodies; keep CreateSmtpClient and SendAsync untouched.
    - .planning/phases/25-confirmation-email-razor-template/25-CONTEXT.md — decisions D-07 and D-08, and the <canonical_refs> "Obsolete Method Removal Targets" block.
  </read_first>
  <action>
    In `IEmailService.cs`: delete the two `[Obsolete(...)]` method declarations `SendQuestFinalizedEmailAsync(...)` and `SendQuestDateChangedEmailAsync(...)` along with the "Legacy typed methods" comment line that precedes them. Keep the generic `Task SendAsync(string toEmail, string subject, string htmlBody);` declaration and its "Generic method" comment. The interface ends with a single method.

    In `EmailService.cs`: delete the entire `public async Task SendQuestFinalizedEmailAsync(...)` method body and the entire `public async Task SendQuestDateChangedEmailAsync(...)` method body. Keep the `CreateSmtpClient()` private helper and the `public async Task SendAsync(...)` method exactly as they are (SendAsync is the only public send method that must remain). Do not change the class declaration, constructor, `_settings` field, or any using directive that is still needed (`System.Net`, `System.Net.Mail`, the logging/options usings remain required by CreateSmtpClient and SendAsync).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Domain</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SendQuestFinalizedEmailAsync" EuphoriaInn.Domain/Interfaces/IEmailService.cs` returns 0.
    - `grep -c "SendQuestDateChangedEmailAsync" EuphoriaInn.Domain/Interfaces/IEmailService.cs` returns 0.
    - `grep -c "SendQuestFinalizedEmailAsync" EuphoriaInn.Domain/Services/EmailService.cs` returns 0.
    - `grep -c "SendQuestDateChangedEmailAsync" EuphoriaInn.Domain/Services/EmailService.cs` returns 0.
    - `IEmailService.cs` still contains the literal `Task SendAsync(string toEmail, string subject, string htmlBody)`.
    - `EmailService.cs` still contains the literal `public async Task SendAsync(string toEmail, string subject, string htmlBody)` and still contains `private SmtpClient? CreateSmtpClient()`.
    - `dotnet build EuphoriaInn.Domain` succeeds with no errors.
  </acceptance_criteria>
  <done>Both obsolete methods are gone from interface and implementation; SendAsync and CreateSmtpClient remain; Domain compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two EmailServiceTests covering the removed methods</name>
  <files>EuphoriaInn.UnitTests/Services/EmailServiceTests.cs</files>
  <read_first>
    - EuphoriaInn.UnitTests/Services/EmailServiceTests.cs — current state: the two [Fact] tests to delete are `SendQuestFinalizedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` (lines 24–30) and `SendQuestDateChangedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` (lines 32–38). The remaining tests — `Constructor_WithValidOptions_DoesNotThrow`, `EmailService_ConstructorUsesIOptionsEmailSettings`, `EmailServiceSource_ContainsSingleSmtpClientConstruction`, `EmailServiceSource_ContainsAppUrlSubstitution`, `SendAsync_WhenSmtpNotConfigured_ReturnsWithoutException` — must stay and keep passing.
  </read_first>
  <action>
    Delete only the two `[Fact]` test methods `SendQuestFinalizedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` and `SendQuestDateChangedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` (each including its `[Fact]` attribute line and full method body). Leave the `Create` helper and all five other tests untouched. Do not remove any using directives that the remaining tests still need.
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailServiceTests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SendQuestFinalizedEmailAsync" EuphoriaInn.UnitTests/Services/EmailServiceTests.cs` returns 0.
    - `grep -c "SendQuestDateChangedEmailAsync" EuphoriaInn.UnitTests/Services/EmailServiceTests.cs` returns 0.
    - `EmailServiceTests.cs` still contains `SendAsync_WhenSmtpNotConfigured_ReturnsWithoutException` and `EmailService_ConstructorUsesIOptionsEmailSettings`.
    - `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailServiceTests"` reports the five remaining tests passed, 0 failed.
  </acceptance_criteria>
  <done>The two obsolete-method tests are gone; the five remaining EmailService tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan removes dead API surface and dead tests — no runtime input path, no network/IO change, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02 | Tampering | Removing a method that still has a live caller would break the build | mitigate | Pre-removal grep over all `*.cs` confirmed the only references are the interface, implementation, and the two tests being deleted; `dotnet build` of the full solution in the phase gate catches any missed caller. |
| T-25-03 | Denial of Service | Accidentally deleting SendAsync or CreateSmtpClient would break every email job | mitigate | Acceptance criteria assert the literal SendAsync signature and CreateSmtpClient remain present after the edit. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `dotnet build` (full solution) succeeds — proves no remaining caller of the removed methods anywhere in Service/Repository/UnitTests.
- `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailServiceTests"` → 5 passed.
- Full suite green alongside Plan 01: `dotnet test` (phase gate).
</verification>

<success_criteria>
- Neither obsolete method appears in any `.cs` file in the solution.
- SendAsync and CreateSmtpClient remain intact; Domain and the full solution build.
- The two obsolete tests are deleted; remaining EmailService tests pass.
</success_criteria>

<output>
Create `.planning/phases/25-confirmation-email-razor-template/25-02-SUMMARY.md` when done.
</output>
