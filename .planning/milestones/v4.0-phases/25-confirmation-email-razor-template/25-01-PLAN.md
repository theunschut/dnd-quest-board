---
phase: 25-confirmation-email-razor-template
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs
autonomous: true
requirements: []
must_haves:
  truths:
    - "ConfirmationEmailJob.ExecuteAsync calls IEmailRenderService.RenderAsync<ConfirmEmail> with UserName, CallbackUrl, and AppUrl render parameters"
    - "ConfirmationEmailJob.ExecuteAsync calls IEmailService.SendAsync with the recipient email, the fixed subject, and the HTML returned by the render service"
    - "Both tests pass under the existing dotnet test suite"
  artifacts:
    - path: "EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs"
      provides: "Happy-path wiring tests for ConfirmationEmailJob"
      contains: "ConfirmationEmailJobTests"
      min_lines: 50
  key_links:
    - from: "EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs"
      to: "EuphoriaInn.Service/Jobs/ConfirmationEmailJob.cs"
      via: "constructs ConfirmationEmailJob with mocked IServiceScopeFactory and invokes ExecuteAsync"
      pattern: "new ConfirmationEmailJob"
    - from: "EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs"
      to: "EuphoriaInn.Service/Components/Emails/ConfirmEmail.razor"
      via: "asserts RenderAsync<ConfirmEmail> render-parameter dictionary keys"
      pattern: "RenderAsync<.*ConfirmEmail>"
---

<objective>
Add `EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs` with two happy-path tests proving `ConfirmationEmailJob` wires `IEmailRenderService` and `IEmailService` correctly (D-01, D-02, D-03). The job itself already exists (implemented during Phase 24) — this plan only adds the missing test coverage required to close Phase 25.

Purpose: The job is the only Phase 25 deliverable lacking automated verification. These tests prove the render-parameter dictionary keys match `ConfirmEmail.razor` parameters and that the rendered HTML flows through to `SendAsync` with the exact subject line.
Output: One net-new test file containing two `[Fact]` tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-confirmation-email-razor-template/25-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConfirmationEmailJobTests with render-parameter and send-wiring tests</name>
  <files>EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs</files>
  <read_first>
    - EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs — canonical IServiceScopeFactory → IServiceScope → IServiceProvider mock chain (constructor lines 21–51); copy verbatim and only change the registered services. Note `_scopeFactory.CreateAsyncScope().Returns(new AsyncServiceScope(scope))` and `serviceProvider.GetService(typeof(T)).Returns(...)` per service. AsyncServiceScope is a struct wrapping a Substitute IServiceScope.
    - EuphoriaInn.Service/Jobs/ConfirmationEmailJob.cs — the system under test. Primary constructor takes `(IServiceScopeFactory scopeFactory, ILogger<ConfirmationEmailJob> logger)`. ExecuteAsync signature is `(string toEmail, string userName, string callbackUrl, CancellationToken cancellationToken = default)`. It resolves IEmailRenderService, IEmailService, IOptions<EmailSettings> via GetRequiredService; renders ConfirmEmail with dictionary keys nameof(ConfirmEmail.UserName)/CallbackUrl/AppUrl where AppUrl comes from emailSettings.AppUrl; then calls emailService.SendAsync(toEmail, "Confirm your D&D Quest Board account", html).
    - EuphoriaInn.Service/Components/Emails/ConfirmEmail.razor — parameter names UserName, CallbackUrl, AppUrl (the dictionary keys asserted in Test 1).
    - .planning/phases/25-confirmation-email-razor-template/25-CONTEXT.md — decisions D-01 through D-06 and the <specifics> block (Arg.Is dictionary assertion form, exact subject string).
  </read_first>
  <behavior>
    - Test 1 (`ExecuteAsync_CallsRenderAsync_WithCorrectParameters`, D-02): given userName "TestUser", callbackUrl "https://example.com/confirm?token=abc", and AppUrl "https://example.com" from mocked EmailSettings, RenderAsync<ConfirmEmail> is Received(1) with a Dictionary<string, object?> whose UserName entry equals "TestUser", CallbackUrl entry equals the passed callbackUrl, and AppUrl entry equals "https://example.com".
    - Test 2 (`ExecuteAsync_CallsSendAsync_WithRenderedHtml`, D-03): the render mock returns a known HTML sentinel (e.g. "<html>confirm-body</html>"); SendAsync is Received(1) with toEmail "player@example.com", subject "Confirm your D&D Quest Board account", and the exact sentinel HTML body.
  </behavior>
  <action>
    Create a new xUnit test class `ConfirmationEmailJobTests` in namespace `EuphoriaInn.UnitTests.Services`. In the constructor, reproduce the `SessionReminderJobTests` service-provider mock chain (D-05): substitute IServiceProvider and register IEmailRenderService, IEmailService, and IOptions<EmailSettings> via `serviceProvider.GetService(typeof(...)).Returns(...)`; substitute IServiceScope returning that provider; substitute IServiceScopeFactory whose `CreateAsyncScope()` returns `new AsyncServiceScope(scope)`. Mock IOptions<EmailSettings> with `Value.Returns(new EmailSettings { AppUrl = "https://example.com" })` per D-06. Substitute ILogger<ConfirmationEmailJob> and build the SUT as `new ConfirmationEmailJob(_scopeFactory, logger)`.

    Test 1: configure the render mock so that for any render-parameter dictionary it returns Task.FromResult of an HTML string, then call ExecuteAsync with toEmail "player@example.com", userName "TestUser", callbackUrl "https://example.com/confirm?token=abc". Assert `await _renderService.Received(1).RenderAsync<ConfirmEmail>(Arg.Is<Dictionary<string, object?>>(d => ...))` where the predicate checks the three entries keyed by `nameof(ConfirmEmail.UserName)`, `nameof(ConfirmEmail.CallbackUrl)`, `nameof(ConfirmEmail.AppUrl)` against "TestUser", the callbackUrl, and "https://example.com" respectively (use `?.ToString()` on the entry values for comparison).

    Test 2: configure `_renderService.RenderAsync<ConfirmEmail>(Arg.Any<Dictionary<string, object?>>()).Returns(Task.FromResult("<html>confirm-body</html>"))`, call ExecuteAsync with toEmail "player@example.com", then assert `await _emailService.Received(1).SendAsync("player@example.com", "Confirm your D&D Quest Board account", "<html>confirm-body</html>")`.

    Use the same `using` directives as SessionReminderJobTests plus `using EuphoriaInn.Service.Components.Emails;` for the ConfirmEmail type and `using EuphoriaInn.Service.Jobs;` for the job. Do not add a no-callbackUrl test (D-04 — out of scope).
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~ConfirmationEmailJobTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs` exists and declares `public class ConfirmationEmailJobTests` in namespace `EuphoriaInn.UnitTests.Services`.
    - Contains exactly two `[Fact]` methods named `ExecuteAsync_CallsRenderAsync_WithCorrectParameters` and `ExecuteAsync_CallsSendAsync_WithRenderedHtml`.
    - Source contains the literal `new AsyncServiceScope(` (proves the canonical scope-mock pattern is used) and `new ConfirmationEmailJob(`.
    - Source contains the literal subject string `Confirm your D&D Quest Board account`.
    - Source references `RenderAsync<` together with `ConfirmEmail` (the generic render call is asserted against the ConfirmEmail component type).
    - `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~ConfirmationEmailJobTests"` reports 2 passed, 0 failed.
  </acceptance_criteria>
  <done>Both tests pass; the render-parameter dictionary assertion and the SendAsync subject/HTML assertion are present and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan adds a unit-test file only — no runtime input, no new network/IO surface, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | Test asserts the wrong subject/keys, masking a real wiring regression | mitigate | Acceptance criteria pin the exact subject literal and the three render-parameter keys against ConfirmEmail.razor parameter names, so a drift in the job is caught. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `dotnet build` succeeds.
- `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~ConfirmationEmailJobTests"` → 2 passed.
- Full suite remains green: `dotnet test` (run by phase gate alongside Plan 02).
</verification>

<success_criteria>
- ConfirmationEmailJobTests.cs exists with the two specified tests, both green.
- No production code is modified by this plan.
</success_criteria>

<artifacts_this_phase_produces>
## Symbols CREATED (Phase 25, across both plans)

- `EuphoriaInn.UnitTests/Services/ConfirmationEmailJobTests.cs` (new file)
  - `ConfirmationEmailJobTests` (test class)
  - `ConfirmationEmailJobTests.ExecuteAsync_CallsRenderAsync_WithCorrectParameters` (test)
  - `ConfirmationEmailJobTests.ExecuteAsync_CallsSendAsync_WithRenderedHtml` (test)

## Symbols REMOVED (Phase 25, Plan 02)

- `IEmailService.SendQuestFinalizedEmailAsync` — DELETED (obsolete since Phase 21; no live callers)
- `IEmailService.SendQuestDateChangedEmailAsync` — DELETED (obsolete since Phase 21; no live callers)
- `EmailService.SendQuestFinalizedEmailAsync` — DELETED (implementation body)
- `EmailService.SendQuestDateChangedEmailAsync` — DELETED (implementation body)
- `EmailServiceTests.SendQuestFinalizedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` — DELETED (tested removed code)
- `EmailServiceTests.SendQuestDateChangedEmailAsync_WhenUsernameEmpty_ReturnsWithoutThrowing` — DELETED (tested removed code)

## Pre-existing (NOT created this phase — implemented in Phase 24)

- `EuphoriaInn.Service/Jobs/ConfirmationEmailJob.cs` — system under test (already exists)
- `EuphoriaInn.Service/Components/Emails/ConfirmEmail.razor` — already exists
- `AdminController.SendConfirmationEmail` — already enqueues the job
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/25-confirmation-email-razor-template/25-01-SUMMARY.md` when done.
</output>
