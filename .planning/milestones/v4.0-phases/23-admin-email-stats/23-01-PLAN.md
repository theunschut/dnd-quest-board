---
phase: 23-admin-email-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Domain/Models/EmailSettings.cs
  - EuphoriaInn.Service/appsettings.json
  - EuphoriaInn.Service/Program.cs
  - EuphoriaInn.Service/Services/ResendStatsAggregator.cs
  - EuphoriaInn.UnitTests/Services/ResendStatsAggregatorTests.cs
autonomous: true
requirements:
  - STATS-01
must_haves:
  truths:
    - "ResendApiKey is bindable from EmailSettings via IOptions like the other SMTP settings"
    - "A named 'Resend' HttpClient is registered with base address https://api.resend.com/ and a 15s timeout"
    - "Email records can be aggregated into Sent, Delivered, Bounced, and Failed counts by last_event"
    - "opened and clicked last_event values count toward Delivered"
    - "records older than the 30-day cutoff are excluded from the counts"
  artifacts:
    - path: "EuphoriaInn.Domain/Models/EmailSettings.cs"
      provides: "ResendApiKey config property on EmailSettings record"
      contains: "ResendApiKey"
    - path: "EuphoriaInn.Service/Services/ResendStatsAggregator.cs"
      provides: "Pure static aggregation of last_event values into the 4 stat counts with 30-day cutoff"
      contains: "Aggregate"
    - path: "EuphoriaInn.UnitTests/Services/ResendStatsAggregatorTests.cs"
      provides: "Unit tests proving STATS-01 aggregation rules"
      contains: "ResendStatsAggregatorTests"
    - path: "EuphoriaInn.Service/Program.cs"
      provides: "Named 'Resend' HttpClient registration"
      contains: "AddHttpClient(\"Resend\""
  key_links:
    - from: "EuphoriaInn.Service/Program.cs"
      to: "EuphoriaInn.Domain/Models/EmailSettings.cs"
      via: "EmailSettings already bound via AddDomainServices; ResendApiKey read through IOptions<EmailSettings>"
      pattern: "EmailSettings"
    - from: "EuphoriaInn.UnitTests/Services/ResendStatsAggregatorTests.cs"
      to: "EuphoriaInn.Service/Services/ResendStatsAggregator.cs"
      via: "test references the static Aggregate method on the aggregator"
      pattern: "ResendStatsAggregator\\.Aggregate"
---

<objective>
Establish the config and pure-logic foundation for the Admin Email Stats page: add the `ResendApiKey` property to `EmailSettings` (D-09), add the empty placeholder to `appsettings.json`, register the named `Resend` HttpClient in `Program.cs` (D-10), and extract the `last_event` aggregation rules into a testable static helper with full unit coverage.

Purpose: The aggregation rules (which `last_event` values map to which stat card, and the 30-day cutoff) are the only piece of real business logic in this phase (STATS-01). Isolating them in a static helper closes the RESEARCH.md Wave 0 unit-test gap and lets the controller in Plan 02 stay thin. This plan owns all the wiring Plan 02 depends on (config property, HttpClient registration, aggregator).

Output: `ResendApiKey` config property, named HttpClient registration, `ResendStatsAggregator` static helper, and its unit test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-admin-email-stats/23-CONTEXT.md
@.planning/phases/23-admin-email-stats/23-RESEARCH.md
@.planning/phases/23-admin-email-stats/23-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following new symbols and files:

- **New file** `EuphoriaInn.Service/Services/ResendStatsAggregator.cs` containing:
  - `public static class ResendStatsAggregator`
  - `public record ResendEmailRecord(string Id, DateTime CreatedAt, string LastEvent)` — input record with `[property: JsonPropertyName]` attributes on `id`, `created_at`, `last_event`
  - `public record ResendEmailListResponse(List<ResendEmailRecord> Data)` — `[property: JsonPropertyName("data")]`
  - `public readonly record struct ResendStatCounts(int Sent, int Delivered, int Bounced, int Failed)`
  - `public static ResendStatCounts Aggregate(IEnumerable<ResendEmailRecord> records, DateTime cutoffUtc)` — maps `last_event` to counts and excludes records with `CreatedAt < cutoffUtc`
- **New file** `EuphoriaInn.UnitTests/Services/ResendStatsAggregatorTests.cs` containing `public class ResendStatsAggregatorTests`
- **Modified** `EuphoriaInn.Domain/Models/EmailSettings.cs` — adds `public string ResendApiKey { get; init; } = string.Empty;`
- **Modified** `EuphoriaInn.Service/Program.cs` — adds named `Resend` HttpClient registration
- **Modified** `EuphoriaInn.Service/appsettings.json` — adds `"ResendApiKey": ""` to the `EmailSettings` section

Plan 02 (`AdminController.EmailStats`) consumes `ResendStatsAggregator.Aggregate`, the record types, and `EmailSettings.ResendApiKey`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResendStatsAggregator with failing unit tests, then implement</name>
  <files>EuphoriaInn.Service/Services/ResendStatsAggregator.cs, EuphoriaInn.UnitTests/Services/ResendStatsAggregatorTests.cs</files>
  <read_first>
    - .planning/phases/23-admin-email-stats/23-RESEARCH.md (Pattern 3 last_event mapping table, Pitfall 5 opened/clicked → Delivered, the aggregation switch in Code Examples)
    - .planning/phases/23-admin-email-stats/23-PATTERNS.md (nested record shapes for ResendEmailListResponse / ResendEmailRecord)
    - EuphoriaInn.UnitTests/Services/ (existing unit test layout — naming, FluentAssertions usage)
    - EuphoriaInn.UnitTests/GlobalUsings.cs (confirms `global using FluentAssertions;` — no per-file using needed)
    - EuphoriaInn.UnitTests/EuphoriaInn.UnitTests.csproj (confirms it references EuphoriaInn.Service, so a Service-layer static helper is reachable from tests)
  </read_first>
  <behavior>
    RED first — write these xUnit tests in ResendStatsAggregatorTests.cs (class-level `[Trait("Category", "EmailStats")]`) before implementing the aggregator:
    - Aggregate counts a "sent" last_event into Sent only
    - Aggregate counts "delivered", "opened", and "clicked" each into Delivered (three separate records produce Delivered == 3)
    - Aggregate counts "bounced" into Bounced only
    - Aggregate counts "failed" into Failed only
    - Aggregate ignores "delivery_delayed", "complained", and "scheduled" (none of the 4 counts increment)
    - Aggregate excludes a record whose CreatedAt is older than cutoffUtc (record at cutoff minus 1 day with a "delivered" event does NOT increment Delivered)
    - Aggregate includes a record whose CreatedAt equals or is newer than cutoffUtc
    - Aggregate over an empty sequence returns ResendStatCounts with all four counts == 0
  </behavior>
  <action>
    Create `EuphoriaInn.Service/Services/ResendStatsAggregator.cs` in namespace `EuphoriaInn.Service.Services`. Define `public readonly record struct ResendStatCounts(int Sent, int Delivered, int Bounced, int Failed)`. Define `public record ResendEmailRecord([property: JsonPropertyName("id")] string Id, [property: JsonPropertyName("created_at")] DateTime CreatedAt, [property: JsonPropertyName("last_event")] string LastEvent)` and `public record ResendEmailListResponse([property: JsonPropertyName("data")] List<ResendEmailRecord> Data)` — add `using System.Text.Json.Serialization;`. Implement `public static ResendStatCounts Aggregate(IEnumerable<ResendEmailRecord> records, DateTime cutoffUtc)`: iterate records, skip any where `CreatedAt < cutoffUtc`, then switch on `LastEvent`: `"sent"` increments Sent; `"delivered"`, `"opened"`, `"clicked"` increment Delivered; `"bounced"` increments Bounced; `"failed"` increments Failed; any other value is ignored. Return a `ResendStatCounts` with the four tallies. The `[property: JsonPropertyName(...)]` attributes are required because Plan 02 deserializes the Resend `GET /emails` response directly into these records. Write the test file first, confirm it fails to compile / fails RED, then implement the aggregator to GREEN. Do not put any HttpClient, IMemoryCache, or IOptions logic in this class — it is pure aggregation only (Plan 02 owns the HTTP call). Do not reference the literal stat-card label strings here; this class only deals with the lowercase `last_event` tokens.
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.UnitTests --filter "Category=EmailStats"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test EuphoriaInn.UnitTests --filter "Category=EmailStats"` passes with at least 8 test cases green
    - `ResendStatsAggregator.Aggregate` is `public static` and returns `ResendStatCounts`
    - A record with `LastEvent == "opened"` newer than cutoff increments Delivered (verified by a passing test asserting Delivered == 1)
    - A record with `CreatedAt < cutoffUtc` does not change any count (verified by a passing test)
    - `grep -c 'JsonPropertyName' EuphoriaInn.Service/Services/ResendStatsAggregator.cs` returns 4 (id, created_at, last_event, data)
  </acceptance_criteria>
  <done>STATS-01 aggregation rules are implemented in a pure static helper, fully unit-tested, with all `last_event` mappings (including opened/clicked → Delivered) and the 30-day cutoff exclusion proven by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add ResendApiKey config and register the named Resend HttpClient</name>
  <files>EuphoriaInn.Domain/Models/EmailSettings.cs, EuphoriaInn.Service/appsettings.json, EuphoriaInn.Service/Program.cs</files>
  <read_first>
    - EuphoriaInn.Domain/Models/EmailSettings.cs (current record — append the new property after AppUrl)
    - EuphoriaInn.Service/appsettings.json (current EmailSettings section — append the placeholder key)
    - EuphoriaInn.Service/Program.cs (lines around 79-80: AddRepositoryServices / AddDomainServices — register the HttpClient after these)
    - .planning/phases/23-admin-email-stats/23-PATTERNS.md (Program.cs named HttpClient registration block; appsettings EmailSettings block; EmailSettings change)
    - .planning/phases/23-admin-email-stats/23-RESEARCH.md (Pitfall 4 — do NOT set the Authorization header on the named client default; Pitfall 2 — keep the appsettings value empty)
  </read_first>
  <action>
    In `EuphoriaInn.Domain/Models/EmailSettings.cs`, append `public string ResendApiKey { get; init; } = string.Empty;` as the last property of the `EmailSettings` record before its closing brace — per D-09. Do not add any new DI registration for binding; `EmailSettings` is already bound via `AddDomainServices` so `IOptions<EmailSettings>.Value.ResendApiKey` works immediately. In `EuphoriaInn.Service/appsettings.json`, add `"ResendApiKey": ""` as the last property inside the existing `EmailSettings` object (the one containing `SmtpServer`, `FromName`, `AppUrl`) — keep the value an empty string; the real key is supplied at runtime via the `EmailSettings__ResendApiKey` environment variable (D-09, Pitfall 2). In `EuphoriaInn.Service/Program.cs`, after the `AddDomainServices(builder.Configuration)` call (around line 80), add `builder.Services.AddHttpClient("Resend", client => { ... })` setting `client.BaseAddress = new Uri("https://api.resend.com/")`, `client.Timeout = TimeSpan.FromSeconds(15)`, and `client.DefaultRequestHeaders.Add("Accept", "application/json")` (D-10). Do NOT set an `Authorization` / Bearer header on the named client here — Plan 02 sets it per request after confirming the key is non-empty (Pitfall 4). Do not add `services.AddMemoryCache()` — `IMemoryCache` is already registered by `AddControllersWithViews` (Pitfall 3).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build` succeeds with no errors
    - `grep -c 'ResendApiKey' EuphoriaInn.Domain/Models/EmailSettings.cs` returns 1
    - `grep -c 'ResendApiKey' EuphoriaInn.Service/appsettings.json` returns 1 and the value is an empty string (no `re_` key committed)
    - `grep -c 'AddHttpClient("Resend"' EuphoriaInn.Service/Program.cs` returns 1
    - `Program.cs` does NOT set `DefaultRequestHeaders.Authorization` on the Resend client (verified: `grep -c 'Authorization' EuphoriaInn.Service/Program.cs` returns 0 in the Resend registration block)
  </acceptance_criteria>
  <done>EmailSettings exposes ResendApiKey (D-09), appsettings.json has an empty placeholder (no secret committed), and a named "Resend" HttpClient with base address, timeout, and Accept header is registered (D-10) with no default Authorization header.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app config → Resend API | The `ResendApiKey` secret crosses from configuration into outbound HTTP requests |
| git repository ↔ secrets | `appsettings.json` is committed; a real key here would leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `appsettings.json` ResendApiKey | mitigate | Commit only an empty-string placeholder; real key supplied via `EmailSettings__ResendApiKey` env var. Acceptance criterion asserts no `re_` value committed. |
| T-23-02 | Information Disclosure | named Resend HttpClient default headers | mitigate | No Authorization/Bearer header set at registration (Pitfall 4); the secret is never baked into the singleton client. |
| T-23-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: none). No `T-23-SC` checkpoint required. |
</threat_model>

<verification>
- `dotnet build` succeeds for the full solution
- `dotnet test EuphoriaInn.UnitTests --filter "Category=EmailStats"` is green (aggregation rules proven)
- `EmailSettings.ResendApiKey` resolves through `IOptions<EmailSettings>` (no new binding registration needed — covered by existing AddDomainServices)
- No Resend API key value committed to `appsettings.json` (empty placeholder only)
</verification>

<success_criteria>
- `ResendApiKey` property exists on `EmailSettings` and binds via existing IOptions registration (D-09)
- Named `Resend` HttpClient registered with `https://api.resend.com/` base address and 15s timeout, no default auth header (D-10)
- `ResendStatsAggregator.Aggregate` maps every `last_event` per STATS-01 (sent/delivered/opened/clicked/bounced/failed) and excludes records older than the 30-day cutoff, with passing unit tests
</success_criteria>

<output>
Create `.planning/phases/23-admin-email-stats/23-01-SUMMARY.md` when done
</output>
