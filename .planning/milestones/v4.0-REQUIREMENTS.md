# Requirements Archive: D&D Quest Board — Milestone 4: Email Notifications

**Archived:** 2026-06-28 (milestone complete)
**Defined:** 2026-06-25
**Core Value:** The quest board must reliably let DMs post quests and players sign up — everything else enhances that loop.

---

## v1 Requirements (Final Status)

### Email Templates

- [x] **EMAIL-01**: An `IEmailRenderService` backed by `HtmlRenderer` (built-in .NET 10) renders Razor component templates to HTML strings; all outbound emails use this service — **COMPLETE** (Phase 21, Plan 01+02)
- [x] **EMAIL-02**: The quest-finalization email is sent as styled HTML; if a quest is re-opened and re-finalized with the **same confirmed date**, no duplicate notification is sent (tracked via `FinalizedEmailSentForDate` stored on the quest) — **COMPLETE** (Phase 21, Plan 01+03)
- [x] **EMAIL-03**: A single-quest session reminder renders as styled HTML using a dedicated Razor component template — **COMPLETE** (Phase 21, Plan 02)
- [ ] **EMAIL-04**: A digest session reminder renders multiple quests (same confirmed date, same player) as a single styled HTML email — **DEFERRED** — dropped from scope; same-day quests have never occurred in one year of operation (D-13)

### Session Reminders

- [x] **REMIND-01**: A Hangfire recurring job runs daily at 09:00 and sends session reminder emails to all players confirmed for quests whose session is the following day — **COMPLETE** (Phase 22, Plan 03)
- [ ] **REMIND-02**: A player confirmed for multiple quests on the same day receives one combined digest email, not a separate email per quest — **DEFERRED** — same as EMAIL-04; scheduling density does not yet justify the complexity
- [x] **REMIND-03**: A DM can manually trigger a session reminder from the quest manage page; it dispatches a Hangfire background job using the same send logic as the recurring job — **COMPLETE** (Phase 22, Plan 04)
- [x] **REMIND-04**: Reminders are idempotent — if the Hangfire job retries after a partial failure, already-notified players are not emailed again (tracked via `ReminderLog` table) — **COMPLETE** (Phase 22, Plan 01+03)

### Hangfire Infrastructure

- [x] **JOBS-01**: Hangfire is installed with SQL Server storage sharing the existing database; the `[HangFire]` schema is auto-created on startup — no EF Core migration required — **COMPLETE** (Phase 20)
- [x] **JOBS-02**: The Hangfire dashboard is accessible at `/hangfire` and requires the `Admin` role; unauthenticated or non-admin requests are rejected — **COMPLETE** (Phase 20)

### Admin Email Stats

- [x] **STATS-01**: Admin users can view email delivery statistics (sent, delivered, bounced, and failed counts) pulled from the Resend API using a plain `HttpClient` and a configurable `ResendApiKey` in `appsettings` — **COMPLETE** (Phase 23)

### Email Confirmation Flow

- [x] **REQ-24-01**: The Admin/Users page shows a "Send Confirmation Email" button for every user where `EmailConfirmed == false`; the button is absent for already-confirmed users — **COMPLETE** (Phase 24, Plan 01+03)
- [x] **REQ-24-02**: Clicking the button POSTs to `AdminController.SendConfirmationEmail`, generates an ASP.NET Identity confirmation token, builds a callback URL, and sends the email via `IEmailService.SendAsync` — a `TempData["Success"]` banner confirms dispatch — **COMPLETE** (Phase 24, Plan 02+03)
- [x] **REQ-24-03**: The confirmation callback `GET /Account/ConfirmEmail?userId=X&token=Y` calls `UserManager.ConfirmEmailAsync` (via `IIdentityService`), sets `EmailConfirmed = true`, and redirects to Login with a `TempData` success or error banner — **COMPLETE** (Phase 24, Plan 04)
- [x] **REQ-24-04**: Every email path (`QuestFinalizedEmailJob`, `QuestDateChangedEmailJob`, `SessionReminderJob`, `DailyReminderJob`) skips recipients whose `EmailConfirmed == false` — verified by unit tests — **COMPLETE** (Phase 24, Plan 05)

---

## Future Requirements

- Vote reminders (remind non-voters to vote on proposed dates) — deferred by user decision
- Digest batching (EMAIL-04 / REMIND-02) — revisit when same-day quest scheduling becomes common
- Webhook-based delivery tracking (real-time bounce/complaint events from Resend) — more complex than polling; revisit at scale
- Per-user email preferences (opt-out of reminders) — defer; group is small and trusting

## Out of Scope

- **Resend SDK for sending** — email delivery continues via SmtpClient → Postfix → Resend SMTP relay; no sending SDK added
- **Resend batch API** — irrelevant at current scale (17 members, SMTP relay)
- **Email verification on registration** — previously deferred; small trusted group

---

## Traceability (Final)

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| EMAIL-01 | Phase 21 | ✅ Complete |
| EMAIL-02 | Phase 21 | ✅ Complete |
| EMAIL-03 | Phase 21 | ✅ Complete |
| EMAIL-04 | Phase 22 | ⏸ Deferred (digest batching — same-day quests have never occurred) |
| REMIND-01 | Phase 22 | ✅ Complete |
| REMIND-02 | Phase 22 | ⏸ Deferred (same as EMAIL-04) |
| REMIND-03 | Phase 22 | ✅ Complete |
| REMIND-04 | Phase 22 | ✅ Complete |
| JOBS-01 | Phase 20 | ✅ Complete |
| JOBS-02 | Phase 20 | ✅ Complete |
| STATS-01 | Phase 23 | ✅ Complete |
| REQ-24-01 | Phase 24 | ✅ Complete |
| REQ-24-02 | Phase 24 | ✅ Complete |
| REQ-24-03 | Phase 24 | ✅ Complete |
| REQ-24-04 | Phase 24 | ✅ Complete |

**Summary:** 13/15 requirements complete. 2 deferred (EMAIL-04, REMIND-02 — same digest feature, explicitly dropped from scope with documented rationale).
