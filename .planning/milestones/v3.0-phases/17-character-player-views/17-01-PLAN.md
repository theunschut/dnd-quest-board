---
phase: 17-character-player-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs
autonomous: true
requirements: [CHAR-01, CHAR-02, CHAR-03, PLAYER-01]

must_haves:
  truths:
    - "REQUIREMENTS.md defines CHAR-01 (mobile character detail), CHAR-02 (mobile create form), CHAR-03 (mobile edit form), PLAYER-01 (mobile players list + desktop email removal)"
    - "REQUIREMENTS.md traceability table includes CHAR-01, CHAR-02, CHAR-03, PLAYER-01 mapped to Phase 17"
    - "MobileViewsTests.cs contains GetMobilePage_CharacterDetails_ReturnsSuccessAndMobileLayout stub"
    - "MobileViewsTests.cs contains GetMobilePage_CharacterCreate_ReturnsSuccessAndMobileLayout stub"
    - "MobileViewsTests.cs contains GetMobilePage_CharacterEdit_ReturnsSuccessAndMobileLayout stub"
    - "MobileViewsTests.cs contains GetMobilePage_PlayersIndex_ReturnsSuccessAndMobileLayout stub"
    - "All four new test methods compile and are discovered by the runner (RED expected — views do not exist yet)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 17 requirement definitions CHAR-01 through PLAYER-01"
      contains: "CHAR-01"
    - path: "EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs"
      provides: "Integration test stubs for CHAR-01..03 and PLAYER-01"
      contains: "GetMobilePage_CharacterDetails_ReturnsSuccessAndMobileLayout"
  key_links:
    - from: "MobileViewsTests.cs"
      to: "GuildMembers/Details.Mobile.cshtml"
      via: "character-detail-card CSS class + character-detail.mobile.css link assertion"
      pattern: "character-detail-card"
    - from: "MobileViewsTests.cs"
      to: "GuildMembers/Create.Mobile.cshtml"
      via: "character-form-card CSS class + character-form.mobile.css link assertion"
      pattern: "character-form-card"
    - from: "MobileViewsTests.cs"
      to: "Players/Index.Mobile.cshtml"
      via: "players-section-card CSS class + players.mobile.css link assertion"
      pattern: "players-section-card"
---

<objective>
Add CHAR-01, CHAR-02, CHAR-03, and PLAYER-01 to REQUIREMENTS.md (they are referenced in ROADMAP.md Phase 17 but not yet defined), then append four integration test stubs to MobileViewsTests.cs that will go RED immediately and GREEN after Wave 2 plans ship the mobile views.

Purpose: Establish the requirement definitions and the Nyquist sampling harness before any implementation. Plans 02–04 have a real `<automated>` command to prove completion.
Output: REQUIREMENTS.md updated with four new requirement entries + four new [Fact] test methods in MobileViewsTests.cs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-character-player-views/17-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CHAR-01, CHAR-02, CHAR-03, PLAYER-01 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the WHOLE file to understand current structure — find the "Browse Pages" section at the bottom of the v1 Requirements block, and the Traceability table at the end; new sections are inserted after Browse Pages and before the "Future Requirements" section)
    - .planning/ROADMAP.md (Phase 17 section — success criteria 1–3 are the basis for requirement wording)
    - .planning/phases/17-character-player-views/17-CONTEXT.md (domain section for exact scope; decisions D-01 through D-14 for requirement wording)
  </read_first>
  <action>
    Edit REQUIREMENTS.md to add a new "Character Views" section and a new "Players Page" section, then update the Traceability table. Insert the new sections immediately after the "Browse Pages" section and before "## Future Requirements".

    Add the following section after Browse Pages:

    ```markdown
    ### Character Views

    - [ ] **CHAR-01**: GuildMembers/Details on mobile shows character stats (level, class/level badges, description, backstory), profile photo (or fa-user fallback), and owner actions (Edit / Retire-Reactivate / Delete) in a single-column glass card layout without overflow
    - [ ] **CHAR-02**: GuildMembers/Create on mobile is a single-column scrollable form with profile picture upload at the top, stacked class entries (class select + level input + remove button each on col-12), and all inputs meeting the 44px minimum touch target
    - [ ] **CHAR-03**: GuildMembers/Edit on mobile has identical structure to Create with the current portrait thumbnail shown above the file input; all owner-only fields are reachable by vertical scroll and all inputs meet the 44px minimum touch target

    ### Players Page

    - [ ] **PLAYER-01**: Players/Index on mobile displays Dungeon Masters and Registered Players as separate single-column name lists in glass card containers; DM names tap-navigate to their DungeonMaster Profile page; no email column is shown; the desktop Players/Index.cshtml also has its email column removed
    ```

    Then add four rows to the Traceability table at the bottom (before the end of the table):

    ```markdown
    | CHAR-01 | Phase 17 | Pending |
    | CHAR-02 | Phase 17 | Pending |
    | CHAR-03 | Phase 17 | Pending |
    | PLAYER-01 | Phase 17 | Pending |
    ```
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains the exact string `CHAR-01`
    - .planning/REQUIREMENTS.md contains the exact string `CHAR-02`
    - .planning/REQUIREMENTS.md contains the exact string `CHAR-03`
    - .planning/REQUIREMENTS.md contains the exact string `PLAYER-01`
    - .planning/REQUIREMENTS.md contains the exact string `### Character Views`
    - .planning/REQUIREMENTS.md contains the exact string `### Players Page`
    - .planning/REQUIREMENTS.md contains the exact string `| CHAR-01 | Phase 17 | Pending |`
    - .planning/REQUIREMENTS.md contains the exact string `| PLAYER-01 | Phase 17 | Pending |`
  </acceptance_criteria>
  <done>REQUIREMENTS.md has four new requirement entries with correct IDs, wording aligned to ROADMAP.md success criteria, and traceability rows added to the table.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 17 integration test stubs to MobileViewsTests.cs</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs (read the WHOLE file — copy the exact authenticated request pattern from `MobileDmManage_MobileUserAgent_RendersCondensedVoteBadges`: `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(_factory, userName, email)`, then `new HttpRequestMessage(HttpMethod.Get, url)`, `request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent)`, `request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization`, `_client.SendAsync(request, TestContext.Current.CancellationToken)`)
    - EuphoriaInn.IntegrationTests/Helpers/TestDataHelper.cs (read the whole file — `CreateTestCharacterAsync(services, ownerId, name, level, status, role, dndClass)` signature is the seed method; `CreateAuthenticatedClientWithUserAsync` produces user IDs needed for ownerId)
    - EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs (verify `CreateAuthenticatedClientWithUserAsync` and `CreateTestUserAsync` signatures; confirm userId is available from the returned tuple)
    - .planning/phases/17-character-player-views/17-CONTEXT.md (D-04: character-detail-card; D-08: character-form-card; D-09/D-10: players-section-card; D-12/D-13: CSS file names)
  </read_first>
  <action>
    Append FOUR new `[Fact]` async test methods inside the existing `MobileViewsTests` class (insert before the closing brace of the class). Do not modify or remove any existing method. Reuse the existing private `GetWithUserAgentAsync` helper and `MobileUserAgent`/`DesktopUserAgent` constants already defined in the file.

    All four routes are under `[Authorize]` on GuildMembersController and PlayersController — use the authenticated HttpRequestMessage pattern throughout.

    Add a comment block before the four methods:

    ```csharp
    // -----------------------------------------------------------------------
    // Phase 17 — CHAR-01: Character Details renders glass card layout on mobile UA
    // -----------------------------------------------------------------------
    ```

    **Method 1** — `GetMobilePage_CharacterDetails_ReturnsSuccessAndMobileLayout`:
    - Seed: `CreateAuthenticatedClientWithUserAsync(_factory, "char_det17", "char_det17@test.com")` to get `(authClient, ownerUser)`.
    - Seed character: `await TestDataHelper.CreateTestCharacterAsync(_factory.Services, ownerUser.Id, "Aria Swiftblade")` to get `character`.
    - Authenticated GET `/GuildMembers/Details/{character.Id}` with `MobileUserAgent`.
    - Assert: `response.StatusCode` is `HttpStatusCode.OK`; `html.Should().Contain("character-detail-card")`; `html.Should().Contain("character-detail.mobile.css")`.

    Add separator comment:
    ```csharp
    // -----------------------------------------------------------------------
    // Phase 17 — CHAR-02: Character Create renders glass card form on mobile UA
    // -----------------------------------------------------------------------
    ```

    **Method 2** — `GetMobilePage_CharacterCreate_ReturnsSuccessAndMobileLayout`:
    - Seed: `CreateAuthenticatedClientWithUserAsync(_factory, "char_cre17", "char_cre17@test.com")` to get `(authClient, _)`.
    - Authenticated GET `/GuildMembers/Create` with `MobileUserAgent`.
    - Assert: OK; `html.Should().Contain("character-form-card")`; `html.Should().Contain("character-form.mobile.css")`.

    Add separator comment:
    ```csharp
    // -----------------------------------------------------------------------
    // Phase 17 — CHAR-03: Character Edit renders glass card form on mobile UA
    // -----------------------------------------------------------------------
    ```

    **Method 3** — `GetMobilePage_CharacterEdit_ReturnsSuccessAndMobileLayout`:
    - Seed: `CreateAuthenticatedClientWithUserAsync(_factory, "char_edi17", "char_edi17@test.com")` to get `(authClient, ownerUser)`.
    - Seed: `await TestDataHelper.CreateTestCharacterAsync(_factory.Services, ownerUser.Id, "Bram Ironfist")` to get `character`.
    - Authenticated GET `/GuildMembers/Edit/{character.Id}` with `MobileUserAgent`.
    - Assert: OK; `html.Should().Contain("character-form-card")`; `html.Should().Contain("character-form.mobile.css")`.

    Add separator comment:
    ```csharp
    // -----------------------------------------------------------------------
    // Phase 17 — PLAYER-01: Players Index renders section list on mobile UA
    // -----------------------------------------------------------------------
    ```

    **Method 4** — `GetMobilePage_PlayersIndex_ReturnsSuccessAndMobileLayout`:
    - Seed: `CreateAuthenticatedClientWithUserAsync(_factory, "player_idx17", "player_idx17@test.com")` to get `(authClient, _)`.
    - Authenticated GET `/Players` with `MobileUserAgent`.
    - Assert: OK; `html.Should().Contain("players-section-card")`; `html.Should().Contain("players.mobile.css")`.

    Unique userName/email per method prevents Identity uniqueness collisions (same convention as prior phases).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MobileViewsTests.cs contains the exact string `GetMobilePage_CharacterDetails_ReturnsSuccessAndMobileLayout`
    - MobileViewsTests.cs contains the exact string `GetMobilePage_CharacterCreate_ReturnsSuccessAndMobileLayout`
    - MobileViewsTests.cs contains the exact string `GetMobilePage_CharacterEdit_ReturnsSuccessAndMobileLayout`
    - MobileViewsTests.cs contains the exact string `GetMobilePage_PlayersIndex_ReturnsSuccessAndMobileLayout`
    - MobileViewsTests.cs contains the exact string `character-detail-card`
    - MobileViewsTests.cs contains the exact string `character-form-card`
    - MobileViewsTests.cs contains the exact string `players-section-card`
    - MobileViewsTests.cs contains the exact string `character-detail.mobile.css`
    - MobileViewsTests.cs contains the exact string `character-form.mobile.css`
    - MobileViewsTests.cs contains the exact string `players.mobile.css`
    - `dotnet build EuphoriaInn.IntegrationTests` exits 0 (all four new stubs compile)
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_Character" --list-tests` lists 3 methods
    - No existing test method was removed or renamed (all 50 prior tests still present)
  </acceptance_criteria>
  <done>Four test stubs compile and are discoverable. They are RED (mobile views do not exist yet; expander falls back to desktop views which lack the mobile CSS class names). All 50 prior tests remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→filesystem | Writing to .planning/REQUIREMENTS.md (documentation file, no runtime exposure) |
| planner→test project | Appending test code to MobileViewsTests.cs (test-only assembly, not shipped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | REQUIREMENTS.md | accept | Documentation file, no runtime execution path; changes are reviewed in git diff |
| T-17-02 | Tampering | MobileViewsTests.cs | accept | Test assembly only, not deployed; changes validated by build gate |
| T-17-03 | Information Disclosure | Test user seeds (`char_det17@test.com` etc.) | accept | Integration test database is ephemeral in-memory (SQLite); credentials never reach production |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.IntegrationTests --no-restore` exits 0
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_Character" --list-tests` lists 3 methods
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_Players" --list-tests` lists 1 method
- .planning/REQUIREMENTS.md contains CHAR-01, CHAR-02, CHAR-03, PLAYER-01 definitions
- No existing requirement was modified or removed
</verification>

<success_criteria>
REQUIREMENTS.md defines CHAR-01 through PLAYER-01 with wording aligned to ROADMAP.md Phase 17 success criteria. Four new [Fact] test stubs exist in MobileViewsTests.cs, all compile, all are RED (no mobile views yet), and each asserts a phase-17 CSS class name and CSS file link.
</success_criteria>

<output>
After completion, create `.planning/phases/17-character-player-views/17-01-SUMMARY.md`
</output>
