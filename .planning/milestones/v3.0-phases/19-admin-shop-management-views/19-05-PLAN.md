---
phase: 19-admin-shop-management-views
plan: 05
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/shop-management-index.mobile.css
autonomous: true
requirements: [ADMIN-02]
must_haves:
  truths:
    - "On a mobile user agent, /ShopManagement renders a single flat glass-card list of all items (ItemsForReview + MyItems + AllOtherItems combined, deduplicated, ordered Draft-first then by name)"
    - "Each item card shows rarity badge, name, price in gp, status badge, and icon-only action buttons (View, Edit, Archive/Reopen, Deny, Delete) with the same conditional/authorization logic as desktop"
    - "The Deny Item modal and Bulk Actions stub modal are present and functional"
    - "Add New Item (full-width primary) and View Shop links are at the top"
  artifacts:
    - path: "EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml"
      provides: "Mobile flat item list with modals and authorization-guarded action buttons"
      contains: "shop-mgmt-index-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/shop-management-index.mobile.css"
      provides: "Flat item list + icon-only button styles"
      contains: "shop-mgmt-index-card-mobile"
  key_links:
    - from: "EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml"
      to: "ShopManagementIndexViewModel collections"
      via: "Concat of ItemsForReview/MyItems/AllOtherItems with DistinctBy(Id)"
      pattern: "ItemsForReview|DistinctBy"
    - from: "EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml"
      to: "AuthorizationService AdminOnly policy"
      via: "conditional Publish/Delete buttons in Razor"
      pattern: "AuthorizeAsync\\(User, \"AdminOnly\"\\)"
---

<objective>
Create the mobile flat-list variant of ShopManagement/Index plus its per-view CSS. This is the most complex view in the phase: it merges three desktop table sections (ItemsForReview, MyItems, AllOtherItems) into one deduplicated flat list ordered Draft-first then alphabetically, preserves status-based and authorization-based conditional action buttons (icon-only on mobile), and carries the Deny Item modal and the Bulk Actions stub modal copied verbatim from desktop.

Purpose: ADMIN-02 — the Shop Management index must be fully functional on mobile. Per D-07 through D-10.
Output: One .Mobile.cshtml view + one .mobile.css file; GetMobilePage_ShopManagementIndex integration test turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-admin-shop-management-views/19-CONTEXT.md
@.planning/phases/19-admin-shop-management-views/19-PATTERNS.md
@.planning/phases/19-admin-shop-management-views/19-UI-SPEC.md
@EuphoriaInn.Service/Views/ShopManagement/Index.cshtml
@EuphoriaInn.Service/Views/Shop/Index.Mobile.cshtml
@EuphoriaInn.Service/wwwroot/css/shop.mobile.css
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05 portion)

New view file:
- `EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml`

New CSS file:
- `EuphoriaInn.Service/wwwroot/css/shop-management-index.mobile.css`

New CSS classes introduced: `shop-mgmt-index-card-mobile`, `shop-mgmt-item-card`. JS reused (copied verbatim from desktop): `bulkAction`, the `denyModal` `show.bs.modal` listener. Reused (do NOT redefine) classes from shop.mobile.css: `item-rarity`, `rarity-common`, `rarity-uncommon`, `rarity-rare`, `rarity-veryrare`, `rarity-legendary`. Enum values referenced: `ItemStatus.Draft`, `ItemStatus.Published`, `ItemStatus.Archived`, `ItemStatus.Denied` (the full set — there is no Pending/Reopen enum value; Reopen is an action on Archived items).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create shop-management-index.mobile.css + Index.Mobile.cshtml (flat list, status/auth conditionals, modals)</name>
  <files>EuphoriaInn.Service/wwwroot/css/shop-management-index.mobile.css, EuphoriaInn.Service/Views/ShopManagement/Index.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/ShopManagement/Index.cshtml (desktop source — read in full. Copy: `@model ShopManagementIndexViewModel`, the `@using` directives, the rarity-badge display, the status-badge switch on item.Status, every action-button conditional including the `(await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded` guards for Publish/Delete, the Deny Item modal, the Bulk Actions modal, and the @section Scripts with the bulkAction() function and the denyModal show.bs.modal listener)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 357-503 (the flat-list construction snippet, the rarity badge, the icon-only action button block with all status/auth conditionals, the modal copy notes, the @section Scripts snippet, and the empty-state)
    - .planning/phases/19-admin-shop-management-views/19-RESEARCH.md "Common Pitfalls" Pitfall 2 (DistinctBy required) and "Don't Hand-Roll" (rarity classes, auth check, modal trigger)
    - EuphoriaInn.Service/Views/Shop/Index.Mobile.cshtml (analog for the mobile shop list card layout and empty-state)
    - EuphoriaInn.Service/wwwroot/css/shop.mobile.css (rarity classes + .item-rarity already defined here — reuse class names, do NOT redefine)
  </read_first>
  <action>
    Create shop-management-index.mobile.css first: header comment `/* shop-management-index.mobile.css - ShopManagement/Index.Mobile.cshtml only */`, no-media-query note comment, `.shop-mgmt-index-card-mobile` outer glass card (16px padding), `.shop-mgmt-item-card` nested per-item sub-card (same glass values, 12px padding, bottom margin), parchment text for `h5`/`.parchment-text`/item name, faded text for `small`/price, `.badge { text-shadow: none !important; }`, and an icon-only-button sizing rule for the action row: scope a `padding: 4px 8px` override to `.shop-mgmt-item-card .btn-sm` so 4-5 icon buttons fit on a 320px screen without wrapping (per UI-SPEC Spacing exception). Do NOT redefine `.item-rarity` or the `.rarity-*` classes (they live in shop.mobile.css). Do NOT add media queries.

    Create Index.Mobile.cshtml declaring `@model ShopManagementIndexViewModel` with the desktop `@using` directives (must include `using EuphoriaInn.Domain.Enums` for ItemStatus and `using System.Linq` for the LINQ chain). Do NOT set `Layout`; do NOT add `@inject IAuthorizationService` (already globally available).

    Add `@section Styles` linking `~/css/shop-management-index.mobile.css` with `asp-append-version="true"`.

    In the top `@{ }` block, build the flat list: concat `Model.ItemsForReview`, `Model.MyItems`, `Model.AllOtherItems`; `.OrderBy(i => i.Status == ItemStatus.Draft ? 0 : 1)` (Draft = "Pending Review" first); `.ThenBy(i => i.Name)`; `.DistinctBy(i => i.Id)` (required to avoid duplicate cards when a DM's own draft also appears in ItemsForReview — Pitfall 2); `.ToList()`.

    At the top of the page render the action links: an "Add New Item" full-width primary anchor (`btn btn-primary w-100 mb-2`, links `/ShopManagement/Create`, fa-plus me-2) and a "View Shop" full-width secondary/outline anchor (`btn btn-outline-secondary w-100 mb-3`, links `/Shop`, fa-store me-2).

    Wrap the list in `<div class="shop-mgmt-index-card-mobile mb-3">`. Per D-08, each item card shows name + rarity badge + price + status badge + icon-only action buttons with the desktop conditional/authorization logic preserved. `@foreach (var item in allItems)` render a `shop-mgmt-item-card mb-3` sub-card containing: the rarity badge `<span class="item-rarity rarity-@item.Rarity.ToString().ToLower()">@item.Rarity.ToString().Replace("VeryRare", "Very Rare")</span>`; the item name in bold parchment; the price `@item.Price gp` (subdued); the status badge (copy the desktop `@switch (item.Status)` block verbatim so badge colors match: Draft=bg-warning text-dark "Pending Review", Published=bg-success "Active", Archived=bg-secondary, Denied=bg-danger); then an icon-only action button row `<div class="d-flex gap-1 mt-2 flex-wrap">` copying ALL the desktop action conditionals: View (`fa-eye`, anchor to `/Shop/Details/@item.Id`), Edit (`fa-edit`, anchor to `/ShopManagement/Edit/@item.Id`), Archive when Published / Reopen when Archived (POST forms with `@Html.AntiForgeryToken()` and confirm), Deny when Draft (button with `data-bs-toggle="modal" data-bs-target="#denyModal" data-item-id data-item-name`), Publish when Draft AND AdminOnly (POST form guarded by `(await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded`), and Delete when Denied OR (non-Draft non-Denied AND AdminOnly) (POST form). Every icon button MUST have BOTH `title="..."` and the exact `aria-label` values from UI-SPEC ("View item", "Edit item", "Archive item"/"Reopen item", "Deny item", "Delete item").

    Add the empty-state per UI-SPEC: glass-card centered, `fa-box-open fa-3x`, heading "No items yet", body "Add the first item to your shop." — rendered when `allItems` is empty.

    Copy the Deny Item modal and the Bulk Actions stub modal verbatim from the desktop view (Bootstrap 5 modals work on mobile, D-09). Place them after the list.

    Add `@section Scripts` copying the desktop bulkAction() function and the `denyModal` `show.bs.modal` event listener verbatim (sets the form action to `/ShopManagement/Deny/{itemId}`, fills the item name, clears the reason).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - Index.Mobile.cshtml contains `shop-mgmt-index-card-mobile` and `shop-mgmt-item-card` and links `~/css/shop-management-index.mobile.css`
    - Index.Mobile.cshtml contains `.DistinctBy(i => i.Id)` and concatenates `ItemsForReview`, `MyItems`, and `AllOtherItems`
    - Index.Mobile.cshtml contains `AuthorizeAsync(User, "AdminOnly")` for the conditional Publish/Delete buttons
    - Index.Mobile.cshtml contains a `#denyModal` element and `data-bs-target="#denyModal"`
    - Index.Mobile.cshtml contains the exact aria-label strings `aria-label="View item"`, `aria-label="Edit item"`, `aria-label="Deny item"`, `aria-label="Delete item"`
    - Index.Mobile.cshtml references `item-rarity` and `rarity-` classes but the CSS file does NOT redefine `.item-rarity` (`grep -c "\.item-rarity" shop-management-index.mobile.css` returns 0)
    - Index.Mobile.cshtml does NOT contain `Layout =` and does NOT contain `@inject IAuthorizationService`
    - `grep -v '^/\*' shop-management-index.mobile.css | grep -c "@media"` returns 0
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_ShopManagementIndex" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    ShopManagement/Index.Mobile.cshtml renders a single deduplicated flat item list ordered Draft-first then by name, with status badges, authorization-guarded icon-only action buttons, both modals, and the Add/View links; the ADMIN-02 Index integration test is GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → ShopManagementController | DM/Admin action POSTs (Archive, Reopen, Deny, Publish, Delete) cross to the server; access enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | item action POST forms | mitigate | Each action form carries `@Html.AntiForgeryToken()` copied from desktop; controller actions retain `[ValidateAntiForgeryToken]` |
| T-19-12 | Elevation of Privilege | Publish/Delete buttons visible to non-admins | mitigate | Buttons gated in Razor by `(await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded`; server endpoints also enforce policy independently — view guard is defense-in-depth, not the sole control |
| T-19-13 | Tampering (XSS) | item name/description in cards and modal | mitigate | Razor auto-encodes `@item.Name`; `data-item-name` attribute set from `@item.Name` is attribute-encoded by Razor; no Html.Raw |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md confirms zero new packages) |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.Service --no-restore -q` succeeds
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_ShopManagementIndex" -q` GREEN
- Flat-list dedup and ordering verified by source assertions (DistinctBy + OrderBy present)
</verification>

<success_criteria>
The ShopManagement index renders as one flat, deduplicated, Draft-first item list on mobile with correct status badges, authorization-guarded icon-only actions, working Deny/Bulk modals, and top action links; ADMIN-02 Index integration test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/19-admin-shop-management-views/19-05-SUMMARY.md` when done
</output>
