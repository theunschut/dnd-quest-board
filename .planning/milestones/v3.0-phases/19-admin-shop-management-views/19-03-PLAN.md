---
phase: 19-admin-shop-management-views
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - EuphoriaInn.Service/Views/Admin/EditUser.Mobile.cshtml
  - EuphoriaInn.Service/Views/Admin/ResetPassword.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "On a mobile user agent, /Admin/EditUser renders a single-column glass-card form with Name, Email, HasKey checkbox, Reset Password link, and Save Changes button"
    - "On a mobile user agent, /Admin/ResetPassword renders a single-column glass-card form with the warning alert kept, New Password, Confirm Password, and a submit button"
    - "Both views share admin-form.mobile.css"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Admin/EditUser.Mobile.cshtml"
      provides: "Mobile single-column EditUser form"
      contains: "admin-form-card-mobile"
    - path: "EuphoriaInn.Service/Views/Admin/ResetPassword.Mobile.cshtml"
      provides: "Mobile single-column ResetPassword form with warning alert"
      contains: "admin-form-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css"
      provides: "Shared single-column form glass card styles"
      contains: "admin-form-card-mobile"
  key_links:
    - from: "EuphoriaInn.Service/Views/Admin/EditUser.Mobile.cshtml"
      to: "EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css"
      via: "@section Styles link"
      pattern: "admin-form\\.mobile\\.css"
    - from: "EuphoriaInn.Service/Views/Admin/ResetPassword.Mobile.cshtml"
      to: "EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css"
      via: "@section Styles link (shared file)"
      pattern: "admin-form\\.mobile\\.css"
---

<objective>
Create the mobile single-column form variants for Admin/EditUser and Admin/ResetPassword, sharing one CSS file (admin-form.mobile.css). Both are simple forms already; the mobile conversion wraps them in a glass card and makes all inputs full-width.

Purpose: ADMIN-01 — the admin user edit and password-reset pages must be usable on mobile. Per D-05 and D-06.
Output: Two .Mobile.cshtml form views + one shared .mobile.css; the GetMobilePage_AdminEditUser and GetMobilePage_AdminResetPassword integration tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-admin-shop-management-views/19-CONTEXT.md
@.planning/phases/19-admin-shop-management-views/19-PATTERNS.md
@.planning/phases/19-admin-shop-management-views/19-UI-SPEC.md
@EuphoriaInn.Service/Views/Admin/EditUser.cshtml
@EuphoriaInn.Service/Views/Admin/ResetPassword.cshtml
@EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml
@EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion)

New view files:
- `EuphoriaInn.Service/Views/Admin/EditUser.Mobile.cshtml`
- `EuphoriaInn.Service/Views/Admin/ResetPassword.Mobile.cshtml`

New CSS file:
- `EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css`

New CSS class introduced: `admin-form-card-mobile` (shared by both views).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create admin-form.mobile.css + Admin/EditUser.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css, EuphoriaInn.Service/Views/Admin/EditUser.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Admin/EditUser.cshtml (desktop source — copy `@model EditUserViewModel`, the `@using` directive, the hidden `Id` field, the Name/Email inputs, the HasKey checkbox block with its fa-key label, the Reset Password link `asp-action="ResetPassword" asp-route-userId="@Model.Id"`, and the Save/Back button row)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 222-297 (exact EditUser excerpts)
    - EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml (analog: @section Styles placement, glass card wrapper, no Layout, no @inject)
    - EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css (canonical CSS template)
  </read_first>
  <action>
    Create admin-form.mobile.css first. Header comment line 1: `/* admin-form.mobile.css - Admin/EditUser.Mobile.cshtml + Admin/ResetPassword.Mobile.cshtml (shared) */`. Comment line 2 noting no media queries and exclusive load via _Layout.Mobile.cshtml. Define `.admin-form-card-mobile` with the standard glass card values (background rgba(255,255,255,0.15), backdrop-filter blur(15px), border 1px solid rgba(255,255,255,0.3), border-radius 12px, box-shadow 0 8px 32px rgba(0,0,0,0.2), padding 16px). Add parchment-text rule for `.form-label`, `.form-check-label`, and `h5` (color #F4E4BC !important + two-layer text-shadow). Add faded rule for `.form-text` and `small`. Add `.badge { text-shadow: none !important; }`. Do NOT add media queries; do NOT redefine input/`.btn` min-height (already in mobile.css).

    Create EditUser.Mobile.cshtml declaring `@model EditUserViewModel` with the desktop `@using` directive and `@{ ViewData["Title"] = "Edit User"; }`. Do NOT set `Layout`; do NOT add `@inject`. Add `@section Styles` linking `~/css/admin-form.mobile.css` with `asp-append-version="true"`.

    Wrap content in `<div class="admin-form-card-mobile mb-3">` with a heading `<h5><i class="fas fa-user-edit text-warning me-2"></i>Edit User</h5>`. Inside, a `<form asp-action="EditUser" method="post">` containing: hidden `<input asp-for="Id" type="hidden" />`; a Name group (label + `<input asp-for="Name" class="form-control" />` + validation span); an Email group (label + `<input asp-for="Email" class="form-control" type="email" />` + validation span); the HasKey checkbox group copied verbatim (form-check, `<input asp-for="HasKey" class="form-check-input" type="checkbox" />`, fa-key label text). All input wrappers are full-width single-column (the mobile layout is single column already; no col-md-* grid). Below the fields add the Reset Password link `<a asp-action="ResetPassword" asp-route-userId="@Model.Id" class="btn btn-outline-danger"><i class="fas fa-key me-2"></i>Reset Password</a>`. Add `<hr>` then the button row `<div class="d-flex justify-content-between">` with a Back-to-Users secondary anchor on the left (`btn btn-secondary`, fa-arrow-left) and a Save Changes submit on the right (`btn btn-success`, fa-save). Use the copy "Save Changes" per UI-SPEC.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - admin-form.mobile.css contains `.admin-form-card-mobile` with `backdrop-filter` and `border-radius: 12px`
    - `grep -v '^/\*' admin-form.mobile.css | grep -c "@media"` returns 0
    - EditUser.Mobile.cshtml contains `admin-form-card-mobile` and links `~/css/admin-form.mobile.css` with `asp-append-version="true"`
    - EditUser.Mobile.cshtml contains `asp-for="HasKey"`, an `asp-action="ResetPassword"` link, and a `Save Changes` submit
    - EditUser.Mobile.cshtml does NOT contain `Layout =` and does NOT contain `@inject`
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminEditUser" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    admin-form.mobile.css and EditUser.Mobile.cshtml exist; the EditUser form renders single-column in a glass card with all fields, the Reset Password link, and Save Changes; the ADMIN-01 EditUser integration test is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Admin/ResetPassword.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/Admin/ResetPassword.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Admin/ResetPassword.cshtml (desktop source — copy `@model ResetPasswordViewModel`, the `@using` directive, the hidden UserId/UserName fields, the NewPassword and ConfirmPassword input groups with validation spans, the alert-warning block verbatim, and the Cancel/Reset button row)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 301-353 (exact ResetPassword excerpts)
    - EuphoriaInn.Service/wwwroot/css/admin-form.mobile.css (created in Task 1 — this view reuses it)
  </read_first>
  <action>
    Create ResetPassword.Mobile.cshtml declaring `@model ResetPasswordViewModel` with the desktop `@using` directive. Do NOT set `Layout`; do NOT add `@inject`. Add `@section Styles` linking the SHARED `~/css/admin-form.mobile.css` with `asp-append-version="true"` (same file as EditUser per D-21 / Claude's discretion to share).

    Wrap content in `<div class="admin-form-card-mobile mb-3">` with a heading `<h5><i class="fas fa-key text-danger me-2"></i>Reset Password</h5>`. Inside a `<form asp-action="ResetPassword" method="post">`: hidden `<input asp-for="UserId" type="hidden" />` and `<input asp-for="UserName" type="hidden" />`; keep the `alert-warning` block verbatim from desktop (D-06 — functional context warning the admin about the impact, including the `@Model.UserName` interpolation); a NewPassword group (label + `<input asp-for="NewPassword" class="form-control" />` + validation span); a ConfirmPassword group (label + `<input asp-for="ConfirmPassword" class="form-control" />` + validation span). Add `<hr>` then a button row `<div class="d-flex justify-content-between">` with a Cancel secondary anchor on the left (`btn btn-secondary`, `asp-action="Users"`, fa-arrow-left) and a Reset Password submit on the right (`btn btn-danger`, fa-save). Use the copy "Reset Password" for the submit per UI-SPEC.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - ResetPassword.Mobile.cshtml contains `admin-form-card-mobile` and links the SHARED `~/css/admin-form.mobile.css`
    - ResetPassword.Mobile.cshtml contains an `alert-warning` block (kept per D-06) and references `@Model.UserName`
    - ResetPassword.Mobile.cshtml contains `asp-for="NewPassword"` and `asp-for="ConfirmPassword"`
    - ResetPassword.Mobile.cshtml does NOT contain `Layout =` and does NOT contain `@inject`
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminResetPassword" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    ResetPassword.Mobile.cshtml renders a single-column glass-card form with the warning alert kept and both password fields; the ADMIN-01 ResetPassword integration test is GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → AdminController | Admin form POSTs (EditUser, ResetPassword) cross to the server; authorization enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | EditUser/ResetPassword POST forms | mitigate | Forms use tag-helper `<form asp-action>` which emits the antiforgery hidden field; controller actions retain `[ValidateAntiForgeryToken]`; no change to validation |
| T-19-06 | Elevation of Privilege | password reset via mobile | mitigate | No new controller actions; `[Authorize(Policy = "AdminOnly")]` still enforces access; mobile view is additive only |
| T-19-07 | Information Disclosure | UserName shown in warning alert | accept | UserName already rendered in desktop alert; no new exposure; admin-only context |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md confirms zero new packages) |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.Service --no-restore -q` succeeds
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminEditUser|FullyQualifiedName~GetMobilePage_AdminResetPassword" -q` GREEN
</verification>

<success_criteria>
Both admin form views render single-column in glass cards on mobile, the ResetPassword warning alert is preserved, and the ADMIN-01 EditUser and ResetPassword integration tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/19-admin-shop-management-views/19-03-SUMMARY.md` when done
</output>
