---
phase: 18-dm-editing-secondary-quest-views
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css
autonomous: true
requirements:
  - QLOG-01

must_haves:
  truths:
    - "Player visiting /QuestLog/Details/{id} on mobile sees quest info, adventurers, and recap in a single main glass card"
    - "Quick Actions glass card appears below the main card (per D-16)"
    - "Quest Statistics glass card appears below Quick Actions (per D-16, D-17)"
    - "ViewBag.CanEditRecap conditional preserved — DM sees recap form, others see read-only or empty state (per D-18)"
    - "Building Access badge uses bg-success or bg-danger based on key availability (per D-17)"
    - "quest-log-detail.mobile.css is linked"
  artifacts:
    - path: "EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml"
      provides: "Mobile QuestLog Details view"
      contains: "quest-log-detail-main-card"
    - path: "EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css"
      provides: "Glass card CSS for QuestLog Details"
      contains: ".quest-log-detail-main-card"
  key_links:
    - from: "Details.Mobile.cshtml"
      to: "quest-log-detail.mobile.css"
      via: "@section Styles link"
      pattern: "quest-log-detail\\.mobile\\.css"
    - from: "Details.Mobile.cshtml"
      to: "ViewBag.CanEditRecap"
      via: "conditional recap form vs read-only display"
      pattern: "CanEditRecap"
---

<objective>
Create QuestLog/Details.Mobile.cshtml and quest-log-detail.mobile.css — the mobile variant of the
Quest Log details page. Three stacked glass cards: main content → Quick Actions → Quest Statistics
(per D-16, D-17). ViewBag.CanEditRecap conditional preserved (per D-18). Building Access badge kept (per D-17).

Purpose: Players and DMs reviewing a completed quest on mobile see quest info, adventurers, and recap in a scrollable single-column layout.
Output: Details.Mobile.cshtml + quest-log-detail.mobile.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-CONTEXT.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quest-log-detail.mobile.css</name>
  <files>EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/wwwroot/css/quest-log.mobile.css (glass card pattern from Phase 13 — closest analog per UI-SPEC)
    - EuphoriaInn.Service/wwwroot/css/dm-create.mobile.css (glass card values reference)
  </read_first>
  <action>
Create `EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css` with the following exact content.
No @media queries.

```css
/* quest-log-detail.mobile.css — QuestLog Details mobile view only */
/* No @media queries — exclusively loaded by QuestLog/Details.Mobile.cshtml via _Layout.Mobile.cshtml */

/* Main content glass card */
.quest-log-detail-main-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Quick Actions glass card */
.quest-log-detail-actions-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Quest Statistics glass card */
.quest-log-detail-stats-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Headings inside all three cards — parchment */
.quest-log-detail-main-card h5,
.quest-log-detail-main-card h6,
.quest-log-detail-actions-card h6,
.quest-log-detail-stats-card h6 {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Body text inside cards — parchment */
.quest-log-detail-main-card p,
.quest-log-detail-main-card li,
.quest-log-detail-stats-card p {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Faded parchment for meta text and empty states */
.quest-log-detail-main-card .text-muted,
.quest-log-detail-main-card small {
    color: rgba(244, 228, 188, 0.7) !important;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;
}

/* Recap display box — white background for readability */
.recap-display-box {
    background-color: rgba(255, 255, 255, 0.9);
    color: #1a1a1a;
    border-radius: 8px;
    padding: 12px;
}

/* Recap textarea — high-contrast for editability */
.quest-log-detail-main-card textarea {
    background-color: rgba(255, 255, 255, 0.95) !important;
    color: #1a1a1a !important;
    border: 2px solid #8B4513;
    font-weight: 500;
}

/* Badges inside cards — no text-shadow */
.quest-log-detail-main-card .badge,
.quest-log-detail-actions-card .badge,
.quest-log-detail-stats-card .badge {
    text-shadow: none !important;
}

/* Character mini avatar (adventurers list) */
.character-mini-avatar {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    object-fit: cover;
    vertical-align: middle;
    margin-right: 4px;
}

.character-mini-avatar-placeholder {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.2);
    font-size: 12px;
    margin-right: 4px;
}
```
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - quest-log-detail.mobile.css exists at EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css
    - File contains `.quest-log-detail-main-card`
    - File contains `.quest-log-detail-actions-card`
    - File contains `.quest-log-detail-stats-card`
    - File contains `background: rgba(255, 255, 255, 0.15)`
    - File contains `backdrop-filter: blur(15px)`
    - File contains `border-radius: 12px`
    - File contains `padding: 16px`
    - File contains `color: #F4E4BC !important`
    - File contains `.recap-display-box`
    - File contains `.character-mini-avatar`
    - File contains NO @media query
  </acceptance_criteria>
  <done>quest-log-detail.mobile.css created with three glass card classes for main, actions, and stats panels</done>
</task>

<task type="auto">
  <name>Task 2: Create QuestLog/Details.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/QuestLog/Details.cshtml (desktop source — full model bindings, selectedPlayers logic, CanEditRecap conditional, Building Access badge, character-mini-avatar pattern)
    - EuphoriaInn.Service/Views/Quest/Create.Mobile.cshtml (mobile pattern — container-fluid, glass card, @section Styles)
    - EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css (just created — class names)
  </read_first>
  <action>
Create `EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml`. Follow these exact specifications:

**Model/using directives**:
```razor
@using EuphoriaInn.Service.ViewModels.QuestLogViewModels
@model QuestLogDetailsViewModel
@{
    ViewData["Title"] = $"Quest Log: {Model.Quest.Title}";
    var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext);
}
```

**@section Styles**:
```razor
@section Styles {
    <link href="~/css/quest-log-detail.mobile.css" asp-append-version="true" rel="stylesheet" />
}
```

**Outer container** — three stacked glass cards (per D-16 layout order):
```html
<div class="container-fluid px-2 mt-2">
    <!-- Card 1: Main content -->
    <div class="quest-log-detail-main-card mb-3">
        ...
    </div>
    <!-- Card 2: Quick Actions -->
    <div class="quest-log-detail-actions-card mb-3">
        ...
    </div>
    <!-- Card 3: Quest Statistics -->
    <div class="quest-log-detail-stats-card mb-3">
        ...
    </div>
</div>
```

**Main card heading** (h5 + CR badge):
```html
<div class="d-flex justify-content-between align-items-center mb-3">
    <h5 class="mb-0">@Model.Quest.Title</h5>
    <span class="badge bg-secondary">
        <i class="fas fa-dice-d20 me-1"></i>CR @Model.Quest.ChallengeRating
    </span>
</div>
```

**Quest Information section**:
```html
<div class="mb-3">
    <h6><i class="fas fa-info-circle me-2"></i>Quest Information</h6>
    <p class="mb-1"><strong>Dungeon Master:</strong> @Model.Quest.DungeonMaster?.Name</p>
    <p class="mb-1"><strong>Challenge Rating:</strong> @Model.Quest.ChallengeRating</p>
    <p class="mb-1"><strong>Completed On:</strong> @Model.Quest.FinalizedDate?.ToString("MMMM dd, yyyy 'at' h:mm tt")</p>
    <p class="mb-0"><strong>Created:</strong> @Model.Quest.CreatedAt.ToString("MMMM dd, yyyy")</p>
</div>
<hr />
```

**Original Quest Description section**:
```html
<div class="mb-3">
    <h6><i class="fas fa-scroll me-2"></i>Original Quest Description</h6>
    <div class="recap-display-box">
        @Model.Quest.Description
    </div>
</div>
<hr />
```

**Adventurers section** (copy selectedPlayers logic and character-mini-avatar loop from desktop):
```razor
<div class="mb-3">
    <h6><i class="fas fa-users me-2"></i>Adventurers</h6>
    @{
        var selectedPlayers = Model.Quest.PlayerSignups.Where(ps => ps.IsSelected).OrderBy(ps => ps.SignupTime).ToList();
    }
    @if (selectedPlayers.Any())
    {
        <ul class="list-unstyled mb-0">
            @foreach (var player in selectedPlayers)
            {
                <li class="mb-2">
                    <strong>@player.Player.Name</strong>
                    @if (player.Character != null)
                    {
                        <span>
                            (<img src="@Url.Action("GetProfilePicture", "GuildMembers", new { id = player.Character.Id })"
                                 alt="@player.Character.Name"
                                 class="character-mini-avatar"
                                 onerror="this.style.display='none'; this.nextElementSibling.style.display='inline-flex';" />
                            <span class="character-mini-avatar-placeholder" style="display: none;">
                                <i class="fas fa-user"></i>
                            </span>
                            @player.Character.Name)
                        </span>
                    }
                </li>
            }
        </ul>
    }
    else
    {
        <p class="mb-0 text-muted">No adventurers participated in this quest.</p>
    }
</div>
<hr />
```

**Session Recap section** (per D-18 — CanEditRecap conditional preserved):
```razor
<div class="mb-0">
    <h6><i class="fas fa-book me-2"></i>Session Recap</h6>
    @if ((bool)ViewBag.CanEditRecap)
    {
        <form asp-action="UpdateRecap" asp-route-id="@Model.Quest.Id" method="post">
            <div class="mb-3">
                <label for="recap" class="form-label">Write or update the session recap:</label>
                <textarea class="form-control"
                          id="recap"
                          name="recap"
                          rows="6"
                          placeholder="Describe what happened during this quest...">@Model.Quest.Recap</textarea>
                <small class="form-text">Share the story of this adventure with your players!</small>
            </div>
            <button type="submit" class="btn btn-warning w-100">
                <i class="fas fa-save me-2"></i>Save Recap
            </button>
        </form>
    }
    else
    {
        @if (!string.IsNullOrWhiteSpace(Model.Quest.Recap))
        {
            <div class="recap-display-box">
                @Model.Quest.Recap
            </div>
        }
        else
        {
            <p class="mb-0 text-muted">No recap has been written for this quest yet.</p>
        }
    }
</div>
```

**Quick Actions card** (per D-16, D-17):
```razor
<div class="quest-log-detail-actions-card mb-3">
    <h6 class="mb-3"><i class="fas fa-bolt text-warning me-2"></i>Quick Actions</h6>
    <a href="@Url.Action("Index", "QuestLog")" class="btn btn-secondary w-100 mb-2">
        <i class="fas fa-arrow-left me-2"></i>Back to Quest Log
    </a>
    @if ((bool)ViewBag.CanEditRecap)
    {
        <a href="@Url.Action("Manage", "Quest", new { id = Model.Quest.Id })" class="btn btn-info w-100">
            <i class="fas fa-cog me-2"></i>Manage Quest
        </a>
    }
</div>
```

**Quest Statistics card** (per D-16, D-17 — Building Access badge with dmHasKey/playersHaveKey logic):
```razor
<div class="quest-log-detail-stats-card mb-3">
    <h6 class="mb-3"><i class="fas fa-chart-bar text-warning me-2"></i>Quest Statistics</h6>
    <p class="mb-1"><strong>Total Signups:</strong> @Model.Quest.PlayerSignups.Count</p>
    <p class="mb-1"><strong>Participants:</strong> @selectedPlayers.Count / @Model.Quest.TotalPlayerCount</p>
    <p class="mb-1">
        <strong>Building Access:</strong>
        @{
            var dmHasKey = Model.Quest.DungeonMaster?.HasKey == true;
            var playersHaveKey = Model.Quest.PlayerSignups?.Any(ps => ps.Player.HasKey == true) == true;
            var anyoneHasKey = dmHasKey || playersHaveKey;
        }
        <span class="badge @(anyoneHasKey ? "bg-success" : "bg-danger")">
            @if (anyoneHasKey)
            {
                <i class="fas fa-key me-1"></i>@:Available
            }
            else
            {
                <i class="fas fa-lock me-1"></i>@:No Key
            }
        </span>
    </p>
    <p class="mb-0">
        <strong>Status:</strong>
        <span class="badge bg-dark">
            <i class="fas fa-flag-checkered me-1"></i>Completed
        </span>
    </p>
</div>
```

Do NOT add @inject — services available globally via _ViewImports.cshtml.
Do NOT set Layout.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - Details.Mobile.cshtml exists at EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml
    - File contains `@model QuestLogDetailsViewModel`
    - File contains `quest-log-detail-main-card`
    - File contains `quest-log-detail-actions-card`
    - File contains `quest-log-detail-stats-card`
    - File contains `quest-log-detail.mobile.css`
    - File contains `ViewBag.CanEditRecap`
    - File contains `UpdateRecap`
    - File contains `No recap has been written for this quest yet.`
    - File contains `character-mini-avatar`
    - File contains `dmHasKey`
    - File contains `playersHaveKey`
    - File contains `bg-success`
    - File contains `bg-danger`
    - File contains `Back to Quest Log`
    - File contains `Manage Quest`
    - dotnet build EuphoriaInn.Service exits 0
  </acceptance_criteria>
  <done>QuestLog/Details.Mobile.cshtml created; three stacked glass cards, CanEditRecap conditional, Building Access badge, adventurers list with mini-avatar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile browser → POST /QuestLog/UpdateRecap | Recap text from DM; existing controller authorization applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Elevation of Privilege | CanEditRecap conditional guards recap form | accept | ViewBag.CanEditRecap set by QuestLogController; mobile view preserves the same conditional as desktop — no new bypass surface |
| T-18-04-02 | Information Disclosure | Adventurers list shows player names | accept | QuestLog/Details is a public-read page; same data shown on desktop |
</threat_model>

<verification>
1. `dotnet build EuphoriaInn.Service --no-restore -q` exits 0
2. `EuphoriaInn.Service/Views/QuestLog/Details.Mobile.cshtml` exists
3. `EuphoriaInn.Service/wwwroot/css/quest-log-detail.mobile.css` exists
4. Grep `quest-log-detail-main-card` in Details.Mobile.cshtml — must match
5. Grep `quest-log-detail-actions-card` in Details.Mobile.cshtml — must match
6. Grep `quest-log-detail-stats-card` in Details.Mobile.cshtml — must match
7. Grep `ViewBag.CanEditRecap` in Details.Mobile.cshtml — must match twice (recap section + Manage Quest button)
8. Grep `dmHasKey` in Details.Mobile.cshtml — must match
9. In Details.Mobile.cshtml: quest-log-detail-actions-card appears AFTER quest-log-detail-main-card (layout order per D-16)
</verification>

<success_criteria>
- Details.Mobile.cshtml compiles cleanly
- Three glass cards in correct order (main → actions → stats)
- CanEditRecap conditional present and correct for both DM (form) and player (read-only) paths
- Building Access badge with bg-success/bg-danger logic
- Adventurers list with character-mini-avatar
</success_criteria>

<output>
After completion, create `.planning/phases/18-dm-editing-secondary-quest-views/18-04-SUMMARY.md`
</output>
