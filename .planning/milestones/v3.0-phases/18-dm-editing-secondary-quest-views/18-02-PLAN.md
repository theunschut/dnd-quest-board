---
phase: 18-dm-editing-secondary-quest-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css
autonomous: true
requirements:
  - DMVIEW-05

must_haves:
  truths:
    - "DM visiting /Quest/CreateFollowUp/{id} on mobile sees a single-column glass card form"
    - "Info alert 'This form is pre-filled...' renders per D-09"
    - "Proposed dates use datetime-local inputs with Add Date / Remove buttons"
    - "addDate, removeDate, renumberDates JS is embedded in @section Scripts"
    - "Pre-Approved Players glass card renders below the form per D-07"
    - "quest-followup.mobile.css is linked"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml"
      provides: "Mobile CreateFollowUp form view"
      contains: "quest-followup-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css"
      provides: "Glass card CSS for CreateFollowUp form"
      contains: ".quest-followup-card-mobile"
  key_links:
    - from: "CreateFollowUp.Mobile.cshtml"
      to: "quest-followup.mobile.css"
      via: "@section Styles link"
      pattern: "quest-followup\\.mobile\\.css"
    - from: "CreateFollowUp.Mobile.cshtml"
      to: "ViewBag.PreApprovedPlayers"
      via: "@foreach loop below </form>"
      pattern: "PreApprovedPlayers"
---

<objective>
Create Quest/CreateFollowUp.Mobile.cshtml and quest-followup.mobile.css — the mobile variant of
the Create Follow-Up Quest form. Two glass cards: main form card + Pre-Approved Players card below
(per D-07). datetime-local date inputs with full inline JS (per D-08). Info alert kept (per D-09).

Purpose: DMs creating follow-up quests on mobile see a scrollable single-column form with pre-approved player confirmation below.
Output: CreateFollowUp.Mobile.cshtml + quest-followup.mobile.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-CONTEXT.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quest-followup.mobile.css</name>
  <files>EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/wwwroot/css/dm-create.mobile.css (base glass card pattern to replicate)
  </read_first>
  <action>
Create `EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css` with the following exact content.
No @media queries.

```css
/* quest-followup.mobile.css — Quest CreateFollowUp mobile view only */
/* No @media queries — exclusively loaded by Quest/CreateFollowUp.Mobile.cshtml via _Layout.Mobile.cshtml */

/* Main form glass card */
.quest-followup-card-mobile {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Pre-Approved Players card */
.quest-followup-players-card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Headings — parchment */
.quest-followup-card-mobile h5,
.quest-followup-players-card h6 {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Form labels — parchment */
.quest-followup-card-mobile .form-label {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Help text — faded parchment */
.quest-followup-card-mobile .form-text,
.quest-followup-card-mobile small,
.quest-followup-players-card .form-text,
.quest-followup-players-card small {
    color: rgba(244, 228, 188, 0.7) !important;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;
}

/* Player list items — parchment */
.quest-followup-players-card li {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Empty-state text — faded parchment */
.quest-followup-players-card p {
    color: rgba(244, 228, 188, 0.7) !important;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;
}

/* Badges inside cards — no text-shadow */
.quest-followup-card-mobile .badge,
.quest-followup-players-card .badge {
    text-shadow: none !important;
}
```
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - quest-followup.mobile.css exists at EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css
    - File contains `.quest-followup-card-mobile`
    - File contains `.quest-followup-players-card`
    - File contains `background: rgba(255, 255, 255, 0.15)`
    - File contains `backdrop-filter: blur(15px)`
    - File contains `border-radius: 12px`
    - File contains `padding: 16px`
    - File contains `color: #F4E4BC !important`
    - File contains NO @media query
  </acceptance_criteria>
  <done>quest-followup.mobile.css created with two glass card classes (main form + players panel)</done>
</task>

<task type="auto">
  <name>Task 2: Create Quest/CreateFollowUp.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Quest/CreateFollowUp.cshtml (desktop source — full inline JS, model bindings, PreApprovedPlayers loop)
    - EuphoriaInn.Service/Views/Quest/Create.Mobile.cshtml (mobile form pattern — container-fluid, glass card, @section Styles/Scripts)
    - EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css (just created — class names)
  </read_first>
  <action>
Create `EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml`. Follow these exact specifications:

**Model/using directives**:
```razor
@using EuphoriaInn.Domain.Interfaces
@using EuphoriaInn.Service.ViewModels.QuestViewModels
@model FollowUpQuestViewModel
@{
    ViewData["Title"] = "Create Follow-Up Quest";
}
```

**@section Styles**:
```razor
@section Styles {
    <link href="~/css/quest-followup.mobile.css" asp-append-version="true" rel="stylesheet" />
}
```

**Outer container** — two cards stacked:
```html
<div class="container-fluid px-2 mt-2">
    <!-- Main form card -->
    <div class="quest-followup-card-mobile mb-3">
        ...heading, alert-info, validation summary, form...
    </div>
    <!-- Pre-Approved Players card (D-07) — OUTSIDE the form -->
    <div class="quest-followup-players-card mb-3">
        ...
    </div>
</div>
```

**Card heading** (h5):
```html
<div class="mb-3">
    <h5 class="mb-0">
        <i class="fas fa-scroll text-warning me-2"></i>Create Follow-Up Quest
    </h5>
</div>
```

**Info alert** (per D-09 — keep verbatim from desktop):
```html
<div class="alert alert-info mb-3">
    <i class="fas fa-info-circle me-2"></i>
    This form is pre-filled from the original quest. Update the title, add at least one date, and save to create the follow-up.
</div>
```

**Validation summary**:
```html
<div asp-validation-summary="All" class="text-danger mb-3"></div>
```

**Form** — `asp-action="CreateFollowUp" asp-route-id="@Model.OriginalQuestId" method="post"`:
```html
<form asp-action="CreateFollowUp" asp-route-id="@Model.OriginalQuestId" method="post">
    <input type="hidden" asp-for="OriginalQuestId" />
    <input type="hidden" asp-for="DungeonMasterId" />
    ... fields ...
</form>
```

**Form fields** (copy from CreateFollowUp.cshtml asp-for bindings):
- Title: `asp-for="Title"` form-control
- Description: `asp-for="Description"` textarea rows="4"
- ChallengeRating: `asp-for="ChallengeRating"` type="number" min="1" max="20" step="1"
- TotalPlayerCount: `asp-for="TotalPlayerCount"` inputmode="numeric"

**Proposed Dates section** (per D-08 — datetime-local inputs, btn-danger Remove):
```razor
<div class="mb-3">
    <label class="form-label">Proposed Dates &amp; Times <span class="text-danger">*</span></label>
    <div id="proposed-dates">
        @for (int i = 0; i < Model.ProposedDates.Count; i++)
        {
            <div class="proposed-date-entry mb-2">
                <div class="input-group">
                    <input type="datetime-local" name="ProposedDates[@i]"
                           value="@Model.ProposedDates[i].ToString("yyyy-MM-ddTHH:mm")"
                           class="form-control" />
                    <button type="button" class="btn btn-danger" onclick="removeDate(this)">
                        <i class="fas fa-trash me-1"></i>Remove
                    </button>
                </div>
            </div>
        }
    </div>
    <button type="button" class="btn btn-warning btn-sm mt-2" onclick="addDate()">
        <i class="fas fa-plus me-1"></i>Add Date
    </button>
    <span asp-validation-for="ProposedDates" class="text-danger d-block mt-1"></span>
</div>
```

**Action buttons** — d-flex gap-2 (per UI-SPEC copywriting: "Back to Quest" + "Save Follow-Up Quest"):
```html
<div class="d-flex gap-2 mt-3">
    <a href="@Url.Action("Manage", "Quest", new { id = Model.OriginalQuestId })" class="btn btn-secondary flex-fill">
        <i class="fas fa-arrow-left me-2"></i>Back to Quest
    </a>
    <button type="submit" class="btn btn-warning flex-fill">
        <i class="fas fa-scroll me-2"></i>Save Follow-Up Quest
    </button>
</div>
```

**Pre-Approved Players card** (per D-07 — below the form, outside `</form>`):
```razor
<div class="quest-followup-players-card mb-3">
    <div class="mb-2">
        <h6 class="mb-0">
            <i class="fas fa-users text-warning me-2"></i>Pre-Approved Players
        </h6>
    </div>
    @if (ViewBag.PreApprovedPlayers != null && ((IEnumerable<dynamic>)ViewBag.PreApprovedPlayers).Any())
    {
        <ul class="list-unstyled mb-0">
            @foreach (var player in ViewBag.PreApprovedPlayers)
            {
                <li class="mb-1"><i class="fas fa-check-circle text-success me-2"></i>@player.Name</li>
            }
        </ul>
    }
    else
    {
        <p class="mb-0 small">No players were selected on the original quest. You can add players manually after saving.</p>
    }
</div>
```

**@section Scripts** — copy the full inline JS verbatim from CreateFollowUp.cshtml @section Scripts (per D-08):
The script block includes: `let dateCount`, `addDate()`, `removeDate()`, `renumberDates()`, and the `DOMContentLoaded` listener. Copy EXACTLY from the desktop source.

Do NOT add @inject.
Do NOT set Layout.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - CreateFollowUp.Mobile.cshtml exists at EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml
    - File contains `@model FollowUpQuestViewModel`
    - File contains `quest-followup-card-mobile`
    - File contains `quest-followup-players-card`
    - File contains `quest-followup.mobile.css`
    - File contains `asp-for="OriginalQuestId"`
    - File contains `asp-for="DungeonMasterId"`
    - File contains `asp-for="Title"`
    - File contains `asp-for="Description"`
    - File contains `alert-info`
    - File contains `This form is pre-filled from the original quest`
    - File contains `type="datetime-local"`
    - File contains `removeDate(this)`
    - File contains `addDate()`
    - File contains `renumberDates()`
    - File contains `PreApprovedPlayers`
    - File contains `No players were selected on the original quest`
    - dotnet build EuphoriaInn.Service exits 0
  </acceptance_criteria>
  <done>Quest/CreateFollowUp.Mobile.cshtml created with glass card form, info alert, datetime-local dates, inline JS, and Pre-Approved Players panel below form</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile browser → POST /Quest/CreateFollowUp | Untrusted follow-up form data; existing controller authorization applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | Hidden OriginalQuestId, DungeonMasterId fields | accept | QuestController.CreateFollowUp validates DM owns original quest; mobile view is additive |
| T-18-02-02 | Information Disclosure | ViewBag.PreApprovedPlayers displays player names | accept | Only visible to authenticated DM who owns the quest; controller enforces this |
</threat_model>

<verification>
1. `dotnet build EuphoriaInn.Service --no-restore -q` exits 0
2. `EuphoriaInn.Service/Views/Quest/CreateFollowUp.Mobile.cshtml` exists
3. `EuphoriaInn.Service/wwwroot/css/quest-followup.mobile.css` exists
4. Grep `quest-followup-card-mobile` in CreateFollowUp.Mobile.cshtml — must match
5. Grep `quest-followup-players-card` in CreateFollowUp.Mobile.cshtml — must match
6. Grep `quest-followup.mobile.css` in CreateFollowUp.Mobile.cshtml — must match
7. Grep `renumberDates` in CreateFollowUp.Mobile.cshtml — must match
8. Grep `PreApprovedPlayers` in CreateFollowUp.Mobile.cshtml — must match
</verification>

<success_criteria>
- CreateFollowUp.Mobile.cshtml compiles cleanly
- Both glass card classes used (form card + players card)
- datetime-local inputs with full inline JS (addDate/removeDate/renumberDates)
- Pre-Approved Players glass card present below the form
- Info alert present
</success_criteria>

<output>
After completion, create `.planning/phases/18-dm-editing-secondary-quest-views/18-02-SUMMARY.md`
</output>
