---
phase: 16-account-browse
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - EuphoriaInn.Service/Views/Account/Login.Mobile.cshtml
  - EuphoriaInn.Service/Views/Account/Register.Mobile.cshtml
  - EuphoriaInn.Service/Views/Account/Edit.Mobile.cshtml
  - EuphoriaInn.Service/Views/Account/Profile.Mobile.cshtml
  - EuphoriaInn.Service/Views/Account/ChangePassword.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/account.mobile.css
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03]

must_haves:
  truths:
    - "Login on mobile is a full-width single-column glass card form with Email/Password/RememberMe and a 44px-min Log in button (ACCT-01)"
    - "Register on mobile is a full-width single-column glass card form with Name/Email/Password/ConfirmPassword (ACCT-02)"
    - "Edit, Profile, and ChangePassword on mobile are usable single-column glass card views (ACCT-03)"
    - "All five mobile views link account.mobile.css and set no Layout (the expander + _ViewStart handle selection)"
    - "Desktop views are unchanged — no .cshtml without .Mobile suffix is modified"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Account/Login.Mobile.cshtml"
      provides: "ACCT-01 mobile Login form"
      contains: "account-card-mobile"
    - path: "EuphoriaInn.Service/Views/Account/Register.Mobile.cshtml"
      provides: "ACCT-02 mobile Register form"
      contains: "account-card-mobile"
    - path: "EuphoriaInn.Service/Views/Account/Edit.Mobile.cshtml"
      provides: "ACCT-03 mobile Edit form"
      contains: "account-card-mobile"
    - path: "EuphoriaInn.Service/Views/Account/Profile.Mobile.cshtml"
      provides: "ACCT-03 mobile Profile display"
      contains: "account-card-mobile"
    - path: "EuphoriaInn.Service/Views/Account/ChangePassword.Mobile.cshtml"
      provides: "ACCT-03 mobile ChangePassword form"
      contains: "account-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/account.mobile.css"
      provides: "Glass card + parchment + text-muted override CSS for all five Account views"
      contains: "account-card-mobile"
  key_links:
    - from: "Login.Mobile.cshtml"
      to: "account.mobile.css"
      via: "@section Styles link with asp-append-version"
      pattern: "account.mobile.css"
    - from: "Login.Mobile.cshtml"
      to: "AccountController.Login (POST)"
      via: "form asp-action=Login asp-route-returnurl"
      pattern: "asp-action=\"Login\""
---

<objective>
Create the five Account mobile views (`Login`, `Register`, `Edit`, `Profile`, `ChangePassword`) and the shared `account.mobile.css`. Each view is a full-width single-column glass card matching the Phase 13–15 aesthetic (D-02). All ViewModel bindings, form actions, and validation summaries are carried over verbatim from the desktop views; only the layout wrappers (`col-md-6`, `col-lg-4`, `row col-sm-3 col-sm-9`) are removed and replaced with the glass card pattern.

Purpose: Satisfies ACCT-01, ACCT-02, ACCT-03 — players can log in, register, edit their profile, change their password, and view their profile on a phone without layout breakage.
Output: 5 new `.Mobile.cshtml` views + 1 new CSS file. Zero desktop files modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-account-browse/16-RESEARCH.md
@.planning/phases/16-account-browse/16-UI-SPEC.md

# Desktop sources (exact bindings) + the glass card CSS reference
@EuphoriaInn.Service/Views/Account/Login.cshtml
@EuphoriaInn.Service/Views/Account/Register.cshtml
@EuphoriaInn.Service/Views/Account/Edit.cshtml
@EuphoriaInn.Service/Views/Account/Profile.cshtml
@EuphoriaInn.Service/Views/Account/ChangePassword.cshtml
@EuphoriaInn.Service/wwwroot/css/dm-profile.mobile.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create account.mobile.css (glass card + parchment + text-muted override)</name>
  <files>EuphoriaInn.Service/wwwroot/css/account.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/wwwroot/css/dm-profile.mobile.css (canonical glass card, parchment, section heading, badge-no-shadow patterns — replicate exactly)
    - EuphoriaInn.Service/wwwroot/css/mobile.css (lines around 40 — the global `.text-muted { color: #2c1810 }` rule that MUST be overridden inside glass cards; confirm the value before writing the scoped override)
    - .planning/phases/16-account-browse/16-RESEARCH.md ("Pattern: Glass Card Container" and "Pitfall 7: Text-muted Override")
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("CSS Architecture" section)
  </read_first>
  <action>
    Create `EuphoriaInn.Service/wwwroot/css/account.mobile.css`. Start with a single-line comment naming the file and stating it is loaded exclusively by the five Account `.Mobile.cshtml` views (no `@media` queries — see existing `*.mobile.css` files line 1). Do NOT add any `@media` block.

    Define class `.account-card-mobile` with exactly: `background: rgba(255, 255, 255, 0.15); backdrop-filter: blur(15px); border: 1px solid rgba(255, 255, 255, 0.3); border-radius: 12px; box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2); padding: 16px;`

    Parchment text rule — selector `.account-card-mobile h5, .account-card-mobile h6, .account-card-mobile .form-label, .account-card-mobile .form-check-label`: `color: #F4E4BC !important; text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);`

    Section heading rule — selector `.account-section-heading`: `color: #F4E4BC !important; text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9); text-transform: uppercase; font-size: 0.875rem; letter-spacing: 0.05em;`

    Faded parchment (hints, secondary) — selector `.account-card-mobile .form-text, .account-card-mobile small, .account-card-mobile .account-field-label`: `color: rgba(244, 228, 188, 0.7) !important; text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;`

    Profile field value — selector `.account-field-value`: `color: #F4E4BC !important; text-shadow: 1px 1px 3px rgba(0,0,0,0.9) !important; font-size: 1rem;`

    Text-muted scoped override (Pitfall 7) — selector `.account-card-mobile .text-muted`: `color: rgba(244, 228, 188, 0.7) !important;` (overrides the global dark-brown `.text-muted` from mobile.css so muted text stays readable on the dark glass surface).

    Badge text-shadow suppression — selector `.account-card-mobile .badge`: `text-shadow: none !important;`

    Subtitle helper — selector `.account-subtitle`: `color: rgba(244, 228, 188, 0.7) !important; text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important; font-size: 0.875rem;`
  </action>
  <verify>
    <automated>grep -v '^/\*' EuphoriaInn.Service/wwwroot/css/account.mobile.css | grep -c 'account-card-mobile'</automated>
  </verify>
  <acceptance_criteria>
    - File `EuphoriaInn.Service/wwwroot/css/account.mobile.css` exists
    - File contains the literal `backdrop-filter: blur(15px)`
    - File contains the literal `color: #F4E4BC !important`
    - File contains a `.account-card-mobile .text-muted` rule with `rgba(244, 228, 188, 0.7)`
    - File contains a `.account-card-mobile .badge` rule with `text-shadow: none !important`
    - File contains NO `@media` substring
  </acceptance_criteria>
  <done>account.mobile.css defines the glass card, parchment, faded parchment, section heading, profile field value, text-muted override, and badge-no-shadow rules; no media queries.</done>
</task>

<task type="auto">
  <name>Task 2: Create Login.Mobile.cshtml and Register.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/Account/Login.Mobile.cshtml, EuphoriaInn.Service/Views/Account/Register.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Account/Login.cshtml (exact `@model`, `@using`, asp-for bindings, form action, returnUrl chaining, `d-grid gap-2` + `btn-warning btn-lg` submit, the `col-md-6 col-lg-4` wrapper to remove)
    - EuphoriaInn.Service/Views/Account/Register.cshtml (exact bindings; confirm IsDungeonMaster is NOT rendered; password hint text "Password must be at least 6 characters long."; `col-md-6 col-lg-5` wrapper to remove)
    - EuphoriaInn.Service/Views/DungeonMaster/Profile.Mobile.cshtml (the `@section Styles` injection pattern + container structure)
    - .planning/phases/16-account-browse/16-RESEARCH.md ("Account/Login.cshtml" and "Account/Register.cshtml" binding tables; "Code Examples" → "Account Card Mobile")
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("Login.Mobile.cshtml" and "Register.Mobile.cshtml" layout contracts; Copywriting Contract)
  </read_first>
  <action>
    Create `Login.Mobile.cshtml`:
    - First line `@model LoginViewModel`; add `@using EuphoriaInn.Service.ViewModels.AccountViewModels` (and any using already present in the desktop view). Do NOT set `Layout`. Do NOT add any `@inject`.
    - `@section Styles { <link href="~/css/account.mobile.css" asp-append-version="true" rel="stylesheet" /> }`.
    - Single `<div class="account-card-mobile mb-3">` (no `row`/`col-*` wrapper — full width). Header `<h5 class="mb-0"><i class="fas fa-sign-in-alt text-warning me-2"></i>Log in</h5>`.
    - `<div asp-validation-summary="All" class="text-danger mb-3"></div>`.
    - `<form asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">` containing: Email field (`asp-for="Email"` label + `form-control` input + `asp-validation-for`), Password field (same), RememberMe (`form-check` with `asp-for="RememberMe"` `form-check-input` + `form-check-label`).
    - Submit: `<div class="d-grid gap-2 mt-3"><button type="submit" class="btn btn-warning btn-lg"><i class="fas fa-sign-in-alt me-2"></i>Log in</button></div>`.
    - `<hr>` then "Don't have an account?" text + `<a asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]" class="btn btn-warning">Create Account</a>` (preserve returnUrl chaining).

    Create `Register.Mobile.cshtml`:
    - `@model RegisterViewModel`; same `@using`; no `Layout`, no `@inject`; same `@section Styles` link to account.mobile.css.
    - `<div class="account-card-mobile mb-3">` header `<h5 class="mb-0"><i class="fas fa-user-plus text-warning me-2"></i>Register</h5>` and a subtitle `<p class="account-subtitle">Join the Quest Board community</p>`.
    - `<div asp-validation-summary="All" class="text-danger mb-3"></div>`.
    - `<form asp-action="Register" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">` with Name, Email, Password, ConfirmPassword fields (label + `form-control` + `asp-validation-for` each). Under Password add `<span class="form-text">Password must be at least 6 characters long.</span>` (verbatim desktop hint). Do NOT render an IsDungeonMaster checkbox (matches desktop).
    - Submit: `d-grid gap-2 mt-3` with `<button type="submit" class="btn btn-warning btn-lg"><i class="fas fa-user-plus me-2"></i>Create Account</button>`.
    - `<hr>` then "Already have an account?" + `<a asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" class="btn btn-warning">Log in</a>`.

    For both views: include the desktop's client-side validation partial reference if the desktop view has one (e.g. `@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }`) — copy it from the desktop view if present, otherwise omit.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Service/Views/Account/Login.Mobile.cshtml` exists and contains `account-card-mobile`, `asp-action="Login"`, `account.mobile.css`, and `btn-warning btn-lg`
    - `Login.Mobile.cshtml` contains NO `@inject` and NO `Layout =` substring
    - `Login.Mobile.cshtml` contains NO `col-md-6` and NO `col-lg-4` substring
    - `EuphoriaInn.Service/Views/Account/Register.Mobile.cshtml` exists and contains `account-card-mobile`, `asp-action="Register"`, and `Password must be at least 6 characters long.`
    - `Register.Mobile.cshtml` does NOT contain `IsDungeonMaster`
    - `dotnet build EuphoriaInn.Service` exits 0
  </acceptance_criteria>
  <done>Login and Register mobile views compile, are full-width single-column glass cards, carry exact ViewModel bindings and form actions, and link account.mobile.css.</done>
</task>

<task type="auto">
  <name>Task 3: Create Edit.Mobile.cshtml, Profile.Mobile.cshtml, ChangePassword.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/Account/Edit.Mobile.cshtml, EuphoriaInn.Service/Views/Account/Profile.Mobile.cshtml, EuphoriaInn.Service/Views/Account/ChangePassword.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Account/Edit.cshtml (exact `@model EditProfileViewModel`; hidden `asp-for="Id"`; HasKey checkbox; `asp-validation-summary="ModelOnly"`; Change Password link; Cancel→Profile; `col-md-6` wrapper to remove)
    - EuphoriaInn.Service/Views/Account/Profile.cshtml (exact `@model ProfileViewModel`; `Model.User?.Name/Email/HasKey`; `ViewData["IsAdmin"]`/`ViewData["IsDungeonMaster"]` flags; `TempData["SuccessMessage"]`; Edit + Home links; the `row col-sm-3 col-sm-9` label-value grid to flatten)
    - EuphoriaInn.Service/Views/Account/ChangePassword.cshtml (exact `@model ChangePasswordViewModel`; CurrentPassword/NewPassword/ConfirmPassword; `asp-validation-summary="All"`; Cancel→Profile; `btn-primary` submit)
    - .planning/phases/16-account-browse/16-RESEARCH.md ("Account/Edit.cshtml", "Account/Profile.cshtml", "Account/ChangePassword.cshtml" binding tables)
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("Edit.Mobile.cshtml", "Profile.Mobile.cshtml", "ChangePassword.Mobile.cshtml" layout contracts)
  </read_first>
  <action>
    Create all three views. Each: first line is the correct `@model`, add the `@using EuphoriaInn.Service.ViewModels.AccountViewModels` (plus any using present in the desktop view), no `Layout`, no `@inject`, and `@section Styles { <link href="~/css/account.mobile.css" asp-append-version="true" rel="stylesheet" /> }`. Wrap content in `<div class="account-card-mobile mb-3">` with no `row`/`col-*` wrappers.

    `Edit.Mobile.cshtml`:
    - Header `<h5 class="mb-0"><i class="fas fa-user-edit text-warning me-2"></i>Edit Profile</h5>`.
    - `<div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>` (note: ModelOnly, matching desktop).
    - `<form asp-action="Edit" method="post">` with `<input asp-for="Id" type="hidden" />` (preserve verbatim), Name field, Email field (`type="email"`), HasKey checkbox (`form-check` + `asp-for="HasKey"` `form-check-input` + label "Has Building Key").
    - `<hr>` then `<div class="account-section-heading mb-2">Password Management</div>` and a `<a asp-action="ChangePassword" class="btn btn-warning"><i class="fas fa-key me-2"></i>Change Password</a>` (filled btn-warning, replacing desktop btn-outline-primary per CLAUDE.md guideline).
    - `<hr>` then button row `<div class="d-flex justify-content-between">` Cancel `<a asp-action="Profile" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Cancel</a>` and Save `<button type="submit" class="btn btn-success"><i class="fas fa-save me-2"></i>Save Changes</button>`.

    `Profile.Mobile.cshtml` (display only — no form):
    - If `TempData["SuccessMessage"] != null`, render `<div class="alert alert-success"><i class="fas fa-check-circle me-2"></i>@TempData["SuccessMessage"]</div>` (do NOT apply `.text-danger`).
    - Header `<h5 class="mb-0"><i class="fas fa-user-cog text-success me-2"></i>Profile</h5>`.
    - Stacked label-above-value pairs (NOT `col-sm-3 col-sm-9`): for Name and Email, render `<div class="account-field-label">Name</div><div class="account-field-value">@Model.User?.Name</div>` then same for Email.
    - Account type indicator: if `(bool?)ViewData["IsAdmin"] == true` show `<span class="account-field-value"><i class="fas fa-shield-alt text-danger me-2"></i>Admin</span>`, else if `(bool?)ViewData["IsDungeonMaster"] == true` show `<i class="fas fa-crown text-warning me-2"></i>Dungeon Master`, else plain "Player". Use the exact ViewData cast pattern from the desktop view.
    - Building access: `@if (Model.User?.HasKey == true) {` icon `fas fa-key text-success` + "Has building key" `} else {` icon `fas fa-times text-muted` + "No building key" `}` (the `.text-muted` here renders correctly because of the account.mobile.css scoped override).
    - `<hr>` then button row `d-flex justify-content-between`: "Back to Quest Board" `<a asp-controller="Home" asp-action="Index" class="btn btn-warning"><i class="fas fa-home me-2"></i>Back to Quest Board</a>` and "Edit Profile" `<a asp-action="Edit" class="btn btn-primary"><i class="fas fa-user-edit me-2"></i>Edit Profile</a>`.

    `ChangePassword.Mobile.cshtml`:
    - Header `<h5 class="mb-0"><i class="fas fa-key text-primary me-2"></i>Change Password</h5>`.
    - `<div asp-validation-summary="All" class="text-danger mb-3"></div>`.
    - `<form asp-action="ChangePassword" method="post">` with CurrentPassword, NewPassword, ConfirmPassword fields (label + `form-control` + `asp-validation-for` each).
    - `<hr>` then button row `d-flex justify-content-between`: Cancel `<a asp-action="Profile" class="btn btn-secondary"><i class="fas fa-arrow-left me-2"></i>Cancel</a>` and `<button type="submit" class="btn btn-primary"><i class="fas fa-save me-2"></i>Change Password</button>`.

    For Edit and ChangePassword: if the desktop view includes a `@section Scripts { @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); } }`, copy it verbatim.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under `EuphoriaInn.Service/Views/Account/` with the `.Mobile.cshtml` suffix
    - `Edit.Mobile.cshtml` contains `account-card-mobile`, `asp-for="Id"`, `type="hidden"`, `asp-action="Edit"`, and `asp-validation-summary="ModelOnly"`
    - `Profile.Mobile.cshtml` contains `account-card-mobile`, `account-field-value`, and `ViewData["IsAdmin"]`; contains NO `col-sm-3`
    - `ChangePassword.Mobile.cshtml` contains `account-card-mobile`, `asp-action="ChangePassword"`, and `asp-validation-summary="All"`
    - None of the three files contain `@inject` or a `Layout =` assignment
    - `dotnet build EuphoriaInn.Service` exits 0
  </acceptance_criteria>
  <done>Edit, Profile, and ChangePassword mobile views compile, are single-column glass cards with exact bindings, and link account.mobile.css.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild test project and run Account mobile tests GREEN</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - .planning/STATE.md (Phase 15 Plan 04 decision: rebuild EuphoriaInn.IntegrationTests before running tests — WebApplicationFactory uses compiled output)
  </read_first>
  <action>
    Do not write code in this task — it is a verification gate. Rebuild the integration test project (so WebApplicationFactory picks up the newly compiled views), then run the four Account mobile tests authored in Plan 01 Task 1. They must turn GREEN now that the five Account `.Mobile.cshtml` views exist. If any assertion fails, fix the corresponding view's class name or CSS link (the test is the contract — do not change the test).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -3 && dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileAccount" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileAccount"` reports all 4 tests Passed, 0 Failed
    - `MobileAccountLogin_DesktopUserAgent_DoesNotRenderGlassCard` passes (desktop view unchanged — no `account-card-mobile` on desktop)
  </acceptance_criteria>
  <done>All four `MobileAccount*` tests pass; desktop regression guard passes; no test file was modified.</done>
</task>

</tasks>

<verification>
- `dotnet build EuphoriaInn.Service` exits 0
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileAccount"` → 4 passed, 0 failed
- `git status` shows only NEW files (5 .Mobile.cshtml + account.mobile.css) — no desktop `.cshtml` modified
</verification>

<success_criteria>
ACCT-01, ACCT-02, ACCT-03 satisfied: Login and Register are full-width single-column 44px-target forms; Edit, Profile, and ChangePassword are usable single-column glass card views on mobile. All five views link account.mobile.css, set no Layout, use no @inject, and carry verbatim desktop ViewModel bindings. Desktop experience unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-account-browse/16-02-SUMMARY.md` when done
</output>
