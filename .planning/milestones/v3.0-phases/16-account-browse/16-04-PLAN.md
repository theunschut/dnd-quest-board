---
phase: 16-account-browse
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - EuphoriaInn.Service/Views/GuildMembers/Index.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css
autonomous: true
requirements: [BROWSE-02]

must_haves:
  truths:
    - "Guild Members on mobile shows characters as single-column list rows (D-06), one per row, in glass-card sections (D-07)"
    - "Each row has a 40x40 circular thumbnail (or placeholder) on the left, name + class/level + badges on the right"
    - "Tapping a row navigates to GuildMembers/Details/{id}; the Create New Character button appears at the top"
    - "Retired character rows are dimmed; Other Characters rows show owner name; empty states render per UI-SPEC"
    - "The view links guild-members.mobile.css, sets no Layout, and uses no @inject"
  artifacts:
    - path: "EuphoriaInn.Service/Views/GuildMembers/Index.Mobile.cshtml"
      provides: "BROWSE-02 mobile Guild Members directory as list rows"
      contains: "guild-member-row"
    - path: "EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css"
      provides: "List row, circular thumbnail, section glass cards, dividers, empty-state CSS"
      contains: "guild-member-row"
  key_links:
    - from: "Index.Mobile.cshtml"
      to: "guild-members.mobile.css"
      via: "@section Styles link"
      pattern: "guild-members.mobile.css"
    - from: "guild-member-row"
      to: "GuildMembersController.Details"
      via: "onclick window.location.href Url.Action Details id"
      pattern: "GetProfilePicture"
---

<objective>
Create `GuildMembers/Index.Mobile.cshtml` and `guild-members.mobile.css`. The desktop `character-grid`/`character-card` layout is replaced with a flat single-column list (D-06): each character is one tappable row with a 40×40 circular profile thumbnail on the left and name + class/level + role/status badges on the right. The two sections (My Characters, Guild Roster) are each wrapped in a glass card (D-07). A full-width "Create New Character" button appears at the top of the page.

Purpose: Satisfies BROWSE-02 — the guild directory is readable and navigable on a phone with no overflow.
Output: 1 new `.Mobile.cshtml` view + 1 new CSS file. Zero desktop files modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-account-browse/16-RESEARCH.md
@.planning/phases/16-account-browse/16-UI-SPEC.md

@EuphoriaInn.Service/Views/GuildMembers/Index.cshtml
@EuphoriaInn.Service/wwwroot/css/dm-profile.mobile.css
@EuphoriaInn.Service/wwwroot/css/mobile.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guild-members.mobile.css (list rows, circular thumbnail, section cards, empty state)</name>
  <files>EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/wwwroot/css/dm-profile.mobile.css (glass card, parchment, section heading, tappable row `:active`, row divider `border-bottom: 1px solid rgba(139, 69, 19, 0.3)`, placeholder avatar patterns — replicate)
    - EuphoriaInn.Service/wwwroot/css/mobile.css (the global `.text-muted` rule to override)
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("Guild Members" layout contract: 40x40 thumbnail, row dividers, opacity 0.7 for retired, :active feedback, empty-state markup)
  </read_first>
  <action>
    Create `EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css`. Line 1: comment naming the file, loaded exclusively by GuildMembers/Index.Mobile.cshtml, no `@media`. Do NOT add any `@media` block.

    Section card — `.guild-section-card`: `background: rgba(255, 255, 255, 0.15); backdrop-filter: blur(15px); border: 1px solid rgba(255, 255, 255, 0.3); border-radius: 12px; box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2); padding: 12px;`

    Section heading — `.guild-section-heading`: `color: #F4E4BC !important; text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9); text-transform: uppercase; font-size: 0.875rem; letter-spacing: 0.05em;`

    List row — `.guild-member-row`: `padding: 12px 0; border-bottom: 1px solid rgba(139, 69, 19, 0.3); cursor: pointer;`
    `.guild-member-row:last-child { border-bottom: none; padding-bottom: 0; }`
    `.guild-member-row:active { background-color: rgba(255, 255, 255, 0.05); border-radius: 4px; }`
    `.guild-member-row.retired { opacity: 0.7; }`

    Thumbnail — `.guild-member-thumbnail`: `width: 40px; height: 40px; border-radius: 50%; object-fit: cover; flex-shrink: 0;`
    Placeholder — `.guild-member-placeholder`: `width: 40px; height: 40px; border-radius: 50%; background: rgba(255, 255, 255, 0.1); display: flex; align-items: center; justify-content: center; flex-shrink: 0; color: rgba(244, 228, 188, 0.7);`

    Name — `.guild-member-name`: `color: #F4E4BC !important; text-shadow: 1px 1px 3px rgba(0,0,0,0.9) !important; font-size: 1rem; line-height: 1.3;`
    Class/meta — `.guild-member-class, .guild-member-owner`: `color: rgba(244, 228, 188, 0.7) !important; text-shadow: 1px 1px 3px rgba(0,0,0,0.9) !important; font-size: 0.875rem;`

    Empty state — `.guild-empty-state`: `text-align: center; padding: 16px;` with `.guild-empty-state i { color: rgba(244, 228, 188, 0.7); }`, a parchment heading rule `.guild-empty-state h5 { color: #F4E4BC !important; text-shadow: 2px 2px 4px rgba(0,0,0,0.9); }`, and faded body `.guild-empty-state p { color: rgba(244, 228, 188, 0.7) !important; text-shadow: 1px 1px 3px rgba(0,0,0,0.9) !important; }`

    Text-muted override (Pitfall 7) — `.guild-section-card .text-muted`: `color: rgba(244, 228, 188, 0.7) !important;`
    Badge no-shadow — `.guild-section-card .badge`: `text-shadow: none !important;`
  </action>
  <verify>
    <automated>grep -v '^/\*' EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css | grep -c 'guild-member-row'</automated>
  </verify>
  <acceptance_criteria>
    - File `EuphoriaInn.Service/wwwroot/css/guild-members.mobile.css` exists
    - File contains a `.guild-member-thumbnail` rule with `border-radius: 50%` and `width: 40px`
    - File contains `.guild-member-row.retired` with `opacity: 0.7`
    - File contains a `.guild-section-card .text-muted` override with `rgba(244, 228, 188, 0.7)`
    - File contains a `.guild-section-card .badge` rule with `text-shadow: none !important`
    - File contains NO `@media` substring
  </acceptance_criteria>
  <done>guild-members.mobile.css defines section cards, list rows with dividers, 40x40 circular thumbnail + placeholder, parchment name/meta, retired dimming, empty state, text-muted override, badge-no-shadow; no media queries.</done>
</task>

<task type="auto">
  <name>Task 2: Create GuildMembers/Index.Mobile.cshtml (list rows + sections + empty states)</name>
  <files>EuphoriaInn.Service/Views/GuildMembers/Index.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/GuildMembers/Index.cshtml (exact `@model CharactersIndexViewModel`; `@using` list; `Model.MyCharacters`/`Model.OtherCharacters` loops; `Url.Action("Details", new { id = character.Id })`; `Url.Action("GetProfilePicture", new { id = character.Id })`; `Url.Action("Create")`; CharacterStatus.Retired + CharacterRole.Main badge logic; the `character-grid`/`character-card` markup to replace)
    - .planning/phases/16-account-browse/16-RESEARCH.md ("GuildMembers/Index.cshtml" binding tables; "Code Examples" → Guild Members List Row Pattern; route confirmations; Pitfall 5 — always route img src through GetProfilePicture)
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("Guild Members" layout contract: Create button at top, section structure, row layout, Other Characters owner line, empty states copy)
  </read_first>
  <action>
    Create `EuphoriaInn.Service/Views/GuildMembers/Index.Mobile.cshtml`:
    - First line `@model CharactersIndexViewModel`. Add `@using EuphoriaInn.Domain.Enums` and `@using EuphoriaInn.Service.ViewModels.CharacterViewModels` (match desktop using list). Do NOT set `Layout`. Do NOT add any `@inject`.
    - `@section Styles { <link href="~/css/guild-members.mobile.css" asp-append-version="true" rel="stylesheet" /> }`.

    Create button at top (UI-SPEC): `<a href="@Url.Action("Create")" class="btn btn-warning w-100 mb-3"><i class="fas fa-plus me-2"></i>Create New Character</a>`.

    My Characters section: `<div class="guild-section-card mb-4">` with `<div class="guild-section-heading mb-2"><i class="fas fa-user-circle text-success me-2"></i>My Characters</div>`. Then `@if (Model.MyCharacters.Any()) {` loop, `} else {` render the My Characters empty state: `<div class="guild-empty-state"><i class="fas fa-users-slash fa-2x mb-2"></i><h5>No Characters Yet</h5><p>Create your first character to join the guild!</p><a href="@Url.Action("Create")" class="btn btn-warning"><i class="fas fa-plus me-2"></i>Create Character</a></div>`.

    For each character in a section, render a row. Declare loop variables directly in C# code mode (no `@{}` wrapper inside foreach — Phase 13 pattern). Row:
    `<div class="guild-member-row d-flex align-items-center@(character.Status == CharacterStatus.Retired ? " retired" : "")" onclick="window.location.href='@Url.Action("Details", new { id = character.Id })'">`
    - Left: `@if (character.ProfilePicture != null) { <img src="@Url.Action("GetProfilePicture", new { id = character.Id })" class="guild-member-thumbnail me-3" alt="@character.Name" /> } else { <div class="guild-member-placeholder me-3"><i class="fas fa-user"></i></div> }` (always route the img src through GetProfilePicture — Pitfall 5).
    - Right: `<div class="flex-grow-1">` containing name `<div class="guild-member-name">@character.Name</div>`; class/level line `<div class="guild-member-class">Level @character.Level — @string.Join(" / ", character.Classes.Select(c => $"{c.Class} {c.ClassLevel}"))</div>`; badges line — for My Characters show a Main badge when `character.Role == CharacterRole.Main`: `<span class="badge bg-warning text-dark"><i class="fas fa-star me-1"></i>Main</span>`, and a Retired badge when `character.Status == CharacterStatus.Retired`: `<span class="badge bg-secondary">Retired</span>`.

    Guild Roster section: `<div class="guild-section-card mb-4">` with `<div class="guild-section-heading mb-2"><i class="fas fa-users text-primary me-2"></i>Guild Roster</div>`. Then `@if (Model.OtherCharacters.Any()) {` loop (same row structure but OMIT the Main badge per desktop behavior, and ADD an owner line `<div class="guild-member-owner"><i class="fas fa-user me-1"></i>@character.OwnerName</div>` below the class/level line), `} else {` render the Guild Roster empty state: `<div class="guild-empty-state"><i class="fas fa-dungeon fa-2x mb-2"></i><h5>No Other Characters</h5><p>The guild awaits other adventurers!</p></div>` (no button).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Service/Views/GuildMembers/Index.Mobile.cshtml` exists
    - File contains `guild-member-row`, `guild-members.mobile.css`, and `guild-section-card`
    - File contains `Url.Action("GetProfilePicture"` and `Url.Action("Details"`
    - File contains the literal `Create New Character` and `Url.Action("Create")`
    - File contains the empty-state copy `No Characters Yet` and `No Other Characters`
    - File contains NO `character-grid` and NO `character-card` substring (desktop layout replaced)
    - File contains NO `@inject` and NO `Layout =` substring
    - `dotnet build EuphoriaInn.Service` exits 0
  </acceptance_criteria>
  <done>Guild Members mobile view compiles: top Create button, two glass-card sections, single-column tappable rows with circular thumbnails/placeholders, Main/Retired badges, owner line on roster rows, retired dimming, both empty states.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild test project and run Guild Members mobile test GREEN</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - .planning/STATE.md (rebuild EuphoriaInn.IntegrationTests before running tests — WebApplicationFactory uses compiled output)
  </read_first>
  <action>
    Verification gate — no new code. Rebuild the integration test project, then run the Guild Members mobile test from Plan 01 Task 2 (`MobileGuildMembers_MobileUserAgent_RendersListRows`). It must turn GREEN (asserts `guild-members.mobile.css` link is present on the authenticated `/GuildMembers` mobile response). If it fails, fix the view's `@section Styles` link (the test is the contract).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -3 && dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileGuildMembers" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileGuildMembers"` reports 1 test Passed, 0 Failed
  </acceptance_criteria>
  <done>The `MobileGuildMembers*` test passes; no test file was modified.</done>
</task>

</tasks>

<verification>
- `dotnet build EuphoriaInn.Service` exits 0
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileGuildMembers"` → 1 passed, 0 failed
- `git status` shows only NEW files (GuildMembers/Index.Mobile.cshtml + guild-members.mobile.css) — desktop GuildMembers/Index.cshtml untouched
</verification>

<success_criteria>
BROWSE-02 satisfied: the Guild Members directory on mobile is a single-column list of tappable rows (circular thumbnail + name + class/level + badges) inside glass-card sections, with a top Create button, retired-row dimming, owner line on roster rows, and both empty states. Desktop Guild Members unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-account-browse/16-04-SUMMARY.md` when done
</output>
