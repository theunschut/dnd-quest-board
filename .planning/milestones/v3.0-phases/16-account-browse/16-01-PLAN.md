---
phase: 16-account-browse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03, BROWSE-01, BROWSE-02]

must_haves:
  truths:
    - "MobileViewsTests.cs contains a test asserting account-card-mobile + account.mobile.css on /Account/Login mobile (ACCT-01)"
    - "MobileViewsTests.cs contains a test asserting account-card-mobile on /Account/Register mobile (ACCT-02)"
    - "MobileViewsTests.cs contains tests asserting account-card-mobile on authenticated /Account/Edit, /Account/Profile, /Account/ChangePassword mobile (ACCT-03)"
    - "MobileViewsTests.cs contains a test asserting shop-item-card-mobile + 'Filter & Sort' + shop.mobile.css on authenticated /Shop mobile (BROWSE-01)"
    - "MobileViewsTests.cs contains a test asserting guild-member-row + guild-members.mobile.css on authenticated /GuildMembers mobile (BROWSE-02)"
    - "All new test methods compile and are discovered by the runner (RED expected — views do not exist yet)"
  artifacts:
    - path: "EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs"
      provides: "Integration test stubs for ACCT-01..03 and BROWSE-01..02"
      contains: "MobileAccountLogin_MobileUserAgent_RendersGlassCardForm"
  key_links:
    - from: "MobileViewsTests.cs"
      to: "Account/Login.Mobile.cshtml"
      via: "account-card-mobile CSS class + account.mobile.css link assertion"
      pattern: "account-card-mobile"
    - from: "MobileViewsTests.cs"
      to: "Shop/Index.Mobile.cshtml"
      via: "shop-item-card-mobile + 'Filter & Sort' assertion"
      pattern: "shop-item-card-mobile"
    - from: "MobileViewsTests.cs"
      to: "GuildMembers/Index.Mobile.cshtml"
      via: "guild-member-row CSS class assertion"
      pattern: "guild-member-row"
---

<objective>
Add integration test stubs to `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` covering all five Phase 16 requirements (ACCT-01, ACCT-02, ACCT-03, BROWSE-01, BROWSE-02). These tests are the Nyquist sampling harness: they assert the CSS class names and per-page CSS file links that Plans 02–04 will produce. They start RED (the `.Mobile.cshtml` views do not exist yet, so the expander falls back to the desktop view which lacks these mobile-only class names) and turn GREEN as each implementation plan lands.

Purpose: Establish executable, automated verification before any view is written, so Plans 02–04 have a real `<automated>` command to prove completion.
Output: 8 new `[Fact]` test methods appended to the existing `MobileViewsTests` class.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-account-browse/16-RESEARCH.md

# The existing test file — append to it, do not recreate
@EuphoriaInn.Service/Views/Account/Login.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Account-page test stubs (ACCT-01, ACCT-02, ACCT-03)</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs (read the WHOLE file — copy the exact pattern of existing methods: `GetWithUserAgentAsync`, the `MobileUserAgent`/`DesktopUserAgent` constants, the authenticated-request pattern using `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync` + manual `HttpRequestMessage` with `request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization`)
    - EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs (lines 87–95 — `CreateAuthenticatedClientWithUserAsync(factory, userName, email, password, name, roles)` signature)
    - .planning/phases/16-account-browse/16-RESEARCH.md (section "Phase Requirements → Test Map" and "Account/Login.cshtml" bindings)
  </read_first>
  <action>
    Append FIVE new `[Fact]` async test methods to the existing `MobileViewsTests` class in `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` (do not modify or remove any existing method; insert before the closing brace of the class). Reuse the existing private `GetWithUserAgentAsync` helper and `MobileUserAgent`/`DesktopUserAgent` constants already defined in the file.

    Login and Register are anonymous routes — use `GetWithUserAgentAsync(url, MobileUserAgent)` directly. Profile, Edit, and ChangePassword are `[Authorize]` routes (confirmed: `AccountController.Profile/Edit/ChangePassword` carry `[Authorize]`) — for these use `AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(_factory, "<unique-user>", "<unique-email>")` then build a manual `HttpRequestMessage`, set `request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent)`, set `request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization`, send via `_client.SendAsync(request, TestContext.Current.CancellationToken)` — copy this exact authenticated pattern from the existing `MobileDmManage_MobileUserAgent_RendersCondensedVoteBadges` method.

    The five methods (use these exact names — they are referenced by `must_haves` and downstream plans):

    1. `MobileAccountLogin_MobileUserAgent_RendersGlassCardForm` — GET `/Account/Login` with `MobileUserAgent`. Assert `response.StatusCode` is `HttpStatusCode.OK`; assert `html.Should().Contain("account-card-mobile")`; assert `html.Should().Contain("account.mobile.css")`.

    2. `MobileAccountLogin_DesktopUserAgent_DoesNotRenderGlassCard` — GET `/Account/Login` with `DesktopUserAgent`. Assert OK; assert `html.Should().NotContain("account-card-mobile")`. (Proves desktop view is unchanged — ROADMAP success-criterion / INFRA contract regression guard.)

    3. `MobileAccountRegister_MobileUserAgent_RendersGlassCardForm` — GET `/Account/Register` with `MobileUserAgent`. Assert OK; assert `html.Should().Contain("account-card-mobile")`; assert `html.Should().Contain("account.mobile.css")`.

    4. `MobileAccountEdit_MobileUserAgent_RendersGlassCardForm` — authenticated GET `/Account/Edit` with `MobileUserAgent` (user `acct_edit16` / `acct_edit16@test.com`). Assert OK; assert `html.Should().Contain("account-card-mobile")`.

    5. `MobileAccountProfile_MobileUserAgent_RendersGlassCardLayout` — authenticated GET `/Account/Profile` with `MobileUserAgent` (user `acct_prof16` / `acct_prof16@test.com`). Assert OK; assert `html.Should().Contain("account-card-mobile")`.

    Use unique userName/email per authenticated test to avoid Identity uniqueness collisions across the shared test database (same convention as existing tests, e.g. `dm_dmview03`).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` contains the exact string `MobileAccountLogin_MobileUserAgent_RendersGlassCardForm`
    - `MobileViewsTests.cs` contains the exact string `MobileAccountRegister_MobileUserAgent_RendersGlassCardForm`
    - `MobileViewsTests.cs` contains the exact string `MobileAccountProfile_MobileUserAgent_RendersGlassCardLayout`
    - A grep of `MobileViewsTests.cs` for `account-card-mobile` returns at least 3 matches
    - A grep of `MobileViewsTests.cs` for `account.mobile.css` returns at least 2 matches
    - `dotnet build EuphoriaInn.IntegrationTests` exits 0 (the new methods compile)
  </acceptance_criteria>
  <done>Five Account test stubs compile and are discoverable; they fail at assertion time because the mobile views do not yet exist (RED by design).</done>
</task>

<task type="auto">
  <name>Task 2: Append Shop + Guild Members test stubs (BROWSE-01, BROWSE-02)</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs (the authenticated-request pattern, after Task 1's edits)
    - .planning/phases/16-account-browse/16-RESEARCH.md (sections "Shop/Index.cshtml" and "GuildMembers/Index.cshtml" — class names `shop-item-card-mobile`, `guild-member-row`; CSS files `shop.mobile.css`, `guild-members.mobile.css`)
    - .planning/phases/16-account-browse/16-UI-SPEC.md ("Filter & Sort" copy contract)
  </read_first>
  <action>
    Append THREE more `[Fact]` async test methods to the same `MobileViewsTests` class. Both `/Shop` and `/GuildMembers` are `[Authorize]` routes (confirmed: `ShopController` and `GuildMembersController` carry `[Authorize]`) — use the authenticated `HttpRequestMessage` pattern (CreateAuthenticatedClientWithUserAsync + manual request with mobile User-Agent + Authorization header copied from authClient), identical to Task 1's authenticated tests.

    The three methods (exact names):

    1. `MobileShopIndex_MobileUserAgent_RendersItemGridAndFilterButton` — authenticated GET `/Shop` with `MobileUserAgent` (user `shop_browse16` / `shop_browse16@test.com`). Assert OK; assert `html.Should().Contain("Filter & Sort")`; assert `html.Should().Contain("shop.mobile.css")`. (Item grid class `shop-item-card-mobile` only renders when items exist; the filter button and CSS link render unconditionally, so assert on those for a stable smoke test — note in a comment that `shop-item-card-mobile` is verified by Plan 03's own seeded test.)

    2. `MobileShopIndex_MobileUserAgent_OmitsPurchaseHistoryPanel` — authenticated GET `/Shop` with `MobileUserAgent` (user `shop_d04_16` / `shop_d04_16@test.com`). Assert OK; assert `html.Should().NotContain("purchase-history-panel")`. (Enforces D-04 — Purchase History side panel omitted on mobile.)

    3. `MobileGuildMembers_MobileUserAgent_RendersListRows` — authenticated GET `/GuildMembers` with `MobileUserAgent` (user `guild_browse16` / `guild_browse16@test.com`). Assert OK; assert `html.Should().Contain("guild-members.mobile.css")`. (The `guild-member-row` class only appears when characters exist; the CSS link renders unconditionally, so assert on the link for a stable smoke test — note in a comment that `guild-member-row` is verified by Plan 04's own seeded test.)
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `MobileViewsTests.cs` contains the exact string `MobileShopIndex_MobileUserAgent_RendersItemGridAndFilterButton`
    - `MobileViewsTests.cs` contains the exact string `MobileShopIndex_MobileUserAgent_OmitsPurchaseHistoryPanel`
    - `MobileViewsTests.cs` contains the exact string `MobileGuildMembers_MobileUserAgent_RendersListRows`
    - A grep of `MobileViewsTests.cs` for `Filter & Sort` returns at least 1 match
    - A grep of `MobileViewsTests.cs` for `shop.mobile.css` returns at least 1 match
    - A grep of `MobileViewsTests.cs` for `guild-members.mobile.css` returns at least 1 match
    - `dotnet build EuphoriaInn.IntegrationTests` exits 0
  </acceptance_criteria>
  <done>Three Browse test stubs compile and are discoverable; `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileShopIndex" --list-tests` lists 2 methods and `~MobileGuildMembers` lists 1; all RED until Plans 03/04 land.</done>
</task>

</tasks>

<verification>
- `dotnet build EuphoriaInn.IntegrationTests --no-restore` exits 0 (all 8 new stubs compile)
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileAccount" --list-tests` lists 4 methods
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileShopIndex" --list-tests` lists 2 methods
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileGuildMembers" --list-tests` lists 1 method
- No existing test method was removed or renamed (the 22 prior tests still present)
</verification>

<success_criteria>
8 new `[Fact]` test methods exist in `MobileViewsTests.cs`, all compile, all are discoverable, and each asserts a Phase 16 mobile-only CSS class name and/or per-page CSS file link. The suite is RED for the new tests (views absent) and unchanged-green for all prior tests.
</success_criteria>

<output>
Create `.planning/phases/16-account-browse/16-01-SUMMARY.md` when done
</output>
