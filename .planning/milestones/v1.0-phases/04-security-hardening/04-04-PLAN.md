---
phase: 04-security-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements: [SEC-06]
must_haves:
  truths:
    - ".gitignore contains an exact `.env` entry (not `*.env`, not commented)"
    - ".env is not tracked by git going forward"
    - ".env.example remains tracked"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rule for .env"
      contains: ".env"
  key_links:
    - from: "git index"
      to: ".env"
      via: "tracked status"
      pattern: "(no tracked .env)"
---

<objective>
Stop tracking `.env` in git going forward. Add a precise `.env` entry to `.gitignore` and run `git rm --cached .env` if the file is currently tracked. `.env.example` remains the only tracked env file.

Purpose: Addresses SEC-06.
Output: `.gitignore` contains a `.env` rule; no `.env` path appears in `git ls-files`. History is NOT rewritten (accepted per D-11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-security-hardening/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .env to .gitignore and untrack if present</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current file — note the existing structure ends with "# Claude Code configuration" / ".claude/" and a trailing "# End of ..." comment)
    - .planning/phases/04-security-hardening/04-CONTEXT.md (D-10, D-11, specifics: entry is literal `.env`, NOT `*.env`)
  </read_first>
  <action>
    (1) Check if `.env` is currently tracked:
        ```bash
        git ls-files -- .env
        ```
        Record the result. Two cases:
        - Case A — output is empty: `.env` is NOT tracked. Proceed to step (2); skip step (3).
        - Case B — output contains `.env`: proceed to step (2) AND step (3).

    (2) Edit `.gitignore`. Append the following block at the END of the file (after the existing `# Claude Code configuration` / `.claude/` block, before the `# End of https://...` trailing comment — or simply at the very end if ordering with the trailing comment is awkward):
        ```
        # Environment variables (SEC-06)
        .env
        ```
        IMPORTANT: the entry must be literally `.env` on its own line. Do NOT use `*.env` (that would also ignore `.env.example`). Do NOT comment the line.

    (3) (Only if Case B from step 1) Untrack the file while keeping it on disk:
        ```bash
        git rm --cached .env
        ```
        This removes the file from the index but NOT from the working tree. History is NOT rewritten — the old contents remain in git history, which is accepted per D-11.

    (4) Verify `.env.example` is still tracked:
        ```bash
        git ls-files -- .env.example
        ```
        Must output `.env.example`. If empty, STOP and report — do not proceed.

    (5) Do NOT commit here — the orchestrator handles the phase commit via gsd-tools.
  </action>
  <verify>
    <automated>grep -Fx ".env" .gitignore && ! git ls-files --error-unmatch -- .env 2>/dev/null && git ls-files --error-unmatch -- .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains a line that is EXACTLY `.env` (no leading/trailing whitespace, no `#` prefix) — verifiable with `grep -Fx ".env" .gitignore` (exits 0)
    - `.gitignore` does NOT contain the pattern `*.env` as an active rule (verifiable with `grep -Fx "*.env" .gitignore` exits 1)
    - `git ls-files -- .env` produces empty output (the file is not tracked)
    - `git ls-files -- .env.example` outputs `.env.example` (the example is still tracked)
    - `git check-ignore -v .env` reports that `.gitignore` matches (if a `.env` file exists in the working tree; harmless if it doesn't)
  </acceptance_criteria>
  <done>`.gitignore` has the exact `.env` entry; `.env` is not in the index; `.env.example` remains tracked.</done>
</task>

</tasks>

<verification>
- `grep -Fx ".env" .gitignore` exits 0
- `git ls-files -- .env` is empty
- `git ls-files -- .env.example` outputs `.env.example`
</verification>

<success_criteria>
SEC-06 complete: `.env` will never be committed again via accidental `git add`, while `.env.example` remains the reference template. History is unchanged (accepted).
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-hardening/04-04-SUMMARY.md`.
</output>
