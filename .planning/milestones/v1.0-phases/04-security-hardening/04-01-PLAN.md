---
phase: 04-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/Program.cs
  - EuphoriaInn.Service/Controllers/Admin/AccountController.cs
  - EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs
autonomous: true
requirements: [SEC-01, SEC-03]
must_haves:
  truths:
    - "Program.cs configures Identity with MaxFailedAccessAttempts = 5 and DefaultLockoutTimeSpan = 15 minutes"
    - "Program.cs sets Identity RequiredLength to 8"
    - "AccountController.Login POST passes lockoutOnFailure: true to PasswordSignInAsync"
    - "AccountController.Login POST shows a lockout-specific error when result.IsLockedOut is true"
    - "RegisterViewModel Password MinimumLength is 8 (aligned with Identity)"
  artifacts:
    - path: "EuphoriaInn.Service/Program.cs"
      provides: "Identity lockout + 8-char password config"
      contains: "MaxFailedAccessAttempts = 5"
    - path: "EuphoriaInn.Service/Controllers/Admin/AccountController.cs"
      provides: "Login with lockout handling"
      contains: "lockoutOnFailure: true"
    - path: "EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs"
      provides: "Register DTO with 8-char client-side minimum"
      contains: "MinimumLength = 8"
  key_links:
    - from: "AccountController.Login POST"
      to: "userService.PasswordSignInAsync"
      via: "lockoutOnFailure parameter"
      pattern: "lockoutOnFailure:\\s*true"
    - from: "Program.cs AddIdentity options"
      to: "Identity lockout enforcement"
      via: "options.Lockout"
      pattern: "options\\.Lockout\\.MaxFailedAccessAttempts"
---

<objective>
Wire up ASP.NET Core Identity account lockout (5 attempts / 15-min lock) and raise the minimum password length from 6 to 8. Friendly inline error when an account is locked.

Purpose: Addresses SEC-01 (lockout) and SEC-03 (password minimum) — the two config-level security changes.
Output: Program.cs, AccountController.cs, and RegisterViewModel.cs updated; build passes; lockout error surfaces on login form after 5 failed attempts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-security-hardening/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enable Identity lockout + raise password minimum to 8</name>
  <files>EuphoriaInn.Service/Program.cs, EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs</files>
  <read_first>
    - EuphoriaInn.Service/Program.cs (see current AddIdentity options block at lines ~33-46)
    - EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs (see MinimumLength = 6 on Password)
    - .planning/phases/04-security-hardening/04-CONTEXT.md (D-02, D-05, D-06, Claude's Discretion on MinLength alignment)
  </read_first>
  <action>
    (1) In `EuphoriaInn.Service/Program.cs`, inside the existing `AddIdentity<UserEntity, IdentityRole<int>>(options => { ... })` lambda:
        - Change `options.Password.RequiredLength = 6;` to `options.Password.RequiredLength = 8;` (per D-05).
        - Add a new `LockoutOptions` configuration block AFTER the `// User settings` block and BEFORE the closing `}` of the lambda (per D-02):
          ```csharp
          // Lockout settings
          options.Lockout.MaxFailedAccessAttempts = 5;
          options.Lockout.DefaultLockoutTimeSpan = TimeSpan.FromMinutes(15);
          options.Lockout.AllowedForNewUsers = true;
          ```
    (2) In `EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs`, update the `[StringLength]` attribute on the `Password` property:
        - Change `MinimumLength = 6` to `MinimumLength = 8` so client-side validation matches Identity's new rule (per Claude's Discretion note in CONTEXT.md).
        - Keep the error message template `"The {0} must be at least {2} and at max {1} characters long."` untouched — `{2}` will now render as `8`.
    (3) Do NOT touch any other property, attribute, or method.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Service/Program.cs` contains the literal string `options.Lockout.MaxFailedAccessAttempts = 5`
    - `EuphoriaInn.Service/Program.cs` contains the literal string `options.Lockout.DefaultLockoutTimeSpan = TimeSpan.FromMinutes(15)`
    - `EuphoriaInn.Service/Program.cs` contains the literal string `options.Password.RequiredLength = 8`
    - `EuphoriaInn.Service/Program.cs` does NOT contain the literal string `options.Password.RequiredLength = 6`
    - `EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs` contains `MinimumLength = 8`
    - `EuphoriaInn.Service/ViewModels/AccountViewModels/RegisterViewModel.cs` does NOT contain `MinimumLength = 6`
    - `dotnet build EuphoriaInn.sln` exits 0 with zero errors
  </acceptance_criteria>
  <done>Identity lockout + 8-char password rule configured in Program.cs; RegisterViewModel matches. Build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire lockoutOnFailure + friendly locked-out error in Login POST</name>
  <files>EuphoriaInn.Service/Controllers/Admin/AccountController.cs</files>
  <read_first>
    - EuphoriaInn.Service/Controllers/Admin/AccountController.cs (current Login POST at lines 18-37)
    - .planning/phases/04-security-hardening/04-CONTEXT.md (D-01, D-03, integration points note)
  </read_first>
  <action>
    In `EuphoriaInn.Service/Controllers/Admin/AccountController.cs`, inside the `[HttpPost]` `Login(LoginViewModel model, string? returnUrl = null)` action:

    (1) Change the call on line 26 from:
        `var result = await userService.PasswordSignInAsync(model.Email, model.Password, model.RememberMe, lockoutOnFailure: false);`
        to:
        `var result = await userService.PasswordSignInAsync(model.Email, model.Password, model.RememberMe, lockoutOnFailure: true);`
        (per D-01).

    (2) After the existing `if (result.Succeeded) { return RedirectToLocal(returnUrl); }` block and BEFORE the line `ModelState.AddModelError(string.Empty, "Invalid login attempt.");`, add a new branch (per D-03):
        ```csharp
        if (result.IsLockedOut)
        {
            ModelState.AddModelError(string.Empty, "Account locked due to too many failed attempts. Try again in 15 minutes.");
            return View(model);
        }
        ```

    (3) The existing `ModelState.AddModelError(string.Empty, "Invalid login attempt.");` remains as the fall-through for non-success, non-lockout cases.

    (4) Verify that `IUserService.PasswordSignInAsync` returns a type whose surface includes both `Succeeded` and `IsLockedOut` (it wraps `Microsoft.AspNetCore.Identity.SignInResult` — which does have `IsLockedOut`). If the wrapper does NOT expose `IsLockedOut`, add the property by forwarding to the underlying Identity result. Do NOT break any existing caller.

    (5) No other change to this file.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln --nologo && dotnet test EuphoriaInn.sln --nologo --no-build</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Service/Controllers/Admin/AccountController.cs` contains the literal string `lockoutOnFailure: true`
    - `EuphoriaInn.Service/Controllers/Admin/AccountController.cs` does NOT contain the literal string `lockoutOnFailure: false`
    - `EuphoriaInn.Service/Controllers/Admin/AccountController.cs` contains the literal string `result.IsLockedOut`
    - `EuphoriaInn.Service/Controllers/Admin/AccountController.cs` contains the literal string `"Account locked due to too many failed attempts. Try again in 15 minutes."`
    - `dotnet build EuphoriaInn.sln` exits 0
    - `dotnet test EuphoriaInn.sln` exits 0 (all existing tests still pass)
  </acceptance_criteria>
  <done>Login POST uses lockoutOnFailure: true and renders the friendly locked-out message when Identity reports IsLockedOut.</done>
</task>

</tasks>

<verification>
- `dotnet build EuphoriaInn.sln` succeeds with zero errors
- `dotnet test EuphoriaInn.sln` all tests pass
- grep confirms all five literal strings in the acceptance criteria above are present in the touched files
</verification>

<success_criteria>
SEC-01 configuration complete (lockout enabled, 5 attempts, 15 min, friendly message on lockout). SEC-03 complete (8-char minimum, aligned in RegisterViewModel). No behavioural change to any flow beyond the two new restrictions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-security-hardening/04-01-SUMMARY.md`.
</output>
