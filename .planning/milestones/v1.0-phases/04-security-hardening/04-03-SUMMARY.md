---
phase: 04-security-hardening
plan: "03"
subsystem: database
tags: [ef-core, migrations, identity, lockout, sql-server]

# Dependency graph
requires:
  - phase: 04-01
    provides: Identity lockout policy configured in Program.cs (SEC-01)
provides:
  - EF Core migration backfilling LockoutEnabled = 1 for all existing AspNetUsers rows
affects: [04-04, any phase touching Identity configuration]

# Tech tracking
tech-stack:
  added: []
  patterns: [migrationBuilder.Sql() for data-only migrations with no schema change]

key-files:
  created:
    - EuphoriaInn.Repository/Migrations/20260420142117_EnableLockoutForExistingUsers.cs
    - EuphoriaInn.Repository/Migrations/20260420142117_EnableLockoutForExistingUsers.Designer.cs
  modified: []

key-decisions:
  - "SEC-02: Data-only migration using migrationBuilder.Sql() — no model/schema changes, empty Up/Down generated by EF tooling then filled manually"

patterns-established:
  - "Data-only migrations: generate empty migration with dotnet ef migrations add, then manually add migrationBuilder.Sql() calls to Up/Down"

requirements-completed: [SEC-02]

# Metrics
duration: 5min
completed: 2026-04-20
---

# Phase 4 Plan 3: Security Hardening — LockoutEnabled Backfill Summary

**EF Core data-only migration backfilling `LockoutEnabled = 1` for all existing `AspNetUsers` rows so SEC-01 lockout policy applies to every account on next startup**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-20T14:21:00Z
- **Completed:** 2026-04-20T14:26:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Generated EF Core migration `EnableLockoutForExistingUsers` using `dotnet ef migrations add` from the Service project
- Added `migrationBuilder.Sql("UPDATE AspNetUsers SET LockoutEnabled = 1")` in `Up()` with SEC-02 comment
- Added `migrationBuilder.Sql("UPDATE AspNetUsers SET LockoutEnabled = 0")` in `Down()` for reversibility
- Solution builds with 0 errors, migration registered with EF tooling

## Task Commits

Each task was committed atomically:

1. **Task 1: Create EF Core migration backfilling LockoutEnabled = 1** - `36b1750` (feat)

## Files Created/Modified

- `EuphoriaInn.Repository/Migrations/20260420142117_EnableLockoutForExistingUsers.cs` - Data-only migration with SEC-02 SQL in Up/Down
- `EuphoriaInn.Repository/Migrations/20260420142117_EnableLockoutForExistingUsers.Designer.cs` - EF-generated designer/snapshot file

## Decisions Made

- Data-only migration pattern: generate empty migration shell with EF tooling, then manually insert `migrationBuilder.Sql()` calls — no model changes needed since `LockoutEnabled` column already exists (managed by ASP.NET Core Identity schema)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. Migration auto-applies on next application startup via `context.Database.Migrate()` in `ServiceExtensions.ConfigureDatabase()`.

## Next Phase Readiness

- SEC-02 complete: on next app startup, all existing `AspNetUsers` rows get `LockoutEnabled = 1`
- SEC-01 lockout policy (5 attempts / 15-min lock) from Plan 04-01 now applies universally to all accounts
- Ready for Plan 04-04

---
*Phase: 04-security-hardening*
*Completed: 2026-04-20*
