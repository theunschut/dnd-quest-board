---
phase: 12-mobile-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - EuphoriaInn.Service/wwwroot/css/mobile.css
  - EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs
autonomous: true
requirements: [INFRA-06]
must_haves:
  truths:
    - "mobile.css applies a minimum 44px height to interactive elements (buttons, form controls, nav links)"
    - "mobile.css defines a mobile typography scale (16px base to prevent iOS input-focus zoom) and spacing overrides"
    - "mobile.css carries the D&D theme baseline: Cinzel navbar-brand font and a dark mobile-layout background"
    - "A mobile-UA page response links mobile.css; a desktop-UA page response does not"
  artifacts:
    - path: "EuphoriaInn.Service/wwwroot/css/mobile.css"
      provides: "Baseline mobile touch-target, typography, spacing, and D&D theme CSS"
      contains: "min-height: 44px"
      min_lines: 20
    - path: "EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs"
      provides: "Integration + file-content tests for INFRA-06"
      contains: "mobile.css"
  key_links:
    - from: "EuphoriaInn.Service/Views/Shared/_Layout.Mobile.cshtml"
      to: "EuphoriaInn.Service/wwwroot/css/mobile.css"
      via: "link rel=stylesheet href=~/css/mobile.css asp-append-version=true (created in Plan 02)"
      pattern: "css/mobile\\.css"
---

<objective>
Create the `mobile.css` baseline stylesheet that gives the mobile shell its touch-target sizing, mobile typography scale, spacing overrides, and D&D theme baseline (Cinzel brand font + dark navbar palette) per INFRA-06 and D-04. Add tests that confirm the file contains the `min-height: 44px` rule and that a live mobile-UA response links `mobile.css` while a desktop-UA response does not.

Purpose: This is the final infrastructure piece. The `_Layout.Mobile.cshtml` from Plan 02 already links `~/css/mobile.css`; this plan creates the file it points to so the mobile shell looks on-brand even before any `.Mobile.cshtml` content views land (D-05). After this, Phase 12 is fully wired and Phases 13-16 can add mobile views.

Output: `mobile.css` (new) and `MobileCssTests.cs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-mobile-infrastructure/12-CONTEXT.md
@.planning/phases/12-mobile-infrastructure/12-RESEARCH.md
@.planning/phases/12-mobile-infrastructure/12-PATTERNS.md
@.planning/phases/12-mobile-infrastructure/12-VALIDATION.md
@.planning/phases/12-mobile-infrastructure/12-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan creates these symbols:

- `EuphoriaInn.Service/wwwroot/css/mobile.css` (static stylesheet) — baseline mobile CSS
  - Touch-target rule: `min-height: 44px` on `.btn, a.nav-link, input, select, textarea, .form-control, .form-select`
  - Typography: `body { font-size: 16px; line-height: 1.5; }`
  - Spacing: `.container-fluid` horizontal padding override
  - Theme: `.navbar-brand { font-family: 'Cinzel', serif; ... }`, `.mobile-layout { background-color: #212529; }`, `.mobile-layout .navbar { min-height: 56px; }`
- Test file: `EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile.css baseline stylesheet</name>
  <files>EuphoriaInn.Service/wwwroot/css/mobile.css</files>
  <read_first>
    - .planning/phases/12-mobile-infrastructure/12-RESEARCH.md (Pattern 5 mobile.css baseline — authoritative content lines 314-359; note no @media query needed lines 357-359)
    - .planning/phases/12-mobile-infrastructure/12-PATTERNS.md (mobile.css section lines 292-341 — complete authoritative content, analog site.css, asp-append-version note)
    - .planning/phases/12-mobile-infrastructure/12-CONTEXT.md (D-04 mobile.css baseline: 44px, typography scale, spacing, Cinzel + dark navbar theme; Claude's Discretion exact selectors lines 37)
    - EuphoriaInn.Service/wwwroot/css/site.css (the static-CSS analog — plain CSS, no preprocessor, hex colors matching Bootstrap semantic colors)
  </read_first>
  <action>
    Create `EuphoriaInn.Service/wwwroot/css/mobile.css` as plain CSS (no preprocessor, no `@media` query — this file is loaded ONLY by `_Layout.Mobile.cshtml` on mobile requests, so device targeting is already handled by the layout selection). Include these rule groups per RESEARCH.md Pattern 5 and D-04:

    1. Touch targets (INFRA-06): a selector group `.btn, a.nav-link, input, select, textarea, .form-control, .form-select` with `min-height: 44px;`. The literal `min-height: 44px` must appear (the INFRA-06 file-content test greps for it).

    2. Typography scale: `body { font-size: 16px; line-height: 1.5; }` — the 16px base prevents iOS auto-zoom on input focus.

    3. Spacing override: `.container-fluid { padding-left: 0.5rem; padding-right: 0.5rem; }`.

    4. D&D theme baseline (D-04): `.navbar-brand { font-family: 'Cinzel', serif; font-weight: 600; }` (Cinzel is already loaded via the Google Fonts CDN link in `_Layout.Mobile.cshtml` — no extra font load needed). `.mobile-layout { background-color: #212529; }` (matches Bootstrap `bg-dark`). `.mobile-layout .navbar { min-height: 56px; }`.

    Do not add any other rules in this phase — D-03/D-05 keep Phase 12 to an infrastructure baseline; per-view mobile styling lands in Phases 13-16.
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.IntegrationTests --filter "MobileCss"</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.Service/wwwroot/css/mobile.css` exists
    - The file contains the literal `min-height: 44px` applied to a selector group including `.btn`, `a.nav-link`, `input`, `select`, `textarea`
    - The file contains `body` rule with `font-size: 16px`
    - The file contains `.navbar-brand` with `font-family: 'Cinzel', serif`
    - The file contains `.mobile-layout` with `background-color: #212529`
    - The file contains no `@media` query
    - `dotnet test EuphoriaInn.IntegrationTests --filter "MobileCss"` is green (file-content test passes)
  </acceptance_criteria>
  <done>mobile.css provides the 44px touch-target baseline, 16px mobile typography, spacing overrides, and the Cinzel/dark D&D theme, with no @media query, and the MobileCss test filter passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add INFRA-06 tests — CSS content and mobile.css link presence</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs</files>
  <read_first>
    - EuphoriaInn.IntegrationTests/Controllers/HomeControllerIntegrationTests.cs (IClassFixture pattern, _client.GetAsync, content assertions, TestContext.Current.CancellationToken)
    - EuphoriaInn.IntegrationTests/Mobile/MobileLayoutTests.cs (Plan 02 — reuse its mobile/desktop UA constants and per-request HttpRequestMessage UA pattern; keep consistent style)
    - .planning/phases/12-mobile-infrastructure/12-RESEARCH.md (INFRA-06 tests — mobile.css link assertion lines 631-641; CSS content smoke test lines 656-664)
    - .planning/phases/12-mobile-infrastructure/12-PATTERNS.md (Integration Test: Custom Request with Headers lines 456-464)
    - .planning/phases/12-mobile-infrastructure/12-VALIDATION.md (12-03-01 → filter "MobileCss"; the test name must contain "MobileCss")
  </read_first>
  <action>
    Create `EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs` in namespace `EuphoriaInn.IntegrationTests.Mobile`. Ensure the class name contains "MobileCss" so the VALIDATION.md `--filter "MobileCss"` selects every test here. Include two kinds of tests:

    1. File-content test (no web host): locate `mobile.css` and assert its contents. Resolve the path robustly — the test working directory is the test project's bin folder, so walk up from `AppContext.BaseDirectory` to the repo and into `EuphoriaInn.Service/wwwroot/css/mobile.css`, OR search upward for the `EuphoriaInn.Service` directory. Read the file with `File.ReadAllText(path)` and assert `css.Should().Contain("min-height: 44px")`. If the path cannot be resolved, fail with a clear message naming the attempted path (do not silently pass). Name this test method to include "MobileCss" (e.g. `MobileCss_File_Contains44pxTouchTargetRule`).

    2. Link-presence integration test: class `MobileCssTests : IClassFixture<WebApplicationFactoryBase>`, `_client = factory.CreateClient()`. Using the same mobile and desktop UA constants as `MobileLayoutTests`, issue a mobile-UA `HttpRequestMessage` GET "/" and assert `html.Should().Contain("mobile.css")` (the `asp-append-version` link rendered by `_Layout.Mobile.cshtml`). Issue a desktop-UA GET "/" and assert `html.Should().NotContain("mobile.css")` and `html.Should().Contain("site.css")` (proving the desktop layout still loads its own CSS — desktop unchanged). Assert `response.StatusCode.Should().Be(System.Net.HttpStatusCode.OK)` on both. Name these methods with "MobileCss" too.

    Add `using EuphoriaInn.IntegrationTests.Helpers;` and `using System.Net.Http.Headers;` as needed. Use `TestContext.Current.CancellationToken` on async calls to match the codebase convention.
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.IntegrationTests --filter "MobileCss"</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs` exists with class name containing "MobileCss"
    - A file-content test reads `EuphoriaInn.Service/wwwroot/css/mobile.css` and asserts it contains `min-height: 44px` (INFRA-06)
    - The file-content test fails with a clear, path-naming message if the file cannot be located (no silent pass)
    - An integration test asserts a mobile-UA response HTML contains "mobile.css" (link present on mobile)
    - An integration test asserts a desktop-UA response HTML does NOT contain "mobile.css" and DOES contain "site.css" (desktop unchanged)
    - `dotnet test EuphoriaInn.IntegrationTests --filter "MobileCss"` is fully green
  </acceptance_criteria>
  <done>INFRA-06 is locked by a file-content test proving the 44px rule exists and an integration test proving mobile.css links only on mobile responses while desktop keeps site.css.</done>
</task>

<task type="auto">
  <name>Task 3: Full mobile suite gate</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileCssTests.cs</files>
  <read_first>
    - .planning/phases/12-mobile-infrastructure/12-VALIDATION.md (Sampling Rate — phase gate: full suite must be green before /gsd-verify-work; Per-Task Verification Map covering 12-01 through 12-03)
    - .planning/phases/12-mobile-infrastructure/12-01-PLAN.md (the middleware/expander tests that must remain green)
    - .planning/phases/12-mobile-infrastructure/12-02-PLAN.md (the layout integration tests that must remain green)
  </read_first>
  <action>
    No new code in this task — it is the phase-merge gate that proves all of Phase 12's infrastructure works together and nothing regressed. Run the full mobile test set across all three plans and then the entire solution test suite. If any test from Plan 01 (MobileDetectionMiddleware, MobileViewLocationExpander), Plan 02 (MobileLayoutOffcanvas, DesktopLayoutParity, MobileViewResolution), or Plan 03 (MobileCss) fails, do not mark the phase complete — diagnose and fix the offending artifact in its owning plan's files before re-running. Confirm zero failures across the whole solution (proves desktop behavior is unregressed — Success Criterion 3).
  </action>
  <verify>
    <automated>dotnet test</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test EuphoriaInn.IntegrationTests --filter "MobileDetectionMiddleware|MobileViewLocationExpander|MobileLayoutOffcanvas|DesktopLayoutParity|MobileViewResolution|MobileCss"` reports all mobile tests passed
    - `dotnet test` (full solution) reports zero failed tests — every pre-existing desktop test still passes (no regression)
  </acceptance_criteria>
  <done>The entire mobile test set and the full solution suite are green, confirming the Phase 12 pipeline works end to end with no desktop regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → static file server | `mobile.css` served via `UseStaticFiles()`; standard static-asset delivery, no dynamic input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | `mobile.css` static file | accept | Static stylesheet contains no secrets, no PII, no environment data — only layout/theme rules. Served from `wwwroot/css/` like the existing six desktop CSS files; same exposure profile, no threat above low. |
| T-12-08 | Tampering | `mobile.css` cache busting | accept | `asp-append-version="true"` (set on the link in Plan 02) appends a content hash so a tampered/stale cached file is detectable by version mismatch; standard ASP.NET static-file integrity behavior. |
| T-12-SC | Tampering | npm/nuget installs | mitigate | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: zero new dependencies). No install checkpoint required. |
</threat_model>

<verification>
- `dotnet test EuphoriaInn.IntegrationTests --filter "MobileCss"` — INFRA-06 green (content + link tests)
- `dotnet test` — full solution green, no desktop regression (Success Criterion 3)
- Manual: open any route with a mobile UA in a browser; confirm Cinzel brand font and dark navbar render
</verification>

<success_criteria>
- INFRA-06: `mobile.css` provides 44px touch targets, 16px typography, spacing overrides, and Cinzel/dark theme; verified by file-content test
- INFRA-06 (link): mobile responses link `mobile.css`; desktop responses do not (and keep `site.css`); verified by integration test
- Phase gate: full `dotnet test` green — every mobile test plus all pre-existing tests pass; desktop unchanged
- ROADMAP Success Criterion 4 satisfied: mobile.css loaded on mobile, touch targets ≥ 44px
</success_criteria>

<output>
Create `.planning/phases/12-mobile-infrastructure/12-03-SUMMARY.md` when done.
</output>
