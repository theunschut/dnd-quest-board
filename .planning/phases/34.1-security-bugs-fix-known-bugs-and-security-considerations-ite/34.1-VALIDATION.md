---
phase: 34.1
slug: security-bugs-fix-known-bugs-and-security-considerations-ite
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-01
---

# Phase 34.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **Source note:** No dedicated RESEARCH.md exists for Phase 34.1 (research was skipped for this phase — Phase 34's `34-RESEARCH.md` already validated this exact scope: Known Bugs + Security Considerations + related fixes, same codebase/test infrastructure). Test Infrastructure and Security Domain sections below are carried over from `.planning/phases/34-codebase-cleanup-and-security-hardening-remove-unused-code-s/34-RESEARCH.md`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | xUnit v3 (`xunit.v3` 3.2.2, `xunit.runner.visualstudio` 3.1.5), `Microsoft.NET.Test.Sdk` 18.7.0 |
| **Config file** | `QuestBoard.IntegrationTests/xunit.runner.json` |
| **Quick run command** | `dotnet test QuestBoard.UnitTests` |
| **Full suite command** | `dotnet test` |
| **Estimated runtime** | ~60 seconds (191 tests as of Phase 33 close, plus whatever this phase adds) |

---

## Sampling Rate

- **After every task commit:** Run `dotnet build`; `dotnet test QuestBoard.UnitTests` for logic changes
- **After every plan wave:** Run `dotnet test` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

No REQ-IDs are mapped to this phase — mapped to `CONCERNS.md` items instead. Task IDs are TBD until the planner assigns them.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | Known Bugs: startup email-config validation | — | App throws in Production with missing `Email:FromEmail`/`Email:SmtpServer`; starts fine in Testing/Development | integration | New test — must not trigger the throw path in `WebApplicationFactoryBase`'s Testing environment | ❌ Wave 0 — net-new test | ⬜ pending |
| TBD | TBD | TBD | Known Bugs: `SessionReminderJob.cs` null-dereference — verify-and-close | — | N/A — already null-safe (`quest.DungeonMaster?.Name ?? string.Empty`), confirm and document, do not write a redundant fix | build | `dotnet build` | N/A — compiler is the test; confirm via source read | ⬜ pending |
| TBD | TBD | TBD | Known Bugs: Resend API pagination/429 retry-backoff | — | Retries on 429, succeeds on eventual 2xx; pagination already exists per Phase 34 RESEARCH.md — confirm, don't re-implement | unit | New test mocking `HttpClient` via `IHttpClientFactory` for the 429-retry path; existing pagination confirmed by source read | ❌ Wave 0 — net-new test for retry only | ⬜ pending |
| TBD | TBD | TBD | Security: CSRF `[ValidateAntiForgeryToken]` regression test | T-34.1-01 (ASVS V4) | Every `[HttpPost]` state-changing controller action carries `[ValidateAntiForgeryToken]` | integration | New test sweeping all `[HttpPost]` actions via reflection or an explicit per-controller assertion list | ❌ Wave 0 — net-new test | ⬜ pending |
| TBD | TBD | TBD | Security: email config secrets not logged | T-34.1-02 (ASVS V7) | No secret values (SMTP password, connection strings) appear in log messages or exception traces | manual/code review | Manual review of `logger.LogError`/`LogWarning` call sites in `EmailService.cs`, `AdminController.cs` | N/A | ⬜ pending |
| TBD | TBD | TBD | Security: Resend token HttpClient pattern — documentation | T-34.1-03 (ASVS V13, informational) | Per-request `Authorization` header injection pattern documented in code comment (already correct behavior, per Phase 34 RESEARCH.md) | build | `dotnet build` | N/A — comment-only change | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New test: startup email-config validation (Production-only throw) — needs care to not trigger in Testing environment
- [ ] New test: Resend API 429 retry-backoff behavior (mock `HttpClient` via `IHttpClientFactory`) — scoped strictly to `GetResendStatsAsync`'s HTTP retry logic per Phase 34 RESEARCH.md's pitfall warning (do not touch `SessionReminderJob`/`DailyReminderJob` send-one-email-each behavior — that's the deferred digest-batching item)
- [ ] New test: CSRF `[ValidateAntiForgeryToken]` coverage sweep across all `[HttpPost]` actions

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Email config secrets never appear in logs | Security Considerations: Email Configuration Secrets Potentially Logged | Requires reading log call sites and exception-handling code, not something a compiled test asserts | Grep `EmailService.cs` and `AdminController.cs` for `logger.Log*` calls; confirm no interpolated secret values in the message template |
| `SessionReminderJob.cs` null-dereference claim is verified stale | Known Bugs: Nullable Navigation Property | Confirms a CONCERNS.md claim is already resolved rather than requiring a code fix | Read `SessionReminderJob.cs`, confirm `quest.DungeonMaster?.Name ?? string.Empty` (or equivalent null-safe pattern) is in place; document as verified-closed rather than fixed |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
