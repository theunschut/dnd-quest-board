---
phase: 19-admin-shop-management-views
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml
  - EuphoriaInn.Service/Views/Admin/Quests.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css
  - EuphoriaInn.Service/wwwroot/css/admin-quests.mobile.css
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "On a mobile user agent, /Admin/Users renders one glass card per user with role badge and applicable Promote/Demote/Edit/Delete buttons"
    - "On a mobile user agent, /Admin/Quests renders one glass card per quest with status badge and Edit/Delete buttons; description is omitted"
    - "deleteUser() and deleteQuest() inline fetch-DELETE functions are present and use the antiforgery RequestVerificationToken header"
    - "Desktop user agent on /Admin/Users and /Admin/Quests is unaffected (no admin-*-card-mobile classes served)"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml"
      provides: "Mobile glass-card list of users with role-based action buttons"
      contains: "admin-users-card-mobile"
    - path: "EuphoriaInn.Service/Views/Admin/Quests.Mobile.cshtml"
      provides: "Mobile glass-card list of quests with status badges"
      contains: "admin-quests-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css"
      provides: "Glass card + stacked action button styles for Users mobile"
      contains: "admin-users-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/admin-quests.mobile.css"
      provides: "Glass card styles for Quests mobile"
      contains: "admin-quests-card-mobile"
  key_links:
    - from: "EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml"
      to: "EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css"
      via: "@section Styles link with asp-append-version"
      pattern: "admin-users\\.mobile\\.css"
    - from: "EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml"
      to: "Antiforgery RequestVerificationToken"
      via: "deleteUser() fetch DELETE header"
      pattern: "RequestVerificationToken"
---

<objective>
Create the mobile glass-card list variants for Admin/Users and Admin/Quests, plus their two per-view CSS files. Both are list views that carry inline fetch-DELETE JavaScript (deleteUser / deleteQuest) copied verbatim from the desktop views into @section Scripts.

Purpose: ADMIN-01 — the admin user list and the admin quest list must be usable on mobile without horizontal table overflow. The desktop versions are wide tables that overflow on phones.
Output: Two .Mobile.cshtml views + two .mobile.css files; the GetMobilePage_AdminUsers and GetMobilePage_AdminQuests integration tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-admin-shop-management-views/19-CONTEXT.md
@.planning/phases/19-admin-shop-management-views/19-PATTERNS.md
@.planning/phases/19-admin-shop-management-views/19-UI-SPEC.md
@EuphoriaInn.Service/Views/Admin/Users.cshtml
@EuphoriaInn.Service/Views/Admin/Quests.cshtml
@EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml
@EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02 portion)

New view files:
- `EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml`
- `EuphoriaInn.Service/Views/Admin/Quests.Mobile.cshtml`

New CSS files:
- `EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css`
- `EuphoriaInn.Service/wwwroot/css/admin-quests.mobile.css`

New CSS class names introduced: `admin-users-card-mobile`, `user-card-mobile`, `admin-quests-card-mobile`, `quest-card-mobile`. JS function names reused (already exist in desktop): `deleteUser`, `deleteQuest`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin/Users.Mobile.cshtml + admin-users.mobile.css</name>
  <files>EuphoriaInn.Service/Views/Admin/Users.Mobile.cshtml, EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/Views/Admin/Users.cshtml (desktop source — copy the model declaration `@model IEnumerable<UserManagementViewModel>`, the `@{ var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext); }` block, the role-badge conditionals, all four Promote/Demote `<form asp-action="...">` blocks, the Edit/Delete buttons, the empty-state markup, and the deleteUser() JS verbatim)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 33-135 (exact excerpts and line references for every element of this view)
    - EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml (analog for @section Styles injection placement and the no-Layout / no-@inject conventions)
    - EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css (canonical CSS template — glass card values, parchment text values, badge no-text-shadow rule)
  </read_first>
  <action>
    Create Users.Mobile.cshtml declaring `@model IEnumerable<UserManagementViewModel>` with the same `@using` directives as the desktop view. Put `@{ var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext); }` in the top code block. Do NOT add `@inject IAntiforgery` (already globally injected via _ViewImports.cshtml). Do NOT set `Layout`.

    Add `@section Styles` linking `~/css/admin-users.mobile.css` with `asp-append-version="true"`.

    Wrap the page in a glass card div with class `admin-users-card-mobile mb-3`. Inside, render a heading `<h5>` with `<i class="fas fa-users-cog text-danger me-2"></i>User Management`. Then `@foreach (var userModel in Model)` render a `user-card-mobile mb-3` sub-card containing: a top row (`d-flex align-items-center gap-2`) with a role icon + `<strong class="parchment-text">@userModel.User.Name</strong>` + the role badge (copy the IsAdmin/IsDungeonMaster/IsPlayer conditional verbatim from desktop — Administrator=bg-danger fa-crown, Dungeon Master=bg-warning fa-crown, Player=bg-primary fa-dice-d20 per the desktop source); omit the email per Claude's discretion (D — UI-SPEC chose to omit). Then a button row `d-flex flex-wrap gap-2` containing all four Promote/Demote `<form asp-action="...">` blocks verbatim from desktop (they use tag helpers and work unchanged), followed by the Edit anchor (`btn btn-info btn-sm` with `asp-action="EditUser" asp-route-userId="@userModel.User.Id"`) and the Delete button (`btn btn-danger btn-sm` with `onclick="deleteUser(@userModel.User.Id)"`).

    Add the empty-state markup (centered `fa-users fa-3x` icon + "No users found. Users appear here once registered." per UI-SPEC Copywriting) for the case where Model has no items.

    Add `@section Scripts` containing the deleteUser() function copied verbatim from the desktop view (fetch DELETE to `/Admin/DeleteUser/${id}`, `'RequestVerificationToken': '@tokens.RequestToken'` header, confirm dialog text "Are you sure you want to delete this user? This action cannot be undone.", `location.reload()` on success).

    Create admin-users.mobile.css using the quest-edit.mobile.css template. Header comment line 1: `/* admin-users.mobile.css - Admin/Users.Mobile.cshtml only */`. Comment line 2 stating the file has no media queries and is loaded exclusively by Admin/Users.Mobile.cshtml via _Layout.Mobile.cshtml. Define `.admin-users-card-mobile` with the glass card values (background rgba(255,255,255,0.15), backdrop-filter blur(15px), border 1px solid rgba(255,255,255,0.3), border-radius 12px, box-shadow 0 8px 32px rgba(0,0,0,0.2), padding 16px). Define `.user-card-mobile` as a nested sub-card with the same glass values but padding 12px and a bottom margin. Add the parchment-text rule for `h5` and `.parchment-text` (color #F4E4BC !important + the two-layer text-shadow). Add the faded-parchment rule for `small`. Add `.badge { text-shadow: none !important; }`. Do NOT add any media queries. Do NOT redefine `.btn` min-height (already in mobile.css).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - Users.Mobile.cshtml contains the literal `admin-users-card-mobile` and `user-card-mobile`
    - Users.Mobile.cshtml links `~/css/admin-users.mobile.css` with `asp-append-version="true"`
    - Users.Mobile.cshtml contains `deleteUser` inside an `@section Scripts` block and references `@tokens.RequestToken` in the fetch header
    - Users.Mobile.cshtml does NOT contain `@inject IAntiforgery` and does NOT contain `Layout =`
    - admin-users.mobile.css contains `.admin-users-card-mobile` with `backdrop-filter` and `border-radius: 12px`
    - `grep -v '^/\*' admin-users.mobile.css | grep -c "@media"` returns 0 (no media queries; the head comment that names the rule is excluded)
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminUsers" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    Admin/Users.Mobile.cshtml renders a per-user glass card list with role badges and conditional action buttons, the deleteUser() fetch works with the antiforgery header, and the ADMIN-01 Users integration test is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Admin/Quests.Mobile.cshtml + admin-quests.mobile.css</name>
  <files>EuphoriaInn.Service/Views/Admin/Quests.Mobile.cshtml, EuphoriaInn.Service/wwwroot/css/admin-quests.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/Views/Admin/Quests.cshtml (desktop source — copy `@model IEnumerable<Quest>`, the `@{ var tokens = Antiforgery.GetAndStoreTokens(...); }` block, the status-badge computation block (statusBadge/statusIcon/statusText with the IsFinalized + FinalizedDate logic producing bg-dark/bg-primary/bg-success), the DM-name display `@(quest.DungeonMaster?.Name ?? "Unknown")`, the Edit/Delete buttons, and the deleteQuest() JS verbatim)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 139-218 (exact excerpts and line references)
    - EuphoriaInn.Service/wwwroot/css/admin-users.mobile.css (the file created in Task 1 — mirror its structure for consistency)
  </read_first>
  <action>
    Create Quests.Mobile.cshtml declaring `@model IEnumerable<Quest>` with the same `@using` directives as the desktop view. Put `@{ var tokens = Antiforgery.GetAndStoreTokens(ViewContext.HttpContext); }` in the top code block. Do NOT add `@inject`. Do NOT set `Layout`.

    Add `@section Styles` linking `~/css/admin-quests.mobile.css` with `asp-append-version="true"`.

    Wrap in a glass card `admin-quests-card-mobile mb-3` with a heading `<h5>` using `<i class="fas fa-scroll text-warning me-2"></i>Quest Management` (or the desktop heading text). Then `@foreach (var quest in Model)` render a `quest-card-mobile mb-3` sub-card. Inside the loop body, declare the status-badge variables directly in C# code mode (do NOT wrap in `@{}` — Razor is already in code mode inside the foreach; per STATE.md Phase 13 Plan 02 rule) by copying the statusBadge/statusIcon/statusText computation block from the desktop view. Render: top row with `<i class="fas fa-dragon text-muted me-2"></i>` + `<strong class="parchment-text">@quest.Title</strong>` + the status badge `<span class="badge @statusBadge"><i class="@statusIcon me-1"></i>@statusText</span>`; a meta line `<small class="text-muted">DM: @(quest.DungeonMaster?.Name ?? "Unknown")</small>`. OMIT the quest description (D-03). Then a button row `d-flex flex-wrap gap-2`: Edit anchor (`btn btn-info btn-sm`, `asp-controller="Quest" asp-action="Edit" asp-route-id="@quest.Id"`) and Delete button (`btn btn-danger btn-sm`, `onclick="deleteQuest(@quest.Id)"`).

    Add empty-state markup ("No quests found. Quests appear here once created by a DM." per UI-SPEC Copywriting, centered with a `fa-scroll fa-3x` icon).

    Add `@section Scripts` containing deleteQuest() copied verbatim from desktop (fetch DELETE to `/Admin/DeleteQuest/${id}`, `'RequestVerificationToken': '@tokens.RequestToken'` header, confirm "Are you sure you want to delete this quest? This action cannot be undone.", `location.reload()` on success).

    Create admin-quests.mobile.css mirroring admin-users.mobile.css: header comment `/* admin-quests.mobile.css - Admin/Quests.Mobile.cshtml only */`, a no-media-query note comment, `.admin-quests-card-mobile` glass card (16px padding), `.quest-card-mobile` nested sub-card (12px padding), parchment text rule for `h5`/`.parchment-text`, faded rule for `small`, and `.badge { text-shadow: none !important; }`. No media queries.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - Quests.Mobile.cshtml contains the literal `admin-quests-card-mobile` and `quest-card-mobile`
    - Quests.Mobile.cshtml links `~/css/admin-quests.mobile.css` with `asp-append-version="true"`
    - Quests.Mobile.cshtml contains `deleteQuest` inside `@section Scripts` and references `@tokens.RequestToken`
    - Quests.Mobile.cshtml renders the status badge using `@statusBadge` and does NOT render the quest description property
    - Quests.Mobile.cshtml does NOT contain `@inject IAntiforgery` and does NOT contain `Layout =`
    - admin-quests.mobile.css contains `.admin-quests-card-mobile` with glass card values
    - `grep -v '^/\*' admin-quests.mobile.css | grep -c "@media"` returns 0
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminQuests" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    Admin/Quests.Mobile.cshtml renders a per-quest glass card list with status badges (no description), the deleteQuest() fetch works with the antiforgery header, and the ADMIN-01 Quests integration test is GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → AdminController | Admin-only DELETE/POST requests cross from the mobile client to the server; authorization enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | deleteUser/deleteQuest fetch DELETE | mitigate | `RequestVerificationToken` header sourced from `Antiforgery.GetAndStoreTokens(ViewContext.HttpContext).RequestToken`, copied verbatim from desktop; controller actions retain `[ValidateAntiForgeryToken]` |
| T-19-03 | Elevation of Privilege | Admin mobile views | mitigate | No new controller actions; `[Authorize(Policy = "AdminOnly")]` on AdminController still enforces access; views are purely additive |
| T-19-04 | Tampering (XSS) | quest title / user name rendering | mitigate | Razor auto-encodes `@quest.Title` and `@userModel.User.Name`; no Html.Raw used |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md confirms zero new packages) |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.Service --no-restore -q` succeeds
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_Admin" -q` shows AdminUsers and AdminQuests GREEN
- Desktop user agent on /Admin/Users still returns the desktop view (no admin-*-card-mobile class) — covered by the mobile-vs-desktop expander already proven in Phase 12
</verification>

<success_criteria>
Both admin list views render as single-column glass-card lists on mobile with working delete-fetch JS and correct badges; ADMIN-01 Users and Quests integration tests are GREEN; desktop views are unchanged.
</success_criteria>

<output>
Create `.planning/phases/19-admin-shop-management-views/19-02-SUMMARY.md` when done
</output>
