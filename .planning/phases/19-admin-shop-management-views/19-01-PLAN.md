---
phase: 19-admin-shop-management-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs
autonomous: true
requirements: [ADMIN-01, ADMIN-02, SHOPMGMT-01]
must_haves:
  truths:
    - "ADMIN-01, ADMIN-02, SHOPMGMT-01 are defined in REQUIREMENTS.md with a Phase 19 traceability row each"
    - "REQUIREMENTS.md no longer contradicts itself by listing admin mobile views as deferred/out-of-scope while Phase 19 implements them"
    - "Seven RED integration test methods exist in MobileViewsTests.cs (one per phase view target), each asserting the per-view glass card class and the per-view CSS filename"
    - "The integration test project compiles (tests fail at assertion, not at compile)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADMIN-01/ADMIN-02/SHOPMGMT-01 requirement definitions + traceability rows"
      contains: "ADMIN-01"
    - path: "EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs"
      provides: "Phase 19 RED test stubs"
      contains: "GetMobilePage_AdminUsers"
  key_links:
    - from: "EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs"
      to: "AuthenticationHelper.CreateAuthenticatedAdminClientAsync"
      via: "Admin route tests authenticate with an Admin-role user"
      pattern: "CreateAuthenticatedAdminClientAsync|roles.*Admin"
---

<objective>
Establish the Phase 19 scaffold: add the three phase requirements to REQUIREMENTS.md, reconcile the existing REQUIREMENTS.md notes that mark admin mobile views as deferred/out-of-scope, and append seven RED integration test methods (one per view target) to the existing MobileViewsTests.cs harness.

Purpose: The Nyquist sampling harness must exist before any view implementation so every implementation plan has an automated GREEN target. The requirement IDs ADMIN-01, ADMIN-02, SHOPMGMT-01 are referenced in ROADMAP.md Phase 19 but are not yet defined in REQUIREMENTS.md.
Output: Updated REQUIREMENTS.md + 7 new RED test methods in MobileViewsTests.cs that compile and fail at assertion (views do not exist yet).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-admin-shop-management-views/19-RESEARCH.md
@EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs
@EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs
@EuphoriaInn.IntegrationTests/Helpers/TestDataHelper.cs
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 portion)

New requirement IDs added to REQUIREMENTS.md:
- `ADMIN-01` — Admin user list and edit pages usable on mobile without horizontal scrolling
- `ADMIN-02` — Shop Management index, create, and edit pages fully functional on mobile
- `SHOPMGMT-01` — Shop item detail page renders in single-column layout with no overflow

New test methods added to `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs`:
- `GetMobilePage_AdminUsers_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_AdminEditUser_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_AdminQuests_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_AdminResetPassword_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_ShopManagementIndex_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_ShopManagementCreate_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_ShopManagementEdit_ReturnsSuccessAndMobileLayout`
- `GetMobilePage_ShopDetails_ReturnsSuccessAndMobileLayout`

Glass-card class tokens these tests assert (produced by later plans, listed here so drift verification excludes them as new symbols): `admin-users-card-mobile`, `admin-form-card-mobile`, `admin-quests-card-mobile`, `shop-mgmt-index-card-mobile`, `shop-mgmt-create-card-mobile`, `shop-mgmt-edit-card-mobile`, `shop-details-card-mobile`. CSS filenames: `admin-users.mobile.css`, `admin-quests.mobile.css`, `admin-form.mobile.css`, `shop-management-index.mobile.css`, `shop-management-create.mobile.css`, `shop-management-edit.mobile.css`, `shop-details.mobile.css`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add ADMIN-01, ADMIN-02, SHOPMGMT-01 to REQUIREMENTS.md and reconcile deferral notes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — see the existing requirement section format, the "Future Requirements" list, the "Out of Scope" list, and the Traceability table at the bottom)
    - .planning/ROADMAP.md lines 197-211 (Phase 19 goal, requirement IDs, 3 success criteria — copy descriptions verbatim)
  </read_first>
  <action>
    Add a new `### Admin & Shop Management` section to the "## v1 Requirements" area of REQUIREMENTS.md (place it after the "### Quest Log" QLOG-01 entry, before "## Future Requirements"). Add exactly three unchecked requirement bullets using the existing bold-ID format:
    - ADMIN-01: "Admin user list and edit pages are usable on mobile without horizontal scrolling"
    - ADMIN-02: "Shop Management index, create, and edit pages are fully functional on mobile"
    - SHOPMGMT-01: "Shop item detail page renders in a single-column layout with no overflow"
    Use the descriptions verbatim from ROADMAP.md lines 204-206.

    Reconcile the existing contradictions: in "## Future Requirements" remove the line "Admin views on mobile — low usage frequency; defer", and in "## Out of Scope" remove the bullet "**Admin views** — admin actions are low-frequency and expected on desktop". These two notes now contradict the active Phase 19 scope (Milestone 3 elevated admin mobile views into scope). Do NOT remove any other Future/Out-of-Scope entries.

    Add three rows to the "## Traceability" table at the bottom, matching the existing column format (`| Requirement | Phase | Status |`):
    - `| ADMIN-01 | Phase 19 | Pending |`
    - `| ADMIN-02 | Phase 19 | Pending |`
    - `| SHOPMGMT-01 | Phase 19 | Pending |`
  </action>
  <verify>
    <automated>grep -E "ADMIN-01|ADMIN-02|SHOPMGMT-01" .planning/REQUIREMENTS.md | grep -v '^#' | wc -l | awk '$1>=6{print "PASS"; exit} {print "FAIL: expected >=6 matches (3 defs + 3 traceability)"}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADMIN-01" .planning/REQUIREMENTS.md` returns at least 2 (definition bullet + traceability row)
    - `grep -c "ADMIN-02" .planning/REQUIREMENTS.md` returns at least 2
    - `grep -c "SHOPMGMT-01" .planning/REQUIREMENTS.md` returns at least 2
    - The string "Admin views on mobile — low usage frequency; defer" no longer appears in the file
    - The Out-of-Scope bullet beginning "**Admin views** — admin actions are low-frequency" no longer appears in the file
    - The three new traceability rows all show Phase column value "Phase 19"
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md defines ADMIN-01, ADMIN-02, SHOPMGMT-01 with Phase 19 traceability rows, and the contradictory deferral/out-of-scope admin-view notes are removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append seven RED Phase 19 integration test methods to MobileViewsTests.cs</name>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <read_first>
    - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs (read the full file — match the existing test structure: the `GetWithUserAgentAsync` helper, the `MobileUserAgent` constant, the `_factory`/`_client` fields, and the Phase 17/18 test methods at lines 620-760 which show the exact authenticated-request pattern using `request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization`)
    - EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs (signatures: `CreateAuthenticatedAdminClientAsync(factory, userName, email, ...)` returns `(client, user)` with Admin role; `CreateAuthenticatedClientWithUserAsync(factory, userName, email, ..., roles)` accepts `roles: new[] { "DungeonMaster" }`)
    - EuphoriaInn.IntegrationTests/Helpers/TestDataHelper.cs lines 85-114 (`CreateShopItemAsync(services, createdByDmId, name, price, quantity, rarity, type)` — seeds a Published ShopItemEntity and returns it with a valid Id)
  </read_first>
  <action>
    Append a new region of seven `[Fact]` test methods at the end of the MobileViewsTests class (after the existing QLOG-01 test, before the closing brace). Each method follows the established Phase 18 authenticated-request pattern: create an authenticated client, build an HttpRequestMessage with the MobileUserAgent header and the auth client's Authorization header, send via `_client.SendAsync(request, TestContext.Current.CancellationToken)`, read the html, then assert status 200 plus the per-view glass card class and per-view CSS filename.

    Method 1 — `GetMobilePage_AdminUsers_ReturnsSuccessAndMobileLayout` (ADMIN-01): authenticate with `CreateAuthenticatedAdminClientAsync(_factory, "admin_users19", "admin_users19@test.com")`; GET `/Admin/Users`; assert html contains "admin-users-card-mobile" and "admin-users.mobile.css".

    Method 2 — `GetMobilePage_AdminEditUser_ReturnsSuccessAndMobileLayout` (ADMIN-01): authenticate as Admin (`admin_edituser19`); create a target user via `AuthenticationHelper.CreateTestUserAsync(_factory.Services, "edit_target19", "edit_target19@test.com")`; GET `/Admin/EditUser?userId={targetUser.Id}` (the desktop link uses asp-route-userId); assert html contains "admin-form-card-mobile" and "admin-form.mobile.css".

    Method 3 — `GetMobilePage_AdminQuests_ReturnsSuccessAndMobileLayout` (ADMIN-01): authenticate as Admin (`admin_quests19`); seed a quest via `TestDataHelper.CreateTestQuestAsync(_factory.Services, adminUser.Id, "Admin Quest Mobile19")`; GET `/Admin/Quests`; assert html contains "admin-quests-card-mobile" and "admin-quests.mobile.css".

    Method 4 — `GetMobilePage_AdminResetPassword_ReturnsSuccessAndMobileLayout` (ADMIN-01): authenticate as Admin (`admin_resetpw19`); create a target user via `CreateTestUserAsync`; GET `/Admin/ResetPassword?userId={targetUser.Id}`; assert html contains "admin-form-card-mobile" and "admin-form.mobile.css".

    Method 5 — `GetMobilePage_ShopManagementIndex_ReturnsSuccessAndMobileLayout` (ADMIN-02): authenticate with `CreateAuthenticatedClientWithUserAsync(_factory, "dm_shopidx19", "dm_shopidx19@test.com", roles: new[] { "DungeonMaster" })`; GET `/ShopManagement`; assert html contains "shop-mgmt-index-card-mobile" and "shop-management-index.mobile.css".

    Method 6 — `GetMobilePage_ShopManagementCreate_ReturnsSuccessAndMobileLayout` (ADMIN-02): authenticate as DungeonMaster (`dm_shopcreate19`); GET `/ShopManagement/Create`; assert html contains "shop-mgmt-create-card-mobile" and "shop-management-create.mobile.css".

    Method 7 — `GetMobilePage_ShopManagementEdit_ReturnsSuccessAndMobileLayout` (ADMIN-02): authenticate as DungeonMaster (`dm_shopedit19`); seed an item via `TestDataHelper.CreateShopItemAsync(_factory.Services, dmUser.Id, "Edit Item Mobile19")`; GET `/ShopManagement/Edit/{item.Id}`; assert html contains "shop-mgmt-edit-card-mobile" and "shop-management-edit.mobile.css".

    Method 8 — `GetMobilePage_ShopDetails_ReturnsSuccessAndMobileLayout` (SHOPMGMT-01): authenticate any user via `CreateAuthenticatedClientWithUserAsync(_factory, "buyer_details19", "buyer_details19@test.com")`; seed a Published item via `TestDataHelper.CreateShopItemAsync(_factory.Services, sellerDmId, "Detail Item Mobile19")` where sellerDmId is a DM user created with `CreateTestUserAsync`; GET `/Shop/Details/{item.Id}`; assert html contains "shop-details-card-mobile" and "shop-details.mobile.css".

    Use FluentAssertions `html.Should().Contain(...)` and `response.StatusCode.Should().Be(HttpStatusCode.OK)` exactly as the existing tests do. These tests are expected to FAIL at the assertion step (RED) because the mobile views do not exist yet — that is correct for this scaffold plan. Add an xml-doc summary above each method noting the requirement ID and that it starts RED until its view lands.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.IntegrationTests --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.IntegrationTests --no-restore` reports Build succeeded with 0 errors
    - `grep -c "GetMobilePage_AdminUsers\|GetMobilePage_AdminEditUser\|GetMobilePage_AdminQuests\|GetMobilePage_AdminResetPassword\|GetMobilePage_ShopManagementIndex\|GetMobilePage_ShopManagementCreate\|GetMobilePage_ShopManagementEdit\|GetMobilePage_ShopDetails" EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` returns 8
    - Each new test asserts a `*.mobile.css` filename and a `*-card-mobile` class via `html.Should().Contain(...)`
    - Admin-route tests (Users, EditUser, Quests, ResetPassword) authenticate with an Admin-role client
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_AdminUsers" -q` runs the test (it fails at assertion, not compilation — RED is expected)
  </acceptance_criteria>
  <done>
    Eight new Phase 19 test methods compile cleanly and run RED (fail at assertion because views do not exist), establishing the GREEN targets for Plans 02-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → application | Integration tests issue real HTTP requests with a Test auth scheme header; no production trust boundary is changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | Test auth header encodes user id/roles | accept | Test-only `Test` authentication scheme already used across the suite; not present in production pipeline |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan or phase (RESEARCH.md confirms zero new packages); nothing to verify |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.IntegrationTests --no-restore -q` succeeds (0 errors)
- REQUIREMENTS.md contains ADMIN-01, ADMIN-02, SHOPMGMT-01 definitions and traceability rows
- The 8 new test methods compile and are discoverable by the test runner
</verification>

<success_criteria>
REQUIREMENTS.md defines all three phase requirements with traceability and no longer contradicts the active scope; the integration test project compiles with 8 new RED test stubs that establish automated GREEN targets for the implementation plans.
</success_criteria>

<output>
Create `.planning/phases/19-admin-shop-management-views/19-01-SUMMARY.md` when done
</output>
