---
phase: 19-admin-shop-management-views
plan: 04
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml
  - EuphoriaInn.Service/Views/ShopManagement/Edit.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/shop-management-create.mobile.css
  - EuphoriaInn.Service/wwwroot/css/shop-management-edit.mobile.css
autonomous: true
requirements: [ADMIN-02]
must_haves:
  truths:
    - "On a mobile user agent, /ShopManagement/Create renders a single-column glass-card form with all fields (Name, Type, Description, Rarity, Quantity, Price, ReferenceUrl, availability window)"
    - "The Price field is full-width with NO dice/calculator input-group buttons; the price-suggestion hint updates on rarity change"
    - "On a mobile user agent, /ShopManagement/Edit/{id} renders the same form structure plus the status info alert kept"
    - "The stripped updatePriceSuggestion() never references the removed randomPriceBtn/calculatedPriceBtn (no JS null error)"
  artifacts:
    - path: "EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml"
      provides: "Mobile single-column shop item create form"
      contains: "shop-mgmt-create-card-mobile"
    - path: "EuphoriaInn.Service/Views/ShopManagement/Edit.Mobile.cshtml"
      provides: "Mobile single-column shop item edit form with status alert"
      contains: "shop-mgmt-edit-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/shop-management-create.mobile.css"
      provides: "Create form glass card + availability window spacing"
      contains: "shop-mgmt-create-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/shop-management-edit.mobile.css"
      provides: "Edit form glass card + status alert spacing"
      contains: "shop-mgmt-edit-card-mobile"
  key_links:
    - from: "EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml"
      to: "price-suggestion hint"
      via: "updatePriceSuggestion() fires on #Rarity change"
      pattern: "updatePriceSuggestion"
    - from: "EuphoriaInn.Service/Views/ShopManagement/Edit.Mobile.cshtml"
      to: "EuphoriaInn.Service/wwwroot/css/shop-management-edit.mobile.css"
      via: "@section Styles link"
      pattern: "shop-management-edit\\.mobile\\.css"
---

<objective>
Create the mobile single-column form variants for ShopManagement/Create and ShopManagement/Edit, with two per-view CSS files. Both forms carry pricing-suggestion JavaScript and an availability-window toggle, but the dice (random price) and calculator (Tasha's recommended) input-group buttons are omitted on mobile — DMs type the price manually (D-12). Edit additionally keeps the status info alert (D-15).

Purpose: ADMIN-02 — Shop Management create and edit pages must be fully functional on mobile. Per D-11 through D-16.
Output: Two .Mobile.cshtml form views + two .mobile.css files; GetMobilePage_ShopManagementCreate and GetMobilePage_ShopManagementEdit integration tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-admin-shop-management-views/19-CONTEXT.md
@.planning/phases/19-admin-shop-management-views/19-PATTERNS.md
@.planning/phases/19-admin-shop-management-views/19-UI-SPEC.md
@EuphoriaInn.Service/Views/ShopManagement/Create.cshtml
@EuphoriaInn.Service/Views/ShopManagement/Edit.cshtml
@EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml
@EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04 portion)

New view files:
- `EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml`
- `EuphoriaInn.Service/Views/ShopManagement/Edit.Mobile.cshtml`

New CSS files:
- `EuphoriaInn.Service/wwwroot/css/shop-management-create.mobile.css`
- `EuphoriaInn.Service/wwwroot/css/shop-management-edit.mobile.css`

New CSS classes introduced: `shop-mgmt-create-card-mobile`, `shop-mgmt-edit-card-mobile`. JS functions reused (carried from desktop, stripped): `toggleAvailabilityWindow`, `updatePriceSuggestion`. JS data objects reused: `pricingGuide`, `calculatedPrices`. Removed on mobile (do NOT carry): `setRandomPrice`, `setCalculatedPrice`, and the `randomPriceBtn`/`calculatedPriceBtn` element references.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Create ShopManagement/Create.Mobile.cshtml + shop-management-create.mobile.css</name>
  <files>EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml, EuphoriaInn.Service/wwwroot/css/shop-management-create.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/Views/ShopManagement/Create.cshtml (desktop source — copy `@model CreateShopItemViewModel`, `@using` directives, every form field, the availability window section + toggleAvailabilityWindow(), the @section Scripts block including pricingGuide/calculatedPrices/updatePriceSuggestion() and the @@keyframes legendary-glow style. Note the price field is currently inside an input-group with random/calculator buttons that must be removed on mobile.)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 507-584 (exact Create excerpts, the stripped price field, the stripped updatePriceSuggestion, the @@keyframes verbatim note)
    - .planning/phases/19-admin-shop-management-views/19-RESEARCH.md "Common Pitfalls" Pitfall 1 + Pitfall 6 (button-reference null error; @@keyframes escaping)
    - EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css (canonical CSS template)
  </read_first>
  <action>
    Create Create.Mobile.cshtml declaring `@model CreateShopItemViewModel` with the desktop `@using` directives. Do NOT set `Layout`; do NOT add `@inject`. Add `@section Styles` linking `~/css/shop-management-create.mobile.css` with `asp-append-version="true"`.

    Wrap content in `<div class="shop-mgmt-create-card-mobile mb-3">` with a heading `<h5><i class="fas fa-plus-circle text-success me-2"></i>Add New Item</h5>`. Inside a `<form>` (copy the desktop form tag/action), render all fields full-width single-column (replace any `col-md-*` grid columns with single full-width stacked groups): Name (text input), Type (select), Description (textarea), Rarity (select with `id="Rarity"` so the JS can hook it), Quantity (number), Price, ReferenceUrl, and the availability window section.

    For the Price field, use a plain full-width input with NO input-group wrapper and NO dice/calculator buttons (D-12): a `form-label` "Price (Gold Pieces)", `<input asp-for="Price" type="number" class="form-control" min="0" step="0.01" />`, the validation span, and the price-suggestion hint `<div class="form-text" id="price-suggestion">Select a rarity to see pricing suggestions.</div>`.

    Copy the availability-window section verbatim from desktop (the checkbox + the AvailabilityFrom/AvailabilityUntil date inputs that toggleAvailabilityWindow() shows/hides). Keep the "Set Availability Window" checkbox label per UI-SPEC.

    Add `<hr>` then a button row `<div class="d-flex justify-content-between">`: a Cancel secondary anchor on the left (`btn btn-secondary`, links to `/ShopManagement`, fa-arrow-left) and a Create Item submit on the right (`btn btn-primary`, fa-save). Use copy "Create Item" per UI-SPEC. Omit any desktop Preview button.

    Add `@section Scripts` copying from desktop with these REQUIRED modifications (per D-13 — keep toggleAvailabilityWindow/updatePriceSuggestion and the rarity CSS animations, drop the button-enable/disable calls). The per-view CSS file is `shop-management-create.mobile.css` per D-14.
    - Keep the `pricingGuide` object, the `calculatedPrices` object, `toggleAvailabilityWindow()`, and `updatePriceSuggestion()`.
    - In `updatePriceSuggestion()`, REMOVE every line that references `randomPriceBtn`/`randomBtn`/`calculatedPriceBtn`/`calculatedBtn` (those buttons do not exist on mobile; calling `.disabled` on a null element throws). Keep only the `suggestionDiv.innerHTML = ...` and `suggestionDiv.className = ...` branches.
    - REMOVE the `setRandomPrice()` and `setCalculatedPrice()` functions entirely.
    - Keep the rarity-animation `<style>` block with `@@keyframes legendary-glow` — the `@@` escaping is already correct in the desktop source; copy it verbatim (do NOT change `@@` to `@`).
    - Keep the rarity-change event wiring that calls `updatePriceSuggestion()` on `#Rarity` change.

    Create shop-management-create.mobile.css: header comment `/* shop-management-create.mobile.css - ShopManagement/Create.Mobile.cshtml only */`, no-media-query note comment, `.shop-mgmt-create-card-mobile` glass card (16px padding), parchment text for `.form-label`/`h5`, faded text for `.form-text`/`small`, `.badge { text-shadow: none !important; }`, and an availability-window section spacing rule (e.g. top margin on the availability block). No media queries.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - Create.Mobile.cshtml contains `shop-mgmt-create-card-mobile` and links `~/css/shop-management-create.mobile.css`
    - Create.Mobile.cshtml contains `id="price-suggestion"`, an `id="Rarity"` select, and the `updatePriceSuggestion` function
    - Create.Mobile.cshtml does NOT contain `setRandomPrice`, `setCalculatedPrice`, `randomPriceBtn`, or `calculatedPriceBtn`
    - Create.Mobile.cshtml does NOT contain an `input-group` wrapper around the Price field
    - Create.Mobile.cshtml contains `@@keyframes` (escaped) inside a style block, not a bare unescaped `@keyframes`
    - Create.Mobile.cshtml does NOT contain `Layout =` and does NOT contain `@inject`
    - `grep -v '^/\*' shop-management-create.mobile.css | grep -c "@media"` returns 0
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_ShopManagementCreate" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    Create.Mobile.cshtml renders a single-column shop-item form with a button-free price field, a working rarity price-suggestion hint, and the availability toggle; no removed-button JS references remain; the ADMIN-02 Create integration test is GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ShopManagement/Edit.Mobile.cshtml + shop-management-edit.mobile.css</name>
  <files>EuphoriaInn.Service/Views/ShopManagement/Edit.Mobile.cshtml, EuphoriaInn.Service/wwwroot/css/shop-management-edit.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/Views/ShopManagement/Edit.cshtml (desktop source — copy `@model EditShopItemViewModel`, `@using` directives, every form field, the alert-info status info block, and the same pricing JS structure as Create)
    - EuphoriaInn.Service/Views/ShopManagement/Create.Mobile.cshtml (created in Task 1 — mirror its structure, JS stripping, and price-field treatment exactly)
    - .planning/phases/19-admin-shop-management-views/19-PATTERNS.md lines 588-604 (Edit-specific notes: same as Create plus the alert-info status block kept)
  </read_first>
  <action>
    Create Edit.Mobile.cshtml declaring `@model EditShopItemViewModel` with the desktop `@using` directives. Do NOT set `Layout`; do NOT add `@inject`. Add `@section Styles` linking `~/css/shop-management-edit.mobile.css` with `asp-append-version="true"`.

    Mirror the Create.Mobile.cshtml structure exactly (same single-column fields, same button-free price field with the price-suggestion hint, same availability window toggle, same submit/cancel button row but with the submit copy "Save Changes" per UI-SPEC), with these Edit-specific additions:
    - Wrap in `<div class="shop-mgmt-edit-card-mobile mb-3">` with a heading `<h5><i class="fas fa-edit text-warning me-2"></i>Edit Item</h5>`.
    - Keep the desktop `alert-info` status info block verbatim (D-15 — DMs need to know if the item is awaiting approval). Place it near the top of the card.
    - Use the form action that posts to the Edit endpoint as in the desktop view (carry any hidden Id field).

    Copy the same `@section Scripts` as Create.Mobile.cshtml with the identical stripping rules (keep updatePriceSuggestion without button refs, keep toggleAvailabilityWindow, keep @@keyframes verbatim, remove setRandomPrice/setCalculatedPrice).

    Create shop-management-edit.mobile.css: header comment `/* shop-management-edit.mobile.css - ShopManagement/Edit.Mobile.cshtml only */`, no-media-query note comment, `.shop-mgmt-edit-card-mobile` glass card (16px padding), the parchment/faded/badge rules, plus a status-alert spacing rule (e.g. margin on `.alert-info`). No media queries.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q 2>&1 | grep -iE "error|Build succeeded" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.Service --no-restore` reports Build succeeded with 0 errors
    - Edit.Mobile.cshtml contains `shop-mgmt-edit-card-mobile` and links `~/css/shop-management-edit.mobile.css`
    - Edit.Mobile.cshtml contains an `alert-info` block (kept per D-15)
    - Edit.Mobile.cshtml contains `updatePriceSuggestion` and `id="price-suggestion"` and does NOT contain `setRandomPrice`/`setCalculatedPrice`/`randomPriceBtn`/`calculatedPriceBtn`
    - Edit.Mobile.cshtml does NOT contain an `input-group` wrapper around the Price field
    - Edit.Mobile.cshtml does NOT contain `Layout =` and does NOT contain `@inject`
    - `grep -v '^/\*' shop-management-edit.mobile.css | grep -c "@media"` returns 0
    - `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_ShopManagementEdit" -q` passes (GREEN)
  </acceptance_criteria>
  <done>
    Edit.Mobile.cshtml renders the same single-column form as Create with the status info alert kept and the button-free price field; the ADMIN-02 Edit integration test is GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → ShopManagementController | DM/Admin form POSTs (Create, Edit) cross to the server; authorization enforced server-side via DungeonMasterOnly policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | Create/Edit POST forms | mitigate | Forms carry the antiforgery token (tag-helper form or @Html.AntiForgeryToken as on desktop); controller actions retain `[ValidateAntiForgeryToken]`; server-side validation unchanged |
| T-19-09 | Input Validation | price / quantity inputs | mitigate | No client-side-only enforcement; `CreateShopItemViewModel`/`EditShopItemViewModel` server validation runs unchanged; mobile only changes layout |
| T-19-10 | Tampering (XSS) | item description rendering on Edit | mitigate | Razor auto-encodes model values bound to inputs via `asp-for`; no Html.Raw |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md confirms zero new packages) |
</threat_model>

<verification>
- `dotnet build EuphoriaInn.Service --no-restore -q` succeeds
- `dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~GetMobilePage_ShopManagementCreate|FullyQualifiedName~GetMobilePage_ShopManagementEdit" -q` GREEN
- No `randomPriceBtn`/`calculatedPriceBtn` references remain in either mobile view (prevents the documented JS null error)
</verification>

<success_criteria>
Both ShopManagement form views render single-column on mobile with button-free price entry, working price-suggestion hints, and no removed-button JS references; Edit keeps the status info alert; ADMIN-02 Create and Edit integration tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/19-admin-shop-management-views/19-04-SUMMARY.md` when done
</output>
