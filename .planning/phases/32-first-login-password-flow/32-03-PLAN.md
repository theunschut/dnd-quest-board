---
phase: 32-first-login-password-flow
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - QuestBoard.Service/Controllers/Admin/AccountController.cs
  - QuestBoard.Service/ViewModels/AccountViewModels/ForgotPasswordViewModel.cs
  - QuestBoard.Service/ViewModels/AccountViewModels/SetPasswordViewModel.cs
  - QuestBoard.Service/Views/Account/ForgotPassword.cshtml
  - QuestBoard.Service/Views/Account/ForgotPassword.Mobile.cshtml
  - QuestBoard.Service/Views/Account/SetPassword.cshtml
  - QuestBoard.Service/Views/Account/SetPassword.Mobile.cshtml
  - QuestBoard.Service/Views/Account/Login.cshtml
  - QuestBoard.Service/Views/Account/Login.Mobile.cshtml
  - QuestBoard.Domain/Interfaces/IIdentityService.cs
  - QuestBoard.Repository/IdentityService.cs
autonomous: true
requirements: [PWFLOW-02, PWFLOW-03, PWFLOW-04]
must_haves:
  truths:
    - "A user can visit /Account/ForgotPassword, submit an email, and always see the same generic 'if that email is registered' message"
    - "A user clicking a reset/welcome link lands on /Account/SetPassword with a form, and submitting a valid password sets it and confirms their email, then redirects to Login"
    - "The Login page shows a 'Forgot password?' link to /Account/ForgotPassword"
    - "The retired confirm-email-only flow leaves no dead code: AccountController.ConfirmEmail and the orphaned IIdentityService confirm-email methods are deleted"
  artifacts:
    - path: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      provides: "ForgotPassword GET/POST and SetPassword GET/POST anonymous actions"
      contains: "SetPassword"
    - path: "QuestBoard.Service/ViewModels/AccountViewModels/ForgotPasswordViewModel.cs"
      provides: "Email-only forgot-password form model"
      contains: "EmailAddress"
    - path: "QuestBoard.Service/ViewModels/AccountViewModels/SetPasswordViewModel.cs"
      provides: "UserId/Token/NewPassword/ConfirmPassword model with MinimumLength 8"
      contains: "Compare"
    - path: "QuestBoard.Service/Views/Account/ForgotPassword.cshtml"
      provides: "Anonymous single-email-field card view"
      contains: "asp-for=\"Email\""
    - path: "QuestBoard.Service/Views/Account/SetPassword.cshtml"
      provides: "Anonymous password-set form with hidden UserId/Token"
      contains: "asp-for=\"Token\""
    - path: "QuestBoard.Domain/Interfaces/IIdentityService.cs"
      provides: "IIdentityService with the orphaned GenerateEmailConfirmationAsync + ConfirmEmailAsync removed"
      absent: "GenerateEmailConfirmationAsync"
  key_links:
    - from: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      to: "ForgotPasswordEmailJob"
      via: "jobClient.Enqueue"
      pattern: "Enqueue<ForgotPasswordEmailJob>"
    - from: "QuestBoard.Service/Controllers/Admin/AccountController.cs"
      to: "IIdentityService.GeneratePasswordResetTokenForUserAsync"
      via: "token issuance"
      pattern: "GeneratePasswordResetTokenForUserAsync"
    - from: "QuestBoard.Service/Views/Account/Login.cshtml"
      to: "ForgotPassword action"
      via: "asp-action link"
      pattern: "asp-action=\"ForgotPassword\""
---

<objective>
Add the self-service Forgot Password flow and the shared SetPassword landing page to `AccountController`, with their ViewModels and desktop/mobile views, add the "Forgot password?" link to the Login pages, and finish retiring the confirm-email-only flow by deleting its now-orphaned code. `ForgotPassword` POST is enumeration-safe (D-11) and rate-limited (D-12, policy from Plan 02). `SetPassword` POST resets the password (reusing existing `ResetPasswordAsync`) and, on success, marks `EmailConfirmed = true` via `ConfirmEmailDirectlyAsync` (Plan 01), then redirects to Login (D-09). Views use the standard `_Layout.cshtml` automatically via `_ViewStart.cshtml` — NO layout override (resolved layout question: reuse `_Layout.cshtml` as Login does today; do NOT build a new auth layout; do NOT reference `_Layout.GroupPicker.cshtml`). Finally, since this plan owns `AccountController.cs`, it deletes the orphaned `AccountController.ConfirmEmail` action plus the two orphaned `IIdentityService`/`IdentityService` confirm-email members (D-05).

Purpose: This is the user-facing half of the password flow — the endpoints a real user hits — plus the dead-code cleanup that completes the D-05 retirement. Depends on Plan 01 (new service methods) and Plan 02 (ForgotPasswordEmailJob type + rate-limit policy name), so it runs in Wave 2.
Output: `AccountController` gains four actions and loses the ConfirmEmail action; two ViewModels; four views; two Login link edits; two IIdentityService/IdentityService confirm-email members deleted. `dotnet build` and targeted integration tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-first-login-password-flow/32-CONTEXT.md
@.planning/phases/32-first-login-password-flow/32-RESEARCH.md
@.planning/phases/32-first-login-password-flow/32-PATTERNS.md
@.planning/phases/32-first-login-password-flow/32-01-SUMMARY.md
@.planning/phases/32-first-login-password-flow/32-02-SUMMARY.md

<interfaces>
<!-- Methods and types this plan consumes. Confirm exact signatures against the two SUMMARYs above. -->

From Plan 01 (IIdentityService / IUserService):
  Task<string?> GeneratePasswordResetTokenForUserAsync(int userId)   // raw ResetPassword-purpose token, or null
  Task<IdentityResult> ConfirmEmailDirectlyAsync(int userId)          // sets EmailConfirmed = true, no token
  Task<int?> GetIdByEmailAsync(string email)                          // existing, reuse
  Task<IdentityResult> ResetPasswordAsync(int userId, string token, string newPassword)  // existing IIdentityService
  IUserService.ResetPasswordAsync(User user, string token, string newPassword)           // existing pass-through

From Plan 02 (Jobs):
  ForgotPasswordEmailJob.ExecuteAsync(string toEmail, string callbackUrl, CancellationToken)   // NO userName

AccountController existing analogs (QuestBoard.Service/Controllers/Admin/AccountController.cs):
  ConfirmEmail GET (lines 23-53): token decode via Encoding.UTF8.GetString(WebEncoders.Base64UrlDecode(token)); try/catch; TempData; RedirectToAction(nameof(Login)). NOTE: use this as the decode analog while writing SetPassword, THEN delete it in Task 4.
  ChangePassword GET/POST (lines 184-213): [HttpGet] returns View(); [HttpPost][ValidateAntiForgeryToken] validates ModelState, on success TempData + redirect, else foreach error -> ModelState.AddModelError
  Imports already present: WebEncoders, System.Text (Encoding), Hangfire (jobClient), Microsoft.AspNetCore.Mvc, Microsoft.Extensions.Logging, QuestBoard.Service.Jobs, QuestBoard.Service.ViewModels.AccountViewModels
  Add: using Microsoft.AspNetCore.RateLimiting;  (for [EnableRateLimiting])

Dead-code members to delete in Task 4 (verified callers via grep):
  AccountController.ConfirmEmail(int userId, string token)  // AccountController.cs GET lines 23-53 — the ONLY caller of IIdentityService.ConfirmEmailAsync
  IIdentityService.GenerateEmailConfirmationAsync(int userId)   // IIdentityService.cs:24 — callers are ONLY AdminController lines 122 & 277 (both removed by Plan 04 Task 2, same wave)
  IIdentityService.ConfirmEmailAsync(int userId, string token)  // IIdentityService.cs:25 — caller is ONLY AccountController.ConfirmEmail (deleted in this same task)
  IdentityService.GenerateEmailConfirmationAsync                // QuestBoard.Repository/IdentityService.cs:118-123 implementation
  IdentityService.ConfirmEmailAsync                             // QuestBoard.Repository/IdentityService.cs:125-131 implementation
  NOTE: these two methods have NO IUserService/UserService wrappers (grep confirms) — do not look for any.
  DO NOT TOUCH: AccountController.ConfirmEmailChange, IIdentityService.GenerateChangeEmailTokenAsync, IIdentityService.ChangeEmailAsync (IIdentityService.cs:26-27), IdentityService.GenerateChangeEmailTokenAsync (line 133+) — these are the SEPARATE, still-active email-address-change flow (D-05).

Token encode idiom: WebEncoders.Base64UrlEncode(Encoding.UTF8.GetBytes(rawToken)); Url.Action(nameof(SetPassword), "Account", new { userId, token = encoded }, Request.Scheme)
Token decode idiom: Encoding.UTF8.GetString(WebEncoders.Base64UrlDecode(model.Token))

SetPasswordViewModel target shape (analog: AdminViewModels/ResetPasswordViewModel.cs — but MinimumLength = 8, NOT 6, per RESEARCH.md Pitfall 5):
  int UserId; string Token; [Required][StringLength(100, MinimumLength = 8)][DataType(Password)] NewPassword; [Compare("NewPassword")] ConfirmPassword
ForgotPasswordViewModel target shape (analog: LoginViewModel.cs Email field): [Required][EmailAddress] string Email
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ForgotPasswordViewModel + SetPasswordViewModel</name>
  <read_first>
    - QuestBoard.Service/ViewModels/AccountViewModels/LoginViewModel.cs (Email field analog)
    - QuestBoard.Service/ViewModels/AdminViewModels/ResetPasswordViewModel.cs (UserId/password/Compare analog — but it uses MinimumLength = 6, which is the bug NOT to copy)
    - .planning/phases/32-first-login-password-flow/32-PATTERNS.md (ForgotPasswordViewModel + SetPasswordViewModel sections)
    - .planning/phases/32-first-login-password-flow/32-RESEARCH.md (Pitfall 5 — MinimumLength must be 8 to match Program.cs Password.RequiredLength = 8)
  </read_first>
  <action>
    Create QuestBoard.Service/ViewModels/AccountViewModels/ForgotPasswordViewModel.cs with a single [Required][EmailAddress] string Email property (namespace QuestBoard.Service.ViewModels.AccountViewModels), copying the field style from LoginViewModel.

    Create QuestBoard.Service/ViewModels/AccountViewModels/SetPasswordViewModel.cs with: int UserId; string Token = string.Empty; a [Required][StringLength(100, MinimumLength = 8)][DataType(DataType.Password)][Display(Name = "New Password")] string NewPassword; and a [DataType(DataType.Password)][Display(Name = "Confirm New Password")][Compare("NewPassword", ...)] string ConfirmPassword. Use MinimumLength = 8 (NOT 6) so DataAnnotation validation matches the actual Identity policy (Program.cs Password.RequiredLength = 8). UserId and Token carry the reset context through the GET->POST round trip as hidden form fields.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - ForgotPasswordViewModel.cs exists with exactly one property Email decorated [Required] and [EmailAddress]
    - SetPasswordViewModel.cs exists with UserId (int), Token (string), NewPassword (string), ConfirmPassword (string)
    - SetPasswordViewModel.NewPassword uses MinimumLength = 8 (grep for "MinimumLength = 8" in the file returns a match; grep for "MinimumLength = 6" returns none)
    - SetPasswordViewModel.ConfirmPassword has a [Compare("NewPassword"...)] attribute
    - dotnet build QuestBoard.Service succeeds
  </acceptance_criteria>
  <done>Both ViewModels compile; password length constraint matches Identity policy.</done>
</task>

<task type="auto">
  <name>Task 2: Add ForgotPassword + SetPassword actions to AccountController</name>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (ConfirmEmail GET lines 23-53 for decode+try/catch+TempData+redirect; ChangePassword GET/POST lines 184-213 for the GET-renders-form / POST-validates shape; existing imports lines 1-10)
    - .planning/phases/32-first-login-password-flow/32-PATTERNS.md (AccountController ForgotPassword + SetPassword sections — full patterns; the enumeration-safe ForgotPassword POST body; the GetIdByEmailAsync reuse note)
    - .planning/phases/32-first-login-password-flow/32-01-SUMMARY.md (confirm the exact new method names: GeneratePasswordResetTokenForUserAsync, ConfirmEmailDirectlyAsync)
    - .planning/phases/32-first-login-password-flow/32-02-SUMMARY.md (confirm ForgotPasswordEmailJob.ExecuteAsync signature and the rate-limit policy name)
  </read_first>
  <action>
    Add four anonymous actions to AccountController (no [Authorize] — these are pre-login callbacks). Add using Microsoft.AspNetCore.RateLimiting;.

    ForgotPassword GET: [HttpGet] returns View() (renders the email-entry form).

    ForgotPassword POST: [HttpPost][ValidateAntiForgeryToken][EnableRateLimiting("forgot-password")] (policy name must match Plan 02's registered policy exactly). If ModelState.IsValid: call identityService.GetIdByEmailAsync(model.Email); if it HasValue, call identityService.GeneratePasswordResetTokenForUserAsync(userId), Base64Url-encode the raw token, build the callback URL via Url.Action(nameof(SetPassword), "Account", new { userId, token = encoded }, Request.Scheme), and jobClient.Enqueue<ForgotPasswordEmailJob>(j => j.ExecuteAsync(model.Email, callbackUrl, CancellationToken.None)). Then — REGARDLESS of whether the user existed (no branch on the response) — set TempData["Success"] = "If that email is registered, a reset link has been sent." and RedirectToAction(nameof(ForgotPassword)). This is the enumeration-safe path (D-11): identical response whether or not the email matched; no email sent for non-existent accounts. If ModelState invalid, return View(model).

    SetPassword GET: [HttpGet] accept userId (int) and token (string) from the query string; construct and return View(new SetPasswordViewModel { UserId = userId, Token = token }) so the hidden fields round-trip. Follow the ChangePassword GET shape but pass the model through (do not require [Authorize]).

    SetPassword POST: [HttpPost][ValidateAntiForgeryToken]. If ModelState.IsValid: decode model.Token via Encoding.UTF8.GetString(WebEncoders.Base64UrlDecode(model.Token)); call userService.ResetPasswordAsync(user...) OR identityService.ResetPasswordAsync(model.UserId, decodedToken, model.NewPassword) (use the userId overload that already exists — confirm in 32-01-SUMMARY). On result.Succeeded: call identityService.ConfirmEmailDirectlyAsync(model.UserId) to mark EmailConfirmed = true (D-09), set TempData["Success"] to a "password set, please log in" message, and RedirectToAction(nameof(Login)). On failure: foreach error in result.Errors add to ModelState and return View(model). Wrap token decode in try/catch mirroring ConfirmEmail, redirecting to Login with a generic TempData["Error"] on exception (expired/tampered token).

    Do NOT call GenerateEmailConfirmationAsync or ConfirmEmailAsync anywhere in these actions — the token is a password-reset-purpose token and must be consumed by ResetPasswordAsync only (RESEARCH.md Pitfall 2 / Pitfall 4). Leave the existing ConfirmEmail GET action in place for now — it is deleted in Task 4 after SetPassword is written (so its decode idiom stays available as a live reference while writing this task).
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - AccountController has ForgotPassword GET, ForgotPassword POST, SetPassword GET, SetPassword POST actions
    - ForgotPassword POST carries [EnableRateLimiting("forgot-password")] and [ValidateAntiForgeryToken]
    - ForgotPassword POST sets the identical TempData message and redirect on BOTH the found and not-found paths (grep confirms the "If that email is registered" string appears exactly once and is outside any userId.HasValue branch)
    - ForgotPassword POST calls Enqueue<ForgotPasswordEmailJob> only inside the userId.HasValue branch
    - SetPassword POST calls ResetPasswordAsync then, on success, ConfirmEmailDirectlyAsync, then RedirectToAction(nameof(Login))
    - Neither new action references GenerateEmailConfirmationAsync or ConfirmEmailAsync (grep returns zero matches within the new actions)
    - dotnet build QuestBoard.Service succeeds
  </acceptance_criteria>
  <done>Both flows compile; enumeration-safety and password-reset-token purity are structurally enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Create ForgotPassword + SetPassword views (desktop + mobile); add Forgot-password link to Login</name>
  <read_first>
    - QuestBoard.Service/Views/Account/Login.cshtml (anonymous card shell; TempData Success/Error alert blocks lines 18-34; RememberMe/submit region lines 51-61)
    - QuestBoard.Service/Views/Account/Login.Mobile.cshtml (mobile account-card-mobile shell; insertion point lines 27-36)
    - QuestBoard.Service/Views/Account/ChangePassword.cshtml (password-field + Compare form shape for SetPassword)
    - QuestBoard.Service/Views/_ViewStart.cshtml (confirms _Layout is applied globally — NO Layout override needed in new views)
    - .planning/phases/32-first-login-password-flow/32-PATTERNS.md (ForgotPassword.cshtml + SetPassword.cshtml sections with full desktop/mobile structures and the verbatim TempData alert blocks; Login link insertion section)
    - CLAUDE.md (UI/UX guidelines: modern-card / modern-card-header / modern-card-body, <hr> before buttons, filled colored buttons with FontAwesome icons)
  </read_first>
  <action>
    Create QuestBoard.Service/Views/Account/ForgotPassword.cshtml (@model ForgotPasswordViewModel): a centered modern-card with header icon + title "Forgot Password", the verbatim TempData Success/Error dismissible alert blocks copied from Login.cshtml (this is where the D-11 generic message renders after the POST redirect), a form posting to asp-action="ForgotPassword" with a single Email field (label/input/validation span) and a d-grid submit button "Send reset link". Do NOT add a Layout = line. Create ForgotPassword.Mobile.cshtml using the account-card-mobile wrapper (mirror Login.Mobile.cshtml) with the same single Email field and submit button.

    Create QuestBoard.Service/Views/Account/SetPassword.cshtml (@model SetPasswordViewModel): a centered modern-card titled "Set Your Password", a form posting to asp-action="SetPassword" containing hidden inputs for UserId and Token (asp-for), NewPassword and ConfirmPassword fields (label/input/validation span each), an <hr> before the button region per CLAUDE.md, and a single d-grid primary submit button "Set Password" (no fabricated Cancel link — this is an anonymous single-purpose landing page, so use Login.cshtml's single-button pattern rather than ChangePassword's two-button justify-between). Include an asp-validation-summary="All" block for the IdentityResult errors surfaced by the POST. Create SetPassword.Mobile.cshtml with the same fields in the account-card-mobile / admin-form-card-mobile wrapper + @Html.AntiForgeryToken() + d-grid gap-2 idiom.

    Edit QuestBoard.Service/Views/Account/Login.cshtml: insert a "Forgot password?" link (<a asp-action="ForgotPassword">Forgot password?</a>) between the RememberMe checkbox div and the submit-button div, styled to match existing Bootstrap link conventions. Edit Login.Mobile.cshtml: insert the same link at the mirrored insertion point.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet build QuestBoard.Service</automated>
  </verify>
  <acceptance_criteria>
    - ForgotPassword.cshtml and ForgotPassword.Mobile.cshtml exist, bind @model ForgotPasswordViewModel, post to asp-action="ForgotPassword", and contain the TempData Success alert block
    - SetPassword.cshtml and SetPassword.Mobile.cshtml exist, bind @model SetPasswordViewModel, contain hidden asp-for="UserId" and asp-for="Token" inputs, and NewPassword + ConfirmPassword fields
    - None of the four new views contain a Layout = "..." statement
    - Login.cshtml and Login.Mobile.cshtml each contain an anchor with asp-action="ForgotPassword"
    - dotnet build QuestBoard.Service succeeds
  </acceptance_criteria>
  <done>Anonymous forgot/set-password views render under the standard layout; Login links to the flow on both desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 4: Delete the orphaned confirm-email-only dead code</name>
  <read_first>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs (ConfirmEmail GET at lines 23-53 — DELETE this action; ConfirmEmailChange further down is the SEPARATE email-change flow — KEEP it)
    - QuestBoard.Domain/Interfaces/IIdentityService.cs (lines 24-25 — the two declarations to delete; lines 26-27 GenerateChangeEmailTokenAsync/ChangeEmailAsync are the email-change flow — KEEP)
    - QuestBoard.Repository/IdentityService.cs (GenerateEmailConfirmationAsync lines 118-123 and ConfirmEmailAsync lines 125-131 — the two implementations to delete; GenerateChangeEmailTokenAsync starting line 133 — KEEP)
    - .planning/phases/32-first-login-password-flow/32-CONTEXT.md (D-05 — the confirm-email-only flow is retired; the email-address-change flow is explicitly NOT part of the retirement and must stay)
    - .planning/phases/32-first-login-password-flow/32-04-SUMMARY.md (confirm Plan 04 removed AdminController's GenerateEmailConfirmationAsync call sites before this method is deleted)
  </read_first>
  <action>
    Run AFTER Tasks 2-3 of this plan (SetPassword must already exist so the ConfirmEmail decode idiom is no longer needed as a live reference). This task completes the D-05 retirement by deleting the orphaned code.

    SAFETY GATES — prove no live callers remain before deleting each member (Plan 04 runs in the same wave and owns AdminController's two GenerateEmailConfirmationAsync call sites; do not delete a method that still has a caller):
      - grep the whole solution for "identityService.ConfirmEmailAsync(" — the only match should be inside AccountController.ConfirmEmail's own body (which is being deleted here). Proceed to delete AccountController.ConfirmEmail and IIdentityService/IdentityService.ConfirmEmailAsync.
      - grep the whole solution for "identityService.GenerateEmailConfirmationAsync(" (call sites, not the definition). If ZERO call sites remain (Plan 04 already retargeted AdminController), delete IIdentityService/IdentityService.GenerateEmailConfirmationAsync too. If any call site STILL remains (Plan 04 has not run yet in this wave), DO NOT delete GenerateEmailConfirmationAsync — leave that one member in place and record in the SUMMARY that Plan 04 (or verify-work) must remove it once AdminController is retargeted. Deleting AccountController.ConfirmEmail + ConfirmEmailAsync is always safe regardless of Plan 04 ordering (their only caller is this file).

    Deletions:
      1. QuestBoard.Service/Controllers/Admin/AccountController.cs — delete the entire ConfirmEmail(int userId, string token) GET action (lines 23-53, including its [HttpGet] attribute). Leave ConfirmEmailChange and every other action intact.
      2. QuestBoard.Domain/Interfaces/IIdentityService.cs — delete the ConfirmEmailAsync(int userId, string token) declaration (line 25) and, IF the GenerateEmailConfirmationAsync gate above passed, the GenerateEmailConfirmationAsync(int userId) declaration (line 24). Do NOT touch GenerateChangeEmailTokenAsync / ChangeEmailAsync.
      3. QuestBoard.Repository/IdentityService.cs — delete the ConfirmEmailAsync implementation (lines 125-131) and, IF the gate passed, the GenerateEmailConfirmationAsync implementation (lines 118-123). Leave GenerateChangeEmailTokenAsync (line 133+) and everything else intact.

    If removing ConfirmEmail leaves an unused `using` in AccountController.cs, the build warns but does not fail — leave usings unless the compiler errors.
  </action>
  <verify>
    <automated>cd /c/Repos/quest-board && dotnet build QuestBoard.Service && dotnet test QuestBoard.UnitTests</automated>
  </verify>
  <acceptance_criteria>
    - QuestBoard.Service/Controllers/Admin/AccountController.cs no longer contains a ConfirmEmail(int userId, string token) action (grep for "IActionResult> ConfirmEmail(" returns zero matches; ConfirmEmailChange is still present)
    - QuestBoard.Domain/Interfaces/IIdentityService.cs no longer declares ConfirmEmailAsync (grep returns zero matches), and STILL declares GenerateChangeEmailTokenAsync + ChangeEmailAsync
    - QuestBoard.Repository/IdentityService.cs no longer implements ConfirmEmailAsync (grep returns zero matches), and STILL implements GenerateChangeEmailTokenAsync
    - If Plan 04 already ran (no GenerateEmailConfirmationAsync call sites): solution-wide grep for "GenerateEmailConfirmationAsync" returns zero matches. If Plan 04 has NOT run: the method definition may remain and the SUMMARY records the follow-up — this is acceptable per the safety gate
    - Solution-wide grep for "identityService.ConfirmEmailAsync(" returns zero matches
    - dotnet build QuestBoard.Service succeeds and dotnet test QuestBoard.UnitTests is green
  </acceptance_criteria>
  <done>The retired confirm-email-only flow leaves no dead code: the orphaned controller action and the ConfirmEmailAsync member are removed (and GenerateEmailConfirmationAsync too, once Plan 04's callers are gone); the separate email-address-change flow is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → ForgotPassword POST | Untrusted email input; rate-limited; must not leak account existence |
| anonymous client → SetPassword POST | Untrusted password + token from email link; token verified by Identity |
| email link → SetPassword GET | userId + encoded token arrive as query params from an emailed URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Information Disclosure | ForgotPassword POST response | mitigate | Identical TempData message + redirect on found and not-found paths (D-11); acceptance criterion enforces single occurrence outside the HasValue branch (email enumeration prevented) |
| T-32-09 | Spoofing / Tampering | SetPassword token | mitigate | Token consumed only via ResetPasswordAsync (ResetPassword purpose); security-stamp binding invalidates the token if the password/email already changed; try/catch on decode returns a generic error for tampered/expired tokens |
| T-32-10 | Elevation of Privilege | ConfirmEmailDirectlyAsync side effect | mitigate | Called only after ResetPasswordAsync returns Succeeded — a valid reset token is a prerequisite, so email-confirmation cannot be forced without proving control of the reset link |
| T-32-11 | Denial of Service | ForgotPassword endpoint | mitigate | [EnableRateLimiting("forgot-password")] applies the 3 req / 15 min per-IP policy from Plan 02 |
| T-32-12 | Input Validation | SetPassword / ForgotPassword models | mitigate | DataAnnotations ([Required], [EmailAddress], [DataType(Password)], [Compare], MinimumLength = 8 matching Identity policy) validated server-side; IdentityResult.Errors surfaced to ModelState |
| T-32-17 | Attack Surface | Removal of the anonymous ConfirmEmail GET endpoint | mitigate | Deleting the orphaned confirm-email GET action removes an unauthenticated, now-unreachable token-consuming endpoint — net reduction in attack surface, consistent with D-05 retirement |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this plan |
</threat_model>

<verification>
- dotnet build QuestBoard.Service succeeds with Plan 01 + Plan 02 merged.
- dotnet test QuestBoard.IntegrationTests --filter FullyQualifiedName~AccountControllerIntegrationTests still green (full new-behavior tests land in Plan 05).
- grep AccountController.cs: "If that email is registered" appears exactly once; no GenerateEmailConfirmationAsync/ConfirmEmailAsync in the new actions; no ConfirmEmail GET action remains.
- grep IIdentityService.cs / IdentityService.cs: ConfirmEmailAsync removed; email-address-change flow (ChangeEmail*) intact.
</verification>

<success_criteria>
- ForgotPassword GET/POST and SetPassword GET/POST exist, anonymous, with the correct token purpose and enumeration-safe response.
- SetPassword success sets password + EmailConfirmed and redirects to Login.
- ForgotPassword POST is rate-limited via the Plan 02 policy.
- Four new views under the standard layout; Login links to ForgotPassword on desktop and mobile.
- The retired confirm-email-only flow leaves no dead code (ConfirmEmail action + ConfirmEmailAsync deleted; GenerateEmailConfirmationAsync deleted once Plan 04's callers are gone); the email-address-change flow is untouched.
</success_criteria>

<output>
Create `.planning/phases/32-first-login-password-flow/32-03-SUMMARY.md` when done, noting the exact SetPassword route/query-param shape (userId + token) so Plan 04's WelcomeEmailJob callback URL matches, confirming the enumeration-safe message text, and recording which dead-code members were deleted in Task 4 (and whether GenerateEmailConfirmationAsync had to be deferred pending Plan 04).
</output>
