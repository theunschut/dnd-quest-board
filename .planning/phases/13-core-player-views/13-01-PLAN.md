---
phase: 13-core-player-views
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - QVIEW-01
  - QVIEW-02
  - QVIEW-03

must_haves:
  truths:
    - "MobileViewsTests.cs exists in EuphoriaInn.IntegrationTests/Mobile/ with test stubs for all seven requirements"
    - "dotnet test --filter MobileViewsTests runs without build errors (tests may be red — stubs are expected to fail until implementation)"
    - "All seven requirement IDs (HOME-01 through QVIEW-03) are covered by named test methods in the file"
  artifacts:
    - path: "EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs"
      provides: "Integration test stubs for all Phase 13 requirements"
      exports: ["MobileViewsTests"]
  key_links:
    - from: "MobileViewsTests.cs"
      to: "WebApplicationFactoryBase"
      via: "IClassFixture<WebApplicationFactoryBase>"
      pattern: "IClassFixture<WebApplicationFactoryBase>"
    - from: "MobileViewsTests.cs"
      to: "AuthenticationHelper/TestDataHelper"
      via: "static helper calls for seeding and authenticated clients"
      pattern: "AuthenticationHelper\\.|TestDataHelper\\."
---

<objective>
Create `MobileViewsTests.cs` — integration test stubs for all seven Phase 13 requirements (HOME-01 through QVIEW-03). Tests start red (implementation views do not exist yet) and go green as Wave 1 plans land. This establishes the Nyquist sampling harness before any implementation.

Purpose: Wave 0 must create the test file so Wave 1 executors can run tests after each task. Without this file, Wave 1 has no automated verify gate.
Output: `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` — 10+ test methods covering all seven requirements.
</objective>

<execution_context>
@C:\Users\t.schut\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\t.schut\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-core-player-views/13-CONTEXT.md
@.planning/phases/13-core-player-views/13-VALIDATION.md
</context>

<interfaces>
<!-- Exact analog to replicate — read this file before writing MobileViewsTests.cs -->

From EuphoriaInn.IntegrationTests/Mobile/MobileLayoutTests.cs:
```csharp
using System.Net;

namespace EuphoriaInn.IntegrationTests.Mobile;

public class MobileLayoutTests : IClassFixture<WebApplicationFactoryBase>
{
    private const string MobileUserAgent =
        "Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.0 Mobile/15E148 Safari/604.1";
    private const string DesktopUserAgent =
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36";

    private readonly HttpClient _client;

    public MobileLayoutTests(WebApplicationFactoryBase factory)
    {
        _client = factory.CreateClient();
    }

    private async Task<(HttpResponseMessage Response, string Html)> GetWithUserAgentAsync(string userAgent)
    {
        var request = new HttpRequestMessage(HttpMethod.Get, "/");
        request.Headers.TryAddWithoutValidation("User-Agent", userAgent);
        var response = await _client.SendAsync(request, TestContext.Current.CancellationToken);
        var html = await response.Content.ReadAsStringAsync(TestContext.Current.CancellationToken);
        return (response, html);
    }
}
```

From EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs (authenticated client pattern):
```csharp
var (authClient, playerUser) = await AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(
    _factory, "player", "player@test.com");
// Combine auth header + UA header for mobile authenticated requests:
var request = new HttpRequestMessage(HttpMethod.Get, "/");
request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent);
request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization;
var response = await _client.SendAsync(request, TestContext.Current.CancellationToken);
```

From EuphoriaInn.IntegrationTests/Helpers/TestDataHelper.cs (data seeding):
```csharp
var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm", "dm@test.com");
var quest = await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Test Quest");
var player = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "player", "player@test.com");
await TestDataHelper.CreatePlayerSignupAsync(_factory.Services, quest.Id, player.Id, isSelected: true);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create MobileViewsTests.cs with stubs for HOME-01 through QVIEW-03</name>
  <read_first>
    - EuphoriaInn.IntegrationTests/Mobile/MobileLayoutTests.cs (exact analog — copy class structure verbatim)
    - EuphoriaInn.IntegrationTests/WebApplicationFactoryBase.cs (factory pattern)
    - EuphoriaInn.IntegrationTests/Helpers/AuthenticationHelper.cs (auth + UA combined pattern)
    - EuphoriaInn.IntegrationTests/Helpers/TestDataHelper.cs (quest + signup seeding)
    - .planning/phases/13-core-player-views/13-VALIDATION.md (per-task verification map)
  </read_first>
  <files>EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs</files>
  <action>
Create `EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs` with the following exact content.

The class must follow the `MobileLayoutTests` pattern exactly: `IClassFixture<WebApplicationFactoryBase>`, UA constants, `GetWithUserAgentAsync(string url, string userAgent)` helper (note: this helper takes a URL param unlike `MobileLayoutTests` which hardcodes `/`).

Store both `_factory` and `_client` as fields because authenticated tests need `_factory.Services` for seeding.

Write these ten test methods — they will be RED until Wave 1 ships:

**HOME-01 — quest-card-mobile class present, fantasy-quest-card absent:**
```csharp
[Fact]
public async Task MobileHome_MobileUserAgent_RendersCardListNotPosterImages()
{
    var (response, html) = await GetWithUserAgentAsync("/", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("quest-card-mobile");
    html.Should().NotContain("fantasy-quest-card");
    html.Should().NotContain("Blanks w Shadow");
}
```

**HOME-01b — desktop UA does NOT get the mobile card class:**
```csharp
[Fact]
public async Task MobileHome_DesktopUserAgent_DoesNotRenderMobileCardList()
{
    var (response, html) = await GetWithUserAgentAsync("/", DesktopUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().NotContain("quest-card-mobile");
}
```

**HOME-02 — card contains CR badge and status badge:**
```csharp
[Fact]
public async Task MobileHome_MobileUserAgent_QuestCardContainsCrAndStatusBadge()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_home02", "dm_home02@test.com", name: "DM Home02");
    await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "The Lost Mine", challengeRating: 3);

    var (response, html) = await GetWithUserAgentAsync("/", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("The Lost Mine");
    html.Should().Contain("CR 3");
    html.Should().ContainAny("bg-success", "bg-primary", "bg-secondary");
}
```

**HOME-02b — finalized quest shows date:**
```csharp
[Fact]
public async Task MobileHome_MobileUserAgent_FinalizedQuestShowsDate()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_home02b", "dm_home02b@test.com", name: "DM Home02b");
    await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Finalized Adventure", isFinalized: true);

    var (response, html) = await GetWithUserAgentAsync("/", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("Finalized Adventure");
    html.Should().Contain("bg-primary");
}
```

**HOME-03 — card links to Details (non-DM user):**
```csharp
[Fact]
public async Task MobileHome_MobileUserAgent_QuestCardLinksToDetails()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_home03", "dm_home03@test.com", name: "DM Home03");
    var quest = await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Navigation Quest");

    var (response, html) = await GetWithUserAgentAsync("/", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain($"/Quest/Details/{quest.Id}");
}
```

**HOME-04 — signed-up badge appears for authenticated player who is signed up:**
```csharp
[Fact]
public async Task MobileHome_AuthenticatedSignedUpPlayer_ShowsSignedUpBadge()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_home04", "dm_home04@test.com", name: "DM Home04");
    var quest = await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Signup Badge Quest");
    var (authClient, playerUser) = await AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(_factory, "player_home04", "player_home04@test.com");
    await TestDataHelper.CreatePlayerSignupAsync(_factory.Services, quest.Id, playerUser.Id, isSelected: false);

    var request = new HttpRequestMessage(HttpMethod.Get, "/");
    request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent);
    request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization;
    var response = await _client.SendAsync(request, TestContext.Current.CancellationToken);
    var html = await response.Content.ReadAsStringAsync(TestContext.Current.CancellationToken);

    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("Signed up");
}
```

**QVIEW-01 — vote buttons present on Quest Details mobile:**
```csharp
[Fact]
public async Task MobileQuestDetails_MobileUserAgent_RendersVoteButtons()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_qview01", "dm_qview01@test.com", name: "DM Qview01");
    var quest = await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Vote Quest");
    var (authClient, _) = await AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(_factory, "player_qview01", "player_qview01@test.com");

    var request = new HttpRequestMessage(HttpMethod.Get, $"/Quest/Details/{quest.Id}");
    request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent);
    request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization;
    var response = await _client.SendAsync(request, TestContext.Current.CancellationToken);
    var html = await response.Content.ReadAsStringAsync(TestContext.Current.CancellationToken);

    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("changeVoteToYes");
    html.Should().Contain("changeVoteToNo");
    html.Should().Contain("changeVoteToMaybe");
    html.Should().Contain("quests.mobile.css");
}
```

**QVIEW-02 — participant table replaced by stacked list (no table element for participants):**
```csharp
[Fact]
public async Task MobileQuestDetails_MobileUserAgent_ParticipantListIsStacked()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_qview02", "dm_qview02@test.com", name: "DM Qview02");
    var quest = await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Stacked Quest", isFinalized: true);
    var player = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "player_qview02a", "player_qview02a@test.com", name: "Player Alpha");
    await TestDataHelper.CreatePlayerSignupAsync(_factory.Services, quest.Id, player.Id, isSelected: true);
    var (authClient, _) = await AuthenticationHelper.CreateAuthenticatedClientWithUserAsync(_factory, "player_qview02b", "player_qview02b@test.com");

    var request = new HttpRequestMessage(HttpMethod.Get, $"/Quest/Details/{quest.Id}");
    request.Headers.TryAddWithoutValidation("User-Agent", MobileUserAgent);
    request.Headers.Authorization = authClient.DefaultRequestHeaders.Authorization;
    var response = await _client.SendAsync(request, TestContext.Current.CancellationToken);
    var html = await response.Content.ReadAsStringAsync(TestContext.Current.CancellationToken);

    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("participant-row");
    html.Should().NotContain("table-responsive");
    html.Should().Contain("Player Alpha");
}
```

**QVIEW-03 — Quest Log mobile renders list with title and DM name:**
```csharp
[Fact]
public async Task MobileQuestLog_MobileUserAgent_RendersListWithTitleAndDmName()
{
    var dm = await AuthenticationHelper.CreateTestUserAsync(_factory.Services, "dm_qview03", "dm_qview03@test.com", name: "DM Qview03");
    await TestDataHelper.CreateTestQuestAsync(_factory.Services, dm.Id, "Ancient Dungeon", isFinalized: true);

    var (response, html) = await GetWithUserAgentAsync("/QuestLog", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("quest-log-item");
    html.Should().Contain("Ancient Dungeon");
    html.Should().Contain("DM Qview03");
}
```

**QVIEW-03b — CSS link for quest-log.mobile.css is present:**
```csharp
[Fact]
public async Task MobileQuestLog_MobileUserAgent_LoadsMobileCssLink()
{
    var (response, html) = await GetWithUserAgentAsync("/QuestLog", MobileUserAgent);
    response.StatusCode.Should().Be(HttpStatusCode.OK);
    html.Should().Contain("quest-log.mobile.css");
}
```

The full file uses `using System.Net;` and `using EuphoriaInn.IntegrationTests.Helpers;` at the top. The class stores `private readonly WebApplicationFactoryBase _factory;` and `private readonly HttpClient _client;`. The `GetWithUserAgentAsync` helper accepts `(string url, string userAgent)` — two parameters — unlike `MobileLayoutTests` which hardcodes `/`.
  </action>
  <verify>
    <automated>dotnet test /c/Repos/quest-board/EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileViewsTests" --no-build 2>&1 | tail -5</automated>
    Note: tests will FAIL (views not yet created) — the goal is zero BUILD errors, not green tests. Run `dotnet build EuphoriaInn.IntegrationTests` first to confirm compilation succeeds.
  </verify>
  <acceptance_criteria>
    - File EuphoriaInn.IntegrationTests/Mobile/MobileViewsTests.cs exists
    - File contains `IClassFixture&lt;WebApplicationFactoryBase&gt;`
    - File contains `MobileViewsTests` class name
    - File contains `GetWithUserAgentAsync` helper method with two parameters (url, userAgent)
    - File contains `private readonly WebApplicationFactoryBase _factory`
    - File contains all ten test method names: MobileHome_MobileUserAgent_RendersCardListNotPosterImages, MobileHome_DesktopUserAgent_DoesNotRenderMobileCardList, MobileHome_MobileUserAgent_QuestCardContainsCrAndStatusBadge, MobileHome_MobileUserAgent_FinalizedQuestShowsDate, MobileHome_MobileUserAgent_QuestCardLinksToDetails, MobileHome_AuthenticatedSignedUpPlayer_ShowsSignedUpBadge, MobileQuestDetails_MobileUserAgent_RendersVoteButtons, MobileQuestDetails_MobileUserAgent_ParticipantListIsStacked, MobileQuestLog_MobileUserAgent_RendersListWithTitleAndDmName, MobileQuestLog_MobileUserAgent_LoadsMobileCssLink
    - File contains assertions: `html.Should().Contain("quest-card-mobile")`, `html.Should().NotContain("fantasy-quest-card")`, `html.Should().Contain("participant-row")`, `html.Should().NotContain("table-responsive")`
    - `dotnet build EuphoriaInn.IntegrationTests` exits 0 (no compile errors)
    - `dotnet test --filter "FullyQualifiedName~MobileViewsTests"` exits non-zero only because views are missing (not because of compile errors or assertion errors in the test setup logic)
  </acceptance_criteria>
  <done>MobileViewsTests.cs compiles cleanly. All ten tests run (and fail with HTTP errors or assertion failures because views don't exist yet). Zero build errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → app | HTTP requests from test client; no user input; read-only assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Spoofing | TestAuthHandler | accept | Test-only auth scheme; never enabled in production builds; `builder.UseEnvironment("Testing")` gate |

View-only plan — no new endpoints, no new form inputs, no new attack surfaces. Antiforgery handled by established `TestAntiforgeryDecorator` pattern already in `WebApplicationFactoryBase`.
</threat_model>

<verification>
`dotnet build EuphoriaInn.IntegrationTests` exits 0.
`dotnet test EuphoriaInn.IntegrationTests --filter "FullyQualifiedName~MobileViewsTests"` discovers and runs 10 tests. Tests fail due to missing views — not due to compile errors or setup crashes.
</verification>

<success_criteria>
- MobileViewsTests.cs exists in EuphoriaInn.IntegrationTests/Mobile/
- 10 test methods covering HOME-01, HOME-02, HOME-03, HOME-04, QVIEW-01, QVIEW-02, QVIEW-03
- Zero build errors
- Test runner discovers all 10 methods
</success_criteria>

<output>
After completion, create `.planning/phases/13-core-player-views/13-01-SUMMARY.md`
</output>
