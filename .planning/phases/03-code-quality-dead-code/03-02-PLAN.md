---
phase: 03-code-quality-dead-code
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - EuphoriaInn.Repository/QuestRepository.cs
  - EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs
  - EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs
autonomous: true
requirements: [QUAL-03, QUAL-04, QUAL-05]
must_haves:
  truths:
    - "QuestRepository no longer contains the literal 'SignupRole == 1'; the Spectator enum cast is used instead"
    - "The 30-minute IsSameDateTime tolerance window is defined as a named private const with an explanatory comment"
    - "CharacterViewModels folder contains CharactersIndexViewModel.cs (not GuildMembersIndexViewModel.cs)"
    - "The GuildMembers/Index page still renders (integration test green) — rename is filesystem-only, class name unchanged"
    - "Solution builds and all tests pass"
  artifacts:
    - path: "EuphoriaInn.Repository/QuestRepository.cs"
      provides: "SignupRole enum cast, named DateMatchWindowMinutes constant"
      contains: "SignupRole.Spectator"
    - path: "EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs"
      provides: "Renamed file whose filename matches its class name"
      contains: "class CharactersIndexViewModel"
  key_links:
    - from: "EuphoriaInn.Repository/QuestRepository.cs"
      to: "EuphoriaInn.Domain/Enums/SignupRole.cs"
      via: "using EuphoriaInn.Domain.Enums; (already present) and explicit cast comparison"
      pattern: "\\(SignupRole\\)playerSignup\\.SignupRole == SignupRole\\.Spectator"
    - from: "EuphoriaInn.Service/Controllers/Characters/GuildMembersController.cs"
      to: "EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs"
      via: "Class name reference (unchanged) — file rename is transparent to C# compiler"
      pattern: "CharactersIndexViewModel"
---

<objective>
Three mechanical edits independent of Plan 01:
- QUAL-03: Replace the `SignupRole == 1` magic number in `QuestRepository.cs` with the established enum-cast pattern.
- QUAL-04: Extract the hardcoded `30` minute tolerance inside `IsSameDateTime` as a named private const with an explanatory comment (per D-02, kept private-scoped in `QuestRepository.cs`).
- QUAL-05 / D-03: Rename `CharacterViewModels/GuildMembersIndexViewModel.cs` to `CharactersIndexViewModel.cs` to match the class name it already declares. D-04 audit result (per RESEARCH.md): no additional moves required — `CharacterViewModel.cs` is already correctly named.

Purpose: Readability and correctness of the single most complex repository file, plus a filename that matches its contained class name.

Output: `QuestRepository.cs` with self-documenting comparisons and constants; `CharacterViewModels/` folder with only correctly-named files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-code-quality-dead-code/03-CONTEXT.md
@.planning/phases/03-code-quality-dead-code/03-RESEARCH.md
@EuphoriaInn.Repository/QuestRepository.cs
@EuphoriaInn.Domain/Enums/SignupRole.cs
@EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs
@EuphoriaInn.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs
@EuphoriaInn.Service/Controllers/Characters/GuildMembersController.cs
@EuphoriaInn.Service/Views/GuildMembers/Index.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SignupRole magic number AND extract 30-minute constant in QuestRepository.cs (QUAL-03 + QUAL-04)</name>
  <files>EuphoriaInn.Repository/QuestRepository.cs</files>
  <read_first>
    - EuphoriaInn.Repository/QuestRepository.cs (locate line ~105 `playerSignup.SignupRole == 1`, line ~198 `<= 30`, and current class header)
    - EuphoriaInn.Domain/Enums/SignupRole.cs (confirm Spectator = 1 is defined)
    - .planning/phases/03-code-quality-dead-code/03-RESEARCH.md (Patterns 2 and 3, lines 82-103)
    - .planning/phases/03-code-quality-dead-code/03-CONTEXT.md (D-02: private const location decision)
  </read_first>
  <action>
    Two edits inside `EuphoriaInn.Repository/QuestRepository.cs`:

    **Edit A — QUAL-03 (magic number replacement):**

    Find the line (research locates it at line ~105; locate by content, not line number):
    ```csharp
    if (playerSignup.SignupRole == 1)
    ```

    Replace with:
    ```csharp
    if ((SignupRole)playerSignup.SignupRole == SignupRole.Spectator)
    ```

    Also remove any adjacent comment that explains the magic `1` (e.g., `// 1 = Spectator`) since the code now self-documents. If a comment like `// Auto-approve spectators` exists, keep it — it describes intent, not the literal.

    The `using EuphoriaInn.Domain.Enums;` directive is already present at the top of the file per RESEARCH.md line 231 — do NOT re-add.

    **Edit B — QUAL-04 (named constant extraction, per D-02 private-scoped in this file):**

    Add a private const at the top of the `QuestRepository` class body (immediately after the class declaration / constructor, before the first method). Use this exact declaration (name chosen per Claude's Discretion in CONTEXT.md for readability):

    ```csharp
    // Tolerance window for treating two proposed dates as the same slot;
    // accommodates minor timezone rounding when users resubmit dates.
    private const int DateMatchWindowMinutes = 30;
    ```

    Then in the `IsSameDateTime` method (research locates at line ~198; locate by method signature), replace:
    ```csharp
    return Math.Abs((date1 - date2).TotalMinutes) <= 30;
    ```
    with:
    ```csharp
    return Math.Abs((date1 - date2).TotalMinutes) <= DateMatchWindowMinutes;
    ```

    Do not alter any other `IsSameDateTime` callers. Do not introduce a public or shared constants class (D-02 explicitly rules that out). Do not alter any other `30` literals elsewhere in the file (they are unrelated — e.g., page sizes, retry counts; only the `IsSameDateTime` one is in scope).

    After both edits, run:
    - `dotnet build EuphoriaInn.sln` (must succeed, 0 errors)
    - `dotnet test EuphoriaInn.UnitTests --no-build` (30 tests pass)
    - `dotnet test EuphoriaInn.IntegrationTests --no-build` (53 tests pass — `QuestFinalizeTests` provides the behavioral regression guard on the Spectator branch)
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln && dotnet test --no-build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.sln` exits 0 with 0 Errors.
    - `dotnet test --no-build` reports all 83 tests passing (30 unit + 53 integration).
    - Grep: `grep -n "SignupRole == 1" EuphoriaInn.Repository/QuestRepository.cs` returns ZERO matches.
    - Grep: `grep -n "SignupRole.Spectator" EuphoriaInn.Repository/QuestRepository.cs` returns AT LEAST ONE match.
    - Grep: `grep -n "(SignupRole)playerSignup.SignupRole" EuphoriaInn.Repository/QuestRepository.cs` returns AT LEAST ONE match.
    - Grep: `grep -n "private const int DateMatchWindowMinutes = 30" EuphoriaInn.Repository/QuestRepository.cs` returns EXACTLY ONE match.
    - Grep: `grep -n "DateMatchWindowMinutes" EuphoriaInn.Repository/QuestRepository.cs` returns AT LEAST TWO matches (declaration + usage in IsSameDateTime).
    - Grep: `grep -n "TotalMinutes) <= 30" EuphoriaInn.Repository/QuestRepository.cs` returns ZERO matches (the literal `30` in this context is gone).
    - Grep (solution-wide sanity): `grep -rn "SignupRole == 1" --include="*.cs" EuphoriaInn.Domain EuphoriaInn.Repository EuphoriaInn.Service` returns ZERO matches.
  </acceptance_criteria>
  <done>
    Magic number `1` replaced with typed enum comparison; 30-minute window replaced with a self-documenting private const; all tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename GuildMembersIndexViewModel.cs to CharactersIndexViewModel.cs (QUAL-05, D-03, D-04)</name>
  <files>
    EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs (rename to),
    EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs
  </files>
  <read_first>
    - EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs (confirm it contains `public class CharactersIndexViewModel` and `namespace EuphoriaInn.Service.ViewModels.CharacterViewModels;`)
    - EuphoriaInn.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs (confirm this file is already correctly named — the D-04 audit baseline)
    - EuphoriaInn.Service/Controllers/Characters/GuildMembersController.cs (confirm it references `CharactersIndexViewModel` by class name, not filename)
    - EuphoriaInn.Service/Views/GuildMembers/Index.cshtml (confirm `@model CharactersIndexViewModel` is present)
    - .planning/phases/03-code-quality-dead-code/03-RESEARCH.md (Pattern 4 + D-04 audit result, lines 105-121)
  </read_first>
  <action>
    Per D-03 and QUAL-05: rename the file only. The class name inside (`CharactersIndexViewModel`) is already correct and already referenced by controller and view — DO NOT change the class name, namespace, or file contents.

    Also per D-04 audit result (RESEARCH.md lines 115-121): `CharacterViewModels/` contains exactly two files; `CharacterViewModel.cs` is correctly named and correctly placed — NO additional moves or renames required in this folder.

    Steps:

    1. Perform a git-tracked rename to preserve history:
       ```
       git mv "EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs" "EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs"
       ```
       If `git mv` refuses (file not tracked), fall back to filesystem rename + `git add` of the new path and `git rm` of the old path.

    2. Do NOT edit the file contents. Specifically:
       - The `namespace EuphoriaInn.Service.ViewModels.CharacterViewModels;` line stays as-is.
       - The `public class CharactersIndexViewModel` declaration stays as-is.
       - Any using directives stay as-is.

    3. Do NOT touch any of the following (they are already correct):
       - `EuphoriaInn.Service/Controllers/Characters/GuildMembersController.cs`
       - `EuphoriaInn.Service/Views/GuildMembers/Index.cshtml`
       - `EuphoriaInn.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs` — this is a DIFFERENT file with a different class, used by `PlayersController`. Leave it entirely alone (per D-03 explicit note and RESEARCH.md Anti-Patterns).
       - `EuphoriaInn.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs` — correctly named per D-04 audit.

    4. Verify:
       - `dotnet build EuphoriaInn.sln` — 0 errors.
       - `dotnet test EuphoriaInn.IntegrationTests --no-build` — 53 tests pass (`GuildMembersControllerIntegrationTests` confirms the Index page still renders).

    5. If the `.csproj` for `EuphoriaInn.Service` explicitly lists ViewModels files by path (it typically does NOT in .NET SDK-style projects with default globs), update the project file accordingly. For SDK-style projects (Microsoft.NET.Sdk.Web), no .csproj edit is needed because files are globbed by convention.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln && dotnet test EuphoriaInn.IntegrationTests --no-build</automated>
  </verify>
  <acceptance_criteria>
    - File `EuphoriaInn.Service/ViewModels/CharacterViewModels/GuildMembersIndexViewModel.cs` does NOT exist.
    - File `EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs` DOES exist.
    - Grep: `grep -n "class CharactersIndexViewModel" EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs` returns AT LEAST ONE match (class name preserved).
    - Grep: `grep -n "namespace EuphoriaInn.Service.ViewModels.CharacterViewModels" EuphoriaInn.Service/ViewModels/CharacterViewModels/CharactersIndexViewModel.cs` returns ONE match (namespace preserved).
    - File `EuphoriaInn.Service/ViewModels/GuildMembersViewModels/GuildMembersIndexViewModel.cs` STILL exists and is unchanged (separate class, must not be touched).
    - File `EuphoriaInn.Service/ViewModels/CharacterViewModels/CharacterViewModel.cs` STILL exists and is unchanged.
    - `dotnet build EuphoriaInn.sln` exits 0 with 0 Errors.
    - `dotnet test EuphoriaInn.IntegrationTests --no-build` reports 53 tests passing, 0 failing (including `GuildMembersControllerIntegrationTests`).
    - `git status` shows the rename as a single renamed file (not add + delete) when git mv was used — indicates history preserved.
  </acceptance_criteria>
  <done>
    Filename matches class name; GuildMembers/Index integration test still green; no other files touched.
  </done>
</task>

</tasks>

<verification>
Post-plan checks (run from repo root):

1. `dotnet build EuphoriaInn.sln` → 0 errors
2. `dotnet test` → 83 tests passing
3. `grep -rn "SignupRole == 1" --include="*.cs" .` → 0 matches
4. `grep -n "DateMatchWindowMinutes" EuphoriaInn.Repository/QuestRepository.cs` → 2+ matches
5. `ls EuphoriaInn.Service/ViewModels/CharacterViewModels/` → CharactersIndexViewModel.cs + CharacterViewModel.cs only
6. `ls EuphoriaInn.Service/ViewModels/GuildMembersViewModels/` → GuildMembersIndexViewModel.cs still present (untouched)
</verification>

<success_criteria>
- QUAL-03 satisfied: enum cast used; no `SignupRole == 1` literal in the solution
- QUAL-04 satisfied: `DateMatchWindowMinutes = 30` private const in QuestRepository.cs with explanatory comment; `IsSameDateTime` uses it
- QUAL-05 satisfied: File renamed from `GuildMembersIndexViewModel.cs` to `CharactersIndexViewModel.cs` inside `CharacterViewModels/`; class name unchanged; controller + view still work
- D-04 satisfied: Folder audit complete — no additional files required moving
- `GuildMembersViewModels/GuildMembersIndexViewModel.cs` untouched (different class, different controller)
- Solution builds with zero errors; all 83 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-dead-code/03-02-SUMMARY.md`.
</output>
