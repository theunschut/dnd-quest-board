---
phase: 03-code-quality-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Domain/Configuration/SecurityConfiguration.cs
  - EuphoriaInn.Service/appsettings.json
  - EuphoriaInn.Domain/Interfaces/IQuestService.cs
  - EuphoriaInn.Domain/Services/QuestService.cs
  - EuphoriaInn.Domain/Interfaces/IQuestRepository.cs
  - EuphoriaInn.Repository/QuestRepository.cs
autonomous: true
requirements: [QUAL-01, QUAL-02]
must_haves:
  truths:
    - "SecurityConfiguration.cs no longer exists in the Domain project"
    - "appsettings.json does not contain a top-level 'Security' key"
    - "UpdateQuestPropertiesAsync (non-notification variant) does not exist in any of the 4 layers"
    - "Solution builds with zero errors after removals"
    - "All existing tests pass (30 unit + 53 integration)"
  artifacts:
    - path: "EuphoriaInn.Service/appsettings.json"
      provides: "Configuration without dead Security section"
      contains: "Logging"
    - path: "EuphoriaInn.Domain/Interfaces/IQuestService.cs"
      provides: "QuestService interface without dead UpdateQuestPropertiesAsync"
    - path: "EuphoriaInn.Domain/Interfaces/IQuestRepository.cs"
      provides: "QuestRepository interface without dead UpdateQuestPropertiesAsync"
  key_links:
    - from: "EuphoriaInn.Domain/Services/QuestService.cs"
      to: "EuphoriaInn.Domain/Interfaces/IQuestRepository.cs"
      via: "No call to repository.UpdateQuestPropertiesAsync remains (only the WithNotifications variant survives)"
      pattern: "UpdateQuestPropertiesWithNotificationsAsync"
---

<objective>
Eliminate dead configuration (SecurityConfiguration + Security JSON block, QUAL-01) and dead service/repository method (UpdateQuestPropertiesAsync non-notification variant across all 4 layers, QUAL-02).

Purpose: Remove unused code that misleads future readers and tooling. Dead code in interfaces is especially harmful — it advertises capabilities that do not exist.

Output: Cleaner Domain layer with no unused types or method declarations. Build and tests still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-quality-dead-code/03-CONTEXT.md
@.planning/phases/03-code-quality-dead-code/03-RESEARCH.md
@EuphoriaInn.Domain/Configuration/SecurityConfiguration.cs
@EuphoriaInn.Service/appsettings.json
@EuphoriaInn.Domain/Interfaces/IQuestService.cs
@EuphoriaInn.Domain/Services/QuestService.cs
@EuphoriaInn.Domain/Interfaces/IQuestRepository.cs
@EuphoriaInn.Repository/QuestRepository.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete SecurityConfiguration.cs and remove Security section from appsettings.json (QUAL-01)</name>
  <files>
    EuphoriaInn.Domain/Configuration/SecurityConfiguration.cs (DELETE),
    EuphoriaInn.Service/appsettings.json (modify)
  </files>
  <read_first>
    - EuphoriaInn.Domain/Configuration/SecurityConfiguration.cs (confirm class is internal and still exists)
    - EuphoriaInn.Service/appsettings.json (confirm current Security block structure)
    - .planning/phases/03-code-quality-dead-code/03-RESEARCH.md (QUAL-01 section, lines 173-190)
  </read_first>
  <action>
    Per D-01 / QUAL-01, fully remove the dead SecurityConfiguration type and its JSON counterpart.

    1. Delete the file `EuphoriaInn.Domain/Configuration/SecurityConfiguration.cs` (git rm, or Delete via filesystem if git-untracked; it is an `internal class` with zero callers — verified in RESEARCH.md).

    2. If the directory `EuphoriaInn.Domain/Configuration/` is empty after deletion, leave it as-is (do NOT delete the directory — keep the folder for potential future config types). If it still contains other files, do nothing to the folder.

    3. Edit `EuphoriaInn.Service/appsettings.json` — remove the entire top-level `"Security"` JSON object. The exact current block (lines 2-6) to remove:
       ```
         "Security": {
           "PasswordIterations": 100000,
           "SaltSize": 32,
           "HashSize": 32
         },
       ```
       After removal, the file must begin:
       ```
       {
         "Logging": {
       ```
       Preserve all other keys verbatim (Logging, ConnectionStrings, EmailSettings, AllowedHosts, etc.).

    4. Run `dotnet build` from the repo root. Build MUST succeed with zero errors. If any error surfaces referencing `SecurityConfiguration`, the class was not truly dead — STOP and investigate; do not proceed to Task 2 until build is green.

    No `Program.cs` or `ServiceExtensions` change is required — SecurityConfiguration was never bound (verified: zero callers).
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln</automated>
  </verify>
  <acceptance_criteria>
    - `EuphoriaInn/Domain/Configuration/SecurityConfiguration.cs` does NOT exist (file deletion).
    - `EuphoriaInn.Service/appsettings.json` contains NO line matching the regex `"Security"\s*:` (grep returns zero matches).
    - `EuphoriaInn.Service/appsettings.json` contains `"Logging"` (other keys preserved).
    - `EuphoriaInn.Service/appsettings.json` is valid JSON (parses without error).
    - `dotnet build EuphoriaInn.sln` exits with code 0 and reports 0 Errors.
    - Grep: `grep -r "SecurityConfiguration" --include="*.cs" EuphoriaInn.Domain EuphoriaInn.Repository EuphoriaInn.Service` returns zero matches.
    - Grep: `grep -r "Security:PasswordIterations\|Security:SaltSize\|Security:HashSize" --include="*.cs" --include="*.json" EuphoriaInn.Domain EuphoriaInn.Repository EuphoriaInn.Service` returns zero matches.
  </acceptance_criteria>
  <done>
    SecurityConfiguration.cs gone, Security JSON block gone, solution builds green, no dangling references anywhere in the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove dead UpdateQuestPropertiesAsync (non-notification variant) from all 4 layers atomically (QUAL-02)</name>
  <files>
    EuphoriaInn.Domain/Interfaces/IQuestService.cs,
    EuphoriaInn.Domain/Services/QuestService.cs,
    EuphoriaInn.Domain/Interfaces/IQuestRepository.cs,
    EuphoriaInn.Repository/QuestRepository.cs
  </files>
  <read_first>
    - EuphoriaInn.Domain/Interfaces/IQuestService.cs (locate the UpdateQuestPropertiesAsync declaration WITHOUT 'WithNotifications')
    - EuphoriaInn.Domain/Services/QuestService.cs (locate its implementation)
    - EuphoriaInn.Domain/Interfaces/IQuestRepository.cs (locate the repository interface declaration)
    - EuphoriaInn.Repository/QuestRepository.cs (locate the repository implementation)
    - .planning/phases/03-code-quality-dead-code/03-RESEARCH.md (Pattern 1 + Pitfall 2, lines 70-80 + 156-159)
  </read_first>
  <action>
    Per D-01 / QUAL-02, remove the dead `UpdateQuestPropertiesAsync` method (the variant WITHOUT "WithNotifications" in its name) from all four layers in a single commit so the solution remains buildable.

    CRITICAL: There are TWO similarly-named methods. Keep `UpdateQuestPropertiesWithNotificationsAsync` intact — it is live and used by QuestController. Remove ONLY the bare `UpdateQuestPropertiesAsync` (no suffix) method, which has the signature:
    ```csharp
    Task UpdateQuestPropertiesAsync(int questId, string title, string description, int challengeRating, int totalPlayerCount, bool dungeonMasterSession, bool updateProposedDates = false, IList<DateTime>? proposedDates = null, CancellationToken token = default);
    ```

    Perform edits in this exact order to keep the codebase compiling at every intermediate save:

    1. `EuphoriaInn.Repository/QuestRepository.cs` — delete the `public async Task UpdateQuestPropertiesAsync(...)` method body (research locates it at lines 136-156; verify current line by method signature, not line number).

    2. `EuphoriaInn.Domain/Interfaces/IQuestRepository.cs` — delete the matching `Task UpdateQuestPropertiesAsync(...)` declaration.

    3. `EuphoriaInn.Domain/Services/QuestService.cs` — delete the `public async Task UpdateQuestPropertiesAsync(...)` method that delegates to `repository.UpdateQuestPropertiesAsync(...)`. Research locates it at lines 105-108.

    4. `EuphoriaInn.Domain/Interfaces/IQuestService.cs` — delete the matching interface declaration.

    5. Run `dotnet build EuphoriaInn.sln` from repo root. Must succeed with 0 errors.

    6. Run `dotnet test EuphoriaInn.UnitTests --no-build` then `dotnet test EuphoriaInn.IntegrationTests --no-build`. Both MUST pass (30 unit + 53 integration per RESEARCH.md).

    Do NOT touch `UpdateQuestPropertiesWithNotificationsAsync` in any file. Do NOT touch any test files — no test references the dead variant (verified solution-wide in RESEARCH.md).

    Preserve XML doc comments on the neighboring `WithNotifications` method. Preserve all using directives.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.sln && dotnet test EuphoriaInn.UnitTests --no-build && dotnet test EuphoriaInn.IntegrationTests --no-build</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build EuphoriaInn.sln` exits 0 with 0 Errors.
    - `dotnet test EuphoriaInn.UnitTests --no-build` reports all 30 tests passing, 0 failing.
    - `dotnet test EuphoriaInn.IntegrationTests --no-build` reports all 53 tests passing, 0 failing.
    - Grep: `grep -rn "Task UpdateQuestPropertiesAsync(" --include="*.cs" EuphoriaInn.Domain EuphoriaInn.Repository` returns ZERO matches (the only matches remaining should be for `UpdateQuestPropertiesWithNotificationsAsync`).
    - Grep: `grep -rn "UpdateQuestPropertiesWithNotificationsAsync" --include="*.cs" EuphoriaInn.Domain EuphoriaInn.Repository EuphoriaInn.Service` returns AT LEAST 4 matches (IQuestService decl, QuestService impl, IQuestRepository decl, QuestRepository impl, plus controller usages) — confirming the live method was preserved.
    - Grep strict (using word boundary to avoid matching the WithNotifications variant): `grep -rEn "UpdateQuestPropertiesAsync\b" --include="*.cs" EuphoriaInn.Domain EuphoriaInn.Repository EuphoriaInn.Service` returns ZERO matches.
  </acceptance_criteria>
  <done>
    Dead method removed from all 4 layers, live WithNotifications variant untouched, solution builds and all 83 tests pass.
  </done>
</task>

</tasks>

<verification>
Post-plan checks (run from repo root):

1. `dotnet build EuphoriaInn.sln` → 0 errors, 0 warnings beyond baseline
2. `dotnet test` → 83 tests passing (30 unit + 53 integration)
3. `grep -r "SecurityConfiguration" --include="*.cs" .` → 0 matches
4. `grep -rEn "UpdateQuestPropertiesAsync\b" --include="*.cs" .` → 0 matches (word boundary excludes the WithNotifications variant)
5. `grep -n '"Security"' EuphoriaInn.Service/appsettings.json` → 0 matches
</verification>

<success_criteria>
- QUAL-01 satisfied: SecurityConfiguration.cs deleted, Security JSON block removed
- QUAL-02 satisfied: Dead method absent from IQuestService, QuestService, IQuestRepository, QuestRepository
- Solution builds with zero errors
- All 83 existing tests pass
- No user-facing behavior change (app still starts; authentication still works via existing Identity config)
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-dead-code/03-01-SUMMARY.md`.
</output>
