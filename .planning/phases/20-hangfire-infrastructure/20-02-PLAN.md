---
phase: 20-hangfire-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/Views/Shared/_Layout.cshtml
autonomous: true
requirements:
  - JOBS-02

must_haves:
  truths:
    - "Admin users see a Background Jobs link in the Admin dropdown navigation"
    - "The Background Jobs link points to /hangfire (plain href, not asp-controller/asp-action)"
    - "The link is only visible when the AdminOnly policy is satisfied (inherits existing dropdown guard)"
    - "Non-Admin users see no change to the navigation"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Shared/_Layout.cshtml"
      provides: "Admin dropdown with Background Jobs nav entry"
      contains: "href=\"/hangfire\""
  key_links:
    - from: "_Layout.cshtml Admin dropdown"
      to: "/hangfire"
      via: "plain href attribute"
      pattern: "href=\"/hangfire\""
---

<objective>
Add the "Background Jobs" navigation link to the Admin dropdown in _Layout.cshtml.

Purpose: Satisfies D-10 — Admin users need a navigation entry to reach the Hangfire dashboard
at /hangfire. This plan is wave-1 parallel to Plan 01 because it touches a different file.

Output: _Layout.cshtml with a third <li> entry in the Admin dropdown, using a plain href="/hangfire"
(not MVC tag helpers), with the fa-tasks icon and "Background Jobs" label.
</objective>

<execution_context>
@C:\Users\t.schut\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\t.schut\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Repos\quest-board\.planning\ROADMAP.md
@C:\Repos\quest-board\.planning\STATE.md

<interfaces>
<!-- Exact existing Admin dropdown markup from _Layout.cshtml lines 31–50 -->
<!-- The new <li> is inserted after line 47 (after Quest Management </li>), before </ul> on line 48 -->

```html
@if ((await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded)
{
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle text-danger" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
            <i class="fas fa-shield-alt me-1"></i>Admin
        </a>
        <ul class="dropdown-menu">
            <li>
                <a class="dropdown-item" asp-controller="Admin" asp-action="Users">
                    <i class="fas fa-users-cog me-2"></i>User Management
                </a>
            </li>
            <li>
                <a class="dropdown-item" asp-controller="Admin" asp-action="Quests">
                    <i class="fas fa-scroll me-2"></i>Quest Management
                </a>
            </li>
        </ul>
    </li>
}
```

The new `<li>` is inserted immediately before `</ul>` (after the Quest Management `</li>`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Background Jobs nav link into Admin dropdown</name>
  <files>EuphoriaInn.Service/Views/Shared/_Layout.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Shared/_Layout.cshtml — read lines 31–50 to locate the exact closing `</ul>` of the Admin dropdown before making the edit; confirm the existing two dropdown items are User Management and Quest Management
  </read_first>
  <action>
In `EuphoriaInn.Service/Views/Shared/_Layout.cshtml`, locate the closing `</ul>` tag of the Admin
dropdown (the one that closes the dropdown-menu list, after the Quest Management `</li>`).

Insert the following `<li>` element immediately before that closing `</ul>` tag:

```html
                        <li>
                            <a class="dropdown-item" href="/hangfire">
                                <i class="fas fa-tasks me-2"></i>Background Jobs
                            </a>
                        </li>
```

The result should produce this Admin dropdown `<ul>`:

```html
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" asp-controller="Admin" asp-action="Users">
                                <i class="fas fa-users-cog me-2"></i>User Management
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" asp-controller="Admin" asp-action="Quests">
                                <i class="fas fa-scroll me-2"></i>Quest Management
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/hangfire">
                                <i class="fas fa-tasks me-2"></i>Background Jobs
                            </a>
                        </li>
                    </ul>
```

Critical rules (per D-10, UI-SPEC.md):
- Use `href="/hangfire"` — NOT `asp-controller`/`asp-action` tag helpers; /hangfire is a Hangfire middleware route, not an MVC controller action
- Use `class="dropdown-item"` — matches all other items in this dropdown
- Use `<i class="fas fa-tasks me-2"></i>` — FontAwesome tasks icon, me-2 spacing (8px)
- Label text is "Background Jobs" (per UI-SPEC.md copywriting contract)
- No `<li><hr class="dropdown-divider"></li>` before this item — it is a peer item, not a new section
- Do NOT add any additional authorization check on this `<li>` — visibility is already controlled by the outer `@if ((await AuthorizationService.AuthorizeAsync(User, "AdminOnly")).Succeeded)` block
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service/EuphoriaInn.Service.csproj --no-restore</automated>
  </verify>
  <acceptance_criteria>
    - `_Layout.cshtml` contains `href="/hangfire"` (plain href, not asp-action)
    - `_Layout.cshtml` contains `<i class="fas fa-tasks me-2"></i>Background Jobs`
    - `_Layout.cshtml` does NOT contain `asp-controller="Hangfire"` or any MVC tag helper pointing to hangfire
    - The `href="/hangfire"` `<li>` appears inside the `<ul class="dropdown-menu">` of the Admin dropdown (after Quest Management, before `</ul>`)
    - `dotnet build EuphoriaInn.Service/EuphoriaInn.Service.csproj --no-restore` exits with code 0
  </acceptance_criteria>
  <done>Admin dropdown contains three items: User Management, Quest Management, Background Jobs; Background Jobs links to /hangfire via plain href</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered HTML → Admin user | The nav link is rendered only within the AdminOnly authorization guard — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-----------------|-----------------|
| T-20-04 | Information Disclosure | _Layout.cshtml nav link | accept | The `/hangfire` href in the nav is visible only inside the `@if (AdminOnly)` Razor block — non-Admin users never receive this HTML; the dashboard itself is independently protected by AdminDashboardAuthFilter (Plan 01) |
</threat_model>

<verification>
After the task completes:

1. Grep `_Layout.cshtml` for `href="/hangfire"` — must find exactly one match
2. Grep `_Layout.cshtml` for `fa-tasks` — must find one match
3. `dotnet build EuphoriaInn.Service/EuphoriaInn.Service.csproj --no-restore` exits 0
</verification>

<success_criteria>
- Admin dropdown in _Layout.cshtml has exactly three items: User Management, Quest Management, Background Jobs
- Background Jobs link uses plain href="/hangfire" (not MVC tag helpers)
- Link is inside the AdminOnly authorization guard (no extra auth check needed on the li)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-hangfire-infrastructure/20-02-SUMMARY.md`
</output>
