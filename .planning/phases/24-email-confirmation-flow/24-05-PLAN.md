---
phase: 24-email-confirmation-flow
plan: 05
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - EuphoriaInn.Domain/Services/QuestService.cs
  - EuphoriaInn.Service/Jobs/SessionReminderJob.cs
  - EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs
  - EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs
autonomous: true
requirements: [REQ-24-04]
must_haves:
  truths:
    - "QuestFinalizedEmailJob never receives an unconfirmed user's email — filtered at the QuestService finalize call site"
    - "QuestDateChangedEmailJob never receives an unconfirmed user's email — filtered at the QuestService date-change call site"
    - "SessionReminderJob skips signups whose Player.EmailConfirmed is false (covers DailyReminderJob, which only enqueues SessionReminderJob)"
  artifacts:
    - path: "EuphoriaInn.Domain/Services/QuestService.cs"
      provides: "EmailConfirmed filter applied before building recipientEmails arrays in both dispatch sites"
      contains: "EmailConfirmed"
    - path: "EuphoriaInn.Service/Jobs/SessionReminderJob.cs"
      provides: "EmailConfirmed guard on targetSignups before send loop"
      contains: "EmailConfirmed"
    - path: "EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs"
      provides: "Tests proving QuestService excludes unconfirmed recipients from both dispatches"
      contains: "EmailConfirmed"
    - path: "EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs"
      provides: "Test proving SessionReminderJob skips unconfirmed players"
      contains: "EmailConfirmed"
  key_links:
    - from: "EuphoriaInn.Domain/Services/QuestService.cs"
      to: "EuphoriaInn.Domain/Models/User.cs"
      via: "filter ps.Player.EmailConfirmed / p.EmailConfirmed before building recipient arrays"
      pattern: "EmailConfirmed"
    - from: "EuphoriaInn.Service/Jobs/SessionReminderJob.cs"
      to: "EuphoriaInn.Domain/Models/User.cs"
      via: "ps.Player.EmailConfirmed guard in target-signup filter"
      pattern: "EmailConfirmed"
---

<objective>
Apply the `EmailConfirmed` guard to every email-sending path so unconfirmed users never receive automated mail (REQ-24-04, D-06). Two of the four jobs (`QuestFinalizedEmailJob`, `QuestDateChangedEmailJob`) receive `string[] recipientEmails` and have no `User` objects, so the guard must be applied at their call sites in `QuestService`; `SessionReminderJob` has `User` objects on `PlayerSignup.Player` and is guarded inline; `DailyReminderJob` only enqueues `SessionReminderJob`, so guarding the latter covers it.

Purpose: Closes the one requirement most likely to be missed (the string[]-array jobs cannot guard internally). Delivers REQ-24-04 with unit tests. Replaces the Wave 0 placeholder in `EmailConfirmationJobGuardTests.cs` (created in plan 01) with real assertions and extends `SessionReminderJobTests.cs`.
Output: Guarded QuestService dispatch sites, guarded SessionReminderJob, two test files filled in.
</objective>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from drift verification):
- New test methods in `EmailConfirmationJobGuardTests` (replaces the plan-01 placeholder Fact)
- New test method(s) in `SessionReminderJobTests` asserting unconfirmed-player skip
- No new public production symbols — only inline filter edits to existing methods
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-email-confirmation-flow/24-CONTEXT.md
@.planning/phases/24-email-confirmation-flow/24-RESEARCH.md
@.planning/phases/24-email-confirmation-flow/24-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard both QuestService dispatch sites on EmailConfirmed</name>
  <files>EuphoriaInn.Domain/Services/QuestService.cs</files>
  <read_first>
    - EuphoriaInn.Domain/Services/QuestService.cs (FinalizeQuestAsync lines 15-42 — `selectedSignups` built at lines 23-26 already filters on `!string.IsNullOrEmpty(ps.Player.Email)`; recipientEmails built at line 30. UpdateQuestPropertiesWithNotificationsAsync lines 106-146 — `affectedPlayers` is `IList<User>`; `withEmail` built at line 133 already filters on `!string.IsNullOrEmpty(p.Email)`)
    - EuphoriaInn.Domain/Models/QuestBoard/PlayerSignup.cs (confirms `public required User Player`, so `ps.Player.EmailConfirmed` is available)
    - EuphoriaInn.Domain/Extensions/UserExtensions.cs (WhereEmailConfirmed on IEnumerable<User> — created in plan 01; usable directly on `affectedPlayers`)
    - EuphoriaInn.Domain/Models/User.cs (has EmailConfirmed after plan 01)
  </read_first>
  <behavior>
    - FinalizeQuestAsync: given selected signups where one player has EmailConfirmed=false, that player's email is NOT in the recipientEmails passed to dispatcher.EnqueueFinalizedEmail
    - FinalizeQuestAsync: if every selected player is unconfirmed, the method returns without calling EnqueueFinalizedEmail
    - UpdateQuestPropertiesWithNotificationsAsync: an unconfirmed affected player's email is NOT in the array passed to dispatcher.EnqueueDateChangedEmail
    - UpdateQuestPropertiesWithNotificationsAsync: if every affected player is unconfirmed, the method returns Ok(0) without dispatching
  </behavior>
  <action>
    In `FinalizeQuestAsync`, extend the existing `selectedSignups` filter (lines 23-26) to also require a confirmed email: add `&& ps.Player.EmailConfirmed` to the `Where` predicate so the chain reads `(selectedPlayerSignupIds.Contains(ps.Id) || ps.Role == SignupRole.Spectator) && !string.IsNullOrEmpty(ps.Player.Email) && ps.Player.EmailConfirmed`. The existing `if (selectedSignups.Count == 0) return;` guard then correctly skips dispatch when no confirmed recipients remain.
    In `UpdateQuestPropertiesWithNotificationsAsync`, apply the `WhereEmailConfirmed()` extension to the `withEmail` projection (line 133). Change `var withEmail = affectedPlayers.Where(p => !string.IsNullOrEmpty(p.Email)).ToList();` to first filter confirmed then email: `var withEmail = affectedPlayers.WhereEmailConfirmed().Where(p => !string.IsNullOrEmpty(p.Email)).ToList();`. Add `using EuphoriaInn.Domain.Extensions;` to the file. The existing `if (withEmail.Count == 0) return ServiceResult<int>.Ok(0);` guard then handles the all-unconfirmed case. Using `WhereEmailConfirmed()` here directly satisfies D-06 ("all four jobs' recipient lists pass through the extension") for the IEnumerable<User> path.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Domain</automated>
  </verify>
  <acceptance_criteria>
    - `FinalizeQuestAsync` `selectedSignups` predicate includes `ps.Player.EmailConfirmed`
    - `UpdateQuestPropertiesWithNotificationsAsync` `withEmail` is built via `affectedPlayers.WhereEmailConfirmed()` before the email-non-empty filter
    - `using EuphoriaInn.Domain.Extensions;` is present in QuestService.cs
    - `dotnet build EuphoriaInn.Domain` exits 0
  </acceptance_criteria>
  <done>Both QuestService dispatch sites exclude unconfirmed users before building recipient arrays; Domain compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard SessionReminderJob and fill in both test files</name>
  <files>EuphoriaInn.Service/Jobs/SessionReminderJob.cs, EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs, EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs</files>
  <read_first>
    - EuphoriaInn.Service/Jobs/SessionReminderJob.cs (targetSignups computed at lines 50-65; the send foreach starts at line 67. The guard must filter targetSignups by `ps.Player.EmailConfirmed` before the loop — applies to BOTH the useYesMaybeVoters branch and the IsSelected branch)
    - EuphoriaInn.UnitTests/Services/SessionReminderJobTests.cs (full file — MakeSignup helper at lines 68-75 builds `Player = new User { ... }`; existing tests show the NSubstitute scope-factory wiring and assertion style)
    - EuphoriaInn.UnitTests/Services/EmailConfirmationJobGuardTests.cs (the Wave 0 placeholder from plan 01 — replace its body)
    - EuphoriaInn.Domain/Services/QuestService.cs (the guarded dispatch sites under test — note QuestService is `internal`; tests reference it via the IQuestService interface and the existing QuestServiceTests fixture pattern)
    - EuphoriaInn.UnitTests/Services/QuestServiceTests.cs (existing fixture: how QuestService is constructed in tests, how IQuestEmailDispatcher / IQuestRepository are substituted, how dispatch calls are asserted with Received/DidNotReceive)
  </read_first>
  <behavior>
    - SessionReminderJob: a signup whose Player.EmailConfirmed=false receives no email even when IsSelected=true and email is present and no prior reminder logged
    - SessionReminderJob: a confirmed selected player still receives the email (regression — existing happy path unaffected)
    - QuestService.FinalizeQuestAsync: dispatcher.EnqueueFinalizedEmail recipientEmails excludes the unconfirmed player's email
    - QuestService.UpdateQuestPropertiesWithNotificationsAsync: dispatcher.EnqueueDateChangedEmail email array excludes the unconfirmed player's email
  </behavior>
  <action>
    In `SessionReminderJob.cs`, add an `EmailConfirmed` guard to the recipient set. After `targetSignups` is assigned (end of the if/else at line 65) and before the `foreach (var signup in targetSignups)` loop, re-bind it to exclude unconfirmed players: `targetSignups = targetSignups.Where(ps => ps.Player != null && ps.Player.EmailConfirmed);`. This covers both the automated IsSelected branch and the DM useYesMaybeVoters branch, and therefore covers DailyReminderJob (which only enqueues SessionReminderJob). Keep the existing per-player email-null and reminder-log dedup checks inside the loop intact.
    In `EmailConfirmationJobGuardTests.cs`, replace the plan-01 placeholder Fact with real tests for the QuestService string[]-array dispatch sites. Reuse the construction/substitution approach from `QuestServiceTests.cs` (substitute `IQuestRepository`, `IPlayerSignupRepository`, `IQuestEmailDispatcher`, `IMapper`; return a quest with PlayerSignups from `GetQuestWithDetailsAsync`). Add at least: (a) a `[Fact]` that finalizes a quest with one confirmed and one unconfirmed selected player and asserts `dispatcher.Received().EnqueueFinalizedEmail(...)` was called with a recipientEmails array that contains the confirmed email and NOT the unconfirmed email (capture the argument via NSubstitute `Arg.Do` or `Received().EnqueueFinalizedEmail(..., Arg.Is<string[]>(a => ...))`); (b) a `[Fact]` for the date-change path asserting the unconfirmed email is excluded from `EnqueueDateChangedEmail`. Build `User` instances with explicit `EmailConfirmed = true/false`.
    In `SessionReminderJobTests.cs`, extend `MakeSignup` (or add inline) to set `EmailConfirmed` on the Player, and add a `[Fact] ExecuteAsync_WhenPlayerEmailNotConfirmed_SkipsPlayer` that builds a selected signup with `Player.EmailConfirmed = false`, runs the job, and asserts `_emailService.DidNotReceive().SendAsync(...)`. Add one positive regression `[Fact]` (or update an existing happy-path test's player to `EmailConfirmed = true`) so a confirmed selected player still triggers exactly one `SendAsync`. Update the existing `MakeSignup` helper to default `EmailConfirmed = true` so prior tests keep passing.
  </action>
  <verify>
    <automated>dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailConfirmationJobGuard|FullyQualifiedName~SessionReminderJobTests"</automated>
  </verify>
  <acceptance_criteria>
    - `SessionReminderJob.cs` filters `targetSignups` on `ps.Player.EmailConfirmed` after the branch assignment and before the send loop
    - `EmailConfirmationJobGuardTests.cs` no longer contains the placeholder Fact; it contains a finalized-email test and a date-change test that assert the unconfirmed email is excluded from the dispatcher call
    - `SessionReminderJobTests.cs` contains a test proving an unconfirmed selected player gets no `SendAsync`, plus a confirmed-player regression test; `MakeSignup` defaults `EmailConfirmed = true`
    - `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailConfirmationJobGuard|FullyQualifiedName~SessionReminderJobTests"` passes (all green)
  </acceptance_criteria>
  <done>SessionReminderJob skips unconfirmed players; both test files prove the guard across all four email paths; filtered suite green.</done>
</task>

</tasks>

<verification>
- `dotnet build EuphoriaInn.Domain` succeeds
- `dotnet test EuphoriaInn.UnitTests --filter "FullyQualifiedName~EmailConfirmationJobGuard|FullyQualifiedName~SessionReminderJobTests"` green
- `dotnet test` (full suite) green — no regression in QuestServiceTests/DailyReminderJobTests after the MakeSignup EmailConfirmed default change
- EmailConfirmed filter present at both QuestService dispatch sites and in SessionReminderJob
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job/service recipient list → SMTP send | The `EmailConfirmed` filter is the last control before an address receives automated mail; an unverified address must never cross it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Spoofing | Automated email to unverified address | mitigate | `EmailConfirmed` filter applied at both QuestService dispatch sites (covers QuestFinalizedEmailJob + QuestDateChangedEmailJob) and inline in SessionReminderJob (covers DailyReminderJob). Unit tests in both test files assert unconfirmed recipients are excluded. |
| T-24-11 | Information Disclosure | Quest details mailed to an unverified inbox | mitigate | Same filter — an attacker who registers an unverified address cannot receive quest schedules/details via any of the four paths. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all guard logic uses existing LINQ + the plan-01 `WhereEmailConfirmed` extension. No audit table required. |
</threat_model>

<success_criteria>
- REQ-24-04: all four email paths (QuestFinalizedEmailJob, QuestDateChangedEmailJob, SessionReminderJob, DailyReminderJob) skip unconfirmed recipients
- Both test files prove the guard; full suite green
- Domain compiles
</success_criteria>

<output>
Create `.planning/phases/24-email-confirmation-flow/24-05-SUMMARY.md` when done
</output>
