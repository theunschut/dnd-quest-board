---
phase: 18-dm-editing-secondary-quest-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml
  - EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css
autonomous: true
requirements:
  - DMVIEW-04

must_haves:
  truths:
    - "DM visiting /Quest/Edit/{id} on mobile sees a single-column glass card form — no sidebar"
    - "HasExistingSignups warning renders when model flag is true"
    - "Existing proposed dates show as readonly text + hidden field + Remove button"
    - "Add Another Date Option button triggers addProposedDate() from site.js"
    - "_QuestFormScripts partial is loaded"
    - "quest-edit.mobile.css is linked via @section Styles"
  artifacts:
    - path: "EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml"
      provides: "Mobile Quest Edit form view"
      contains: "quest-edit-card-mobile"
    - path: "EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css"
      provides: "Glass card CSS for Edit form"
      contains: ".quest-edit-card-mobile"
  key_links:
    - from: "Edit.Mobile.cshtml"
      to: "quest-edit.mobile.css"
      via: "@section Styles link"
      pattern: "quest-edit\\.mobile\\.css"
    - from: "Edit.Mobile.cshtml"
      to: "_QuestFormScripts"
      via: "@section Scripts RenderPartialAsync"
      pattern: "_QuestFormScripts"
---

<objective>
Create Quest/Edit.Mobile.cshtml and quest-edit.mobile.css — the mobile variant of the Quest Edit
form. Single-column glass card layout; omits the Tips sidebar (per D-01); keeps the HasExistingSignups
alert (per D-02); uses readonly+hidden+Remove date rows (per D-03); loads _QuestFormScripts (per D-04).

Purpose: Dungeon Masters editing quests on mobile get a scrollable single-column form with no horizontal overflow.
Output: Edit.Mobile.cshtml + quest-edit.mobile.css
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-CONTEXT.md
@.planning/phases/18-dm-editing-secondary-quest-views/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quest-edit.mobile.css</name>
  <files>EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css</files>
  <read_first>
    - EuphoriaInn.Service/wwwroot/css/dm-create.mobile.css (base pattern — copy glass card values verbatim)
    - EuphoriaInn.Service/wwwroot/css/character-form.mobile.css (16px padding reference)
  </read_first>
  <action>
Create `EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css` with the following exact content.
No @media queries — this file is exclusively loaded by Edit.Mobile.cshtml.

```css
/* quest-edit.mobile.css — Quest Edit mobile view only */
/* No @media queries — exclusively loaded by Quest/Edit.Mobile.cshtml via _Layout.Mobile.cshtml */

/* Glass card container for quest edit form */
.quest-edit-card-mobile {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.3);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    padding: 16px;
}

/* Form labels — parchment text */
.quest-edit-card-mobile .form-label {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Card heading */
.quest-edit-card-mobile h5 {
    color: #F4E4BC !important;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.9), -1px -1px 2px rgba(0,0,0,0.9);
}

/* Help text and small meta — faded parchment */
.quest-edit-card-mobile .form-text,
.quest-edit-card-mobile small {
    color: rgba(244, 228, 188, 0.7) !important;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;
}

/* Proposed date sub-labels — small, faded */
.quest-edit-card-mobile .proposed-date-item .form-label {
    font-size: 0.875rem;
    color: rgba(244, 228, 188, 0.7) !important;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.9) !important;
}

/* Badges inside card — no text-shadow */
.quest-edit-card-mobile .badge {
    text-shadow: none !important;
}
```
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - quest-edit.mobile.css exists at EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css
    - File contains `.quest-edit-card-mobile` selector
    - File contains `background: rgba(255, 255, 255, 0.15)`
    - File contains `backdrop-filter: blur(15px)`
    - File contains `border-radius: 12px`
    - File contains `padding: 16px`
    - File contains `color: #F4E4BC !important` for .form-label
    - File contains NO @media query
  </acceptance_criteria>
  <done>quest-edit.mobile.css created with glass card + parchment text styles for .quest-edit-card-mobile</done>
</task>

<task type="auto">
  <name>Task 2: Create Quest/Edit.Mobile.cshtml</name>
  <files>EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml</files>
  <read_first>
    - EuphoriaInn.Service/Views/Quest/Edit.cshtml (desktop source — model binding, date loop, form fields, _QuestFormScripts)
    - EuphoriaInn.Service/Views/Quest/Create.Mobile.cshtml (mobile pattern — glass card structure, @section Styles, @section Scripts, container-fluid px-2 mt-2)
    - EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css (just created — class name to use)
  </read_first>
  <action>
Create `EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml`. Follow these exact specifications:

**Model/using directives** (copy from Edit.cshtml top):
```razor
@using EuphoriaInn.Domain.Interfaces
@using EuphoriaInn.Service.ViewModels.QuestViewModels
@model EditQuestViewModel
@{
    ViewData["Title"] = $"Edit Quest: {Model.Quest.Title}";
}
```

**@section Styles** — link quest-edit.mobile.css:
```razor
@section Styles {
    <link href="~/css/quest-edit.mobile.css" asp-append-version="true" rel="stylesheet" />
}
```

**Layout** — container-fluid px-2 mt-2 wrapping a single `.quest-edit-card-mobile mb-3` div (no Tips sidebar per D-01):
```html
<div class="container-fluid px-2 mt-2">
    <div class="quest-edit-card-mobile mb-3">
        ...
    </div>
</div>
```

**Card heading** (h5, parchment, fa-edit icon):
```html
<div class="mb-3">
    <h5 class="mb-0">
        <i class="fas fa-edit text-warning me-2"></i>Edit Quest: @Model.Quest.Title
    </h5>
</div>
```

**HasExistingSignups warning** (per D-02 — conditional alert-warning BEFORE the form):
```razor
@if (Model.HasExistingSignups)
{
    <div class="alert alert-warning">
        <i class="fas fa-exclamation-triangle me-2"></i>
        <strong>Note:</strong> Players have already signed up for this quest.
        Removing or significantly changing dates will notify affected players via email and remove their votes for those dates.
    </div>
}
```

**Form** — `asp-action="Edit" asp-route-id="@Model.Id" method="post"`:
```html
<div asp-validation-summary="All" class="text-danger mb-3"></div>
<form asp-action="Edit" asp-route-id="@Model.Id" method="post">
    <input type="hidden" asp-for="Id" />
    ... fields ...
</form>
```

**Form fields** (copy labels, inputs, validation spans from Edit.cshtml — same asp-for bindings):
- Title: `asp-for="Quest.Title"` input.form-control
- Description: `asp-for="Quest.Description"` textarea rows="4"
- ChallengeRating: `asp-for="Quest.ChallengeRating"` type="number" min="1" max="20" step="1"
- TotalPlayerCount: `asp-for="Quest.TotalPlayerCount"` inputmode="numeric"
- DungeonMasterSession: `asp-for="Quest.DungeonMasterSession"` form-check-input checkbox with `<i class="fas fa-crown text-warning me-2"></i>Dungeon Master Session Only` label

**Proposed Dates section** (per D-03 — readonly text + hidden + Remove):
```razor
<div class="mb-3">
    <label class="form-label">Proposed Dates &amp; Times <span class="text-danger">*</span></label>
    <div id="proposed-dates">
        @{
            var sortedProposedDates = Model.Quest.ProposedDates.OrderBy(pd => pd).ToList();
        }
        @for (int i = 0; i < sortedProposedDates.Count; i++)
        {
            <div class="mb-3 proposed-date-item">
                <label class="form-label small">Proposed Date @(i + 1)</label>
                <div class="input-group">
                    <input type="text" class="form-control" readonly value="@sortedProposedDates[i].ToString("dddd, MMMM dd, yyyy 'at' h:mm tt")" />
                    <input type="hidden" name="Quest.ProposedDates[@i]" value="@sortedProposedDates[i].ToString("yyyy-MM-ddTHH:mm")" />
                    <button type="button" class="btn btn-danger" onclick="removeProposedDate(this)">
                        <i class="fas fa-trash me-1"></i>Remove
                    </button>
                </div>
            </div>
        }
    </div>
    <button type="button" class="btn btn-warning btn-sm" onclick="addProposedDate()">
        <i class="fas fa-plus me-1"></i>Add Another Date Option
    </button>
    <span asp-validation-for="Quest.ProposedDates" class="text-danger d-block mt-1"></span>
</div>
```

**Action buttons** — d-flex gap-2 (per UI-SPEC copywriting: "Update Quest" + "Back to Quest"):
```html
<div class="d-flex gap-2 mt-3">
    <button type="submit" class="btn btn-warning flex-fill">
        <i class="fas fa-save me-2"></i>Update Quest
    </button>
    <a href="@Url.Action("Manage", "Quest", new { id = Model.Id })" class="btn btn-secondary flex-fill">
        <i class="fas fa-arrow-left me-2"></i>Back to Quest
    </a>
</div>
```

**@section Scripts** — render _QuestFormScripts partial (per D-04):
```razor
@section Scripts {
    @{ await Html.RenderPartialAsync("_QuestFormScripts"); }
}
```

Do NOT add @inject — services are available globally via _ViewImports.cshtml.
Do NOT set Layout — _ViewStart.cshtml handles layout selection.
Do NOT include the Tips sidebar.
  </action>
  <verify>
    <automated>dotnet build EuphoriaInn.Service --no-restore -q</automated>
  </verify>
  <acceptance_criteria>
    - Edit.Mobile.cshtml exists at EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml
    - File contains `@model EditQuestViewModel`
    - File contains `quest-edit-card-mobile`
    - File contains `quest-edit.mobile.css`
    - File contains `asp-for="Quest.Title"`
    - File contains `asp-for="Quest.Description"`
    - File contains `asp-for="Quest.ChallengeRating"`
    - File contains `asp-for="Quest.TotalPlayerCount"`
    - File contains `asp-for="Quest.DungeonMasterSession"`
    - File contains `Model.HasExistingSignups`
    - File contains `alert-warning`
    - File contains `type="hidden" name="Quest.ProposedDates`
    - File contains `readonly`
    - File contains `removeProposedDate(this)`
    - File contains `addProposedDate()`
    - File contains `_QuestFormScripts`
    - File does NOT contain `Quest Editing Tips`
    - File does NOT contain `col-lg-4`
    - dotnet build EuphoriaInn.Service exits 0
  </acceptance_criteria>
  <done>Quest/Edit.Mobile.cshtml created; DM can edit quest on mobile with full single-column form, date management, and _QuestFormScripts validation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile browser → POST /Quest/Edit | Untrusted form input from mobile DM; same validation path as desktop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | Edit.Mobile.cshtml hidden asp-for="Id" | accept | ASP.NET Core model binding + DM authorization already enforced by QuestController.Edit; no new surface area |
| T-18-01-02 | Spoofing | Quest.ProposedDates hidden fields | accept | Server-side re-validates ownership in QuestService; mobile view is additive, no new auth bypass |
</threat_model>

<verification>
1. `dotnet build EuphoriaInn.Service --no-restore -q` exits 0
2. `EuphoriaInn.Service/Views/Quest/Edit.Mobile.cshtml` exists
3. `EuphoriaInn.Service/wwwroot/css/quest-edit.mobile.css` exists
4. Grep `quest-edit-card-mobile` in Edit.Mobile.cshtml — must match
5. Grep `quest-edit.mobile.css` in Edit.Mobile.cshtml — must match
6. Grep `_QuestFormScripts` in Edit.Mobile.cshtml — must match
7. Grep `Model.HasExistingSignups` in Edit.Mobile.cshtml — must match
8. Grep `Quest Editing Tips` in Edit.Mobile.cshtml — must NOT match
</verification>

<success_criteria>
- Edit.Mobile.cshtml compiles cleanly
- Glass card class `.quest-edit-card-mobile` used in view and defined in CSS
- All form fields present with correct asp-for bindings
- HasExistingSignups alert conditional is present
- Readonly date rows with hidden fields and Remove buttons are present
- _QuestFormScripts partial rendered in @section Scripts
- Tips sidebar absent
</success_criteria>

<output>
After completion, create `.planning/phases/18-dm-editing-secondary-quest-views/18-01-SUMMARY.md`
</output>
